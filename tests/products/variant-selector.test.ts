import { describe, it } from 'vitest';

// PDPG-02: Variant selection
describe('Variant Selector (PDPG-02)', () => {
  it.todo('updates displayed price when a variant is selected');
  it.todo('updates displayed SKU when a variant is selected');
  it.todo('updates stock status when a variant is selected');
  it.todo('disables unavailable variant option combinations');
  it.todo('shows which option groups are available (e.g. Size, Color)');
  it.todo('pre-selects first available variant on initial render');
  it.todo('marks currently selected variant options as active');
});
