import { describe, it } from 'vitest';

describe('Category Attributes', () => {
  it.todo('creates category attribute with SELECT type and values');
  it.todo('creates category attribute with RANGE type');
  it.todo('validates SELECT type requires non-empty values array');
  it.todo('enforces unique key per category');
  it.todo('updates attribute position for reordering');
  it.todo('deletes attribute and cascades');
});
