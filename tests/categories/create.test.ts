import { describe, it } from 'vitest';

describe('Category Creation', () => {
  it.todo('creates root category with correct path');
  it.todo('creates child category with materialized path from parent');
  it.todo('auto-generates slug from name');
  it.todo('validates unique slug constraint');
  it.todo('sets depth based on parent depth');
  it.todo('enforces maximum category depth of 5');
});
