import { describe, it } from 'vitest';

describe('Category SEO Metadata', () => {
  it.todo('returns metaTitle when set, falls back to name');
  it.todo('returns metaDescription when set, falls back to description');
  it.todo('generates canonical URL from slug');
  it.todo('includes Open Graph metadata');
});
