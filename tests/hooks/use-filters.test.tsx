import { describe, it } from 'vitest';

describe('useFilters', () => {
  it.todo('initializes with default filter values');

  it.todo('persists filter state to URL search params');

  it.todo('restores filter state from URL on mount');

  it.todo('resets page to 1 when filters change');

  it.todo('clearOnDefault removes default values from URL');
});
