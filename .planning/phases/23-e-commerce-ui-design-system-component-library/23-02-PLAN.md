---
phase: 23-e-commerce-ui-design-system-component-library
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - apps/client/src/components/layout/site-header.tsx
  - apps/client/src/components/layout/header-actions.tsx
  - apps/client/src/components/layout/announcement-bar.tsx
  - apps/client/src/components/layout/mobile-menu-drawer.tsx
  - apps/client/src/components/layout/site-footer.tsx
  - apps/client/src/components/layout/footer-link-group.tsx
  - apps/client/src/app/layout.tsx
autonomous: true
requirements:
  - DS-03

must_haves:
  truths:
    - "Site header is sticky, starts transparent over hero, becomes solid white with border on scroll"
    - "Mobile menu opens as a full-height left drawer with accordion category navigation"
    - "Footer has 4-column link layout on desktop, accordion on mobile, with newsletter signup"
    - "Announcement bar is dismissible and rotates promo messages"
  artifacts:
    - path: "apps/client/src/components/layout/site-header.tsx"
      provides: "Sticky header with transparent-to-solid scroll behavior"
      exports: ["SiteHeader"]
    - path: "apps/client/src/components/layout/mobile-menu-drawer.tsx"
      provides: "Full-height left slide-in with accordion categories"
      exports: ["MobileMenuDrawer"]
    - path: "apps/client/src/components/layout/site-footer.tsx"
      provides: "4-column footer with newsletter and accordion mobile"
      exports: ["SiteFooter"]
    - path: "apps/client/src/app/layout.tsx"
      provides: "Updated root layout using SiteHeader and SiteFooter"
  key_links:
    - from: "apps/client/src/components/layout/site-header.tsx"
      to: "apps/client/src/components/ui/sheet.tsx"
      via: "Sheet for mobile menu"
      pattern: "import.*Sheet"
    - from: "apps/client/src/app/layout.tsx"
      to: "apps/client/src/components/layout/site-header.tsx"
      via: "Component composition"
      pattern: "import.*SiteHeader"
---

<objective>
Build the site layout shell: header with transparent-to-solid scroll behavior, announcement bar, mobile menu drawer, and 4-column footer. Wire into root layout.

Purpose: The layout wraps every page and establishes the luxury brand feel. Current header is a basic nav bar. This transforms it into a polished e-commerce header with mobile support.
Output: 6 layout components and updated root layout.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-e-commerce-ui-design-system-component-library/UI-RESEARCH.md
@apps/client/src/app/layout.tsx
@apps/client/src/components/navigation/mega-menu.tsx

<interfaces>
From apps/client/src/components/ui/sheet.tsx (created in Plan 01):
```typescript
export function Sheet({ children, open, onOpenChange }: SheetProps): JSX.Element;
export function SheetTrigger({ children, asChild }: SheetTriggerProps): JSX.Element;
export function SheetContent({ children, side, className }: SheetContentProps): JSX.Element;
export function SheetHeader({ children }: { children: React.ReactNode }): JSX.Element;
export function SheetTitle({ children }: { children: React.ReactNode }): JSX.Element;
```

From apps/client/src/components/ui/accordion.tsx (created in Plan 01):
```typescript
export function AccordionItem({ title, children, defaultOpen }: AccordionItemProps): JSX.Element;
```

From apps/client/src/lib/cn.ts (created in Plan 01):
```typescript
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SiteHeader with scroll behavior, AnnouncementBar, and HeaderActions</name>
  <files>apps/client/src/components/layout/site-header.tsx, apps/client/src/components/layout/header-actions.tsx, apps/client/src/components/layout/announcement-bar.tsx</files>
  <action>
**AnnouncementBar (`announcement-bar.tsx`):**
- "use client". Dismissible top banner, 36px height, bg-accent text-accent-foreground.
- Prop: `messages: string[]` — rotates every 5 seconds using setInterval + useState index.
- Close X button on right side. Stores dismissed state in sessionStorage key `announcement-dismissed`.
- Text: uppercase, text-[11px], tracking-widest, text-center.
- Animate message transitions with opacity fade (CSS transition).

**HeaderActions (`header-actions.tsx`):**
- "use client". Right-side icon group: search toggle, account (UserButton from Clerk), wishlist badge, cart badge.
- Import WishlistHeaderBadge from existing `@/components/wishlist/wishlist-header-badge`.
- Search icon: magnifying glass SVG, onClick toggles search overlay (just dispatches a custom event `toggle-search` for now — SearchBar will listen).
- Cart icon: shopping bag SVG with count badge (reads from cart store if available, else shows nothing).
- Account: renders UserButton if authenticated, SignInButton if not. Receive `userId` as prop from server parent.
- All icons 20px, text-foreground, hover:text-foreground-muted transition.

**SiteHeader (`site-header.tsx`):**
- "use client". Sticky header with transparent-to-solid scroll behavior.
- Structure (top to bottom): AnnouncementBar (optional) → main header row → MegaMenu (from existing component)
- Main header row: logo (left), main nav links (center, hidden on mobile), HeaderActions (right), hamburger menu button (mobile only, left side before logo).
- Scroll behavior: `useEffect` with scroll listener. When `scrollY > 50`, add `bg-white border-b border-border shadow-sm` classes. When at top, remove them (transparent over hero content).
- Use `cn()` for conditional classes. Height: 72px (matching --spacing-header token).
- Logo: text link "STORE" in font-display, text-lg, tracking-[0.2em], uppercase.
- Nav links: "New Arrivals", "Women", "Men", "Collections" — text-xs uppercase tracking-widest, hover:text-foreground-muted.
- Hamburger: three lines icon, visible only on `lg:hidden`.
- The header must accept a `transparent` prop (default false) that enables the transparent-over-hero behavior. When transparent=true AND scrollY<50: text becomes white, logo becomes white. When not transparent: always solid white bg.
- Pass `onMenuOpen` callback that parent will wire to MobileMenuDrawer.
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && test -f apps/client/src/components/layout/site-header.tsx && test -f apps/client/src/components/layout/header-actions.tsx && test -f apps/client/src/components/layout/announcement-bar.tsx && grep -q "use client" apps/client/src/components/layout/site-header.tsx && grep -q "scrollY" apps/client/src/components/layout/site-header.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SiteHeader renders with transparent-to-solid scroll behavior. AnnouncementBar rotates messages and is dismissible. HeaderActions shows search/account/wishlist/cart icons.</done>
</task>

<task type="auto">
  <name>Task 2: MobileMenuDrawer, SiteFooter, and layout integration</name>
  <files>apps/client/src/components/layout/mobile-menu-drawer.tsx, apps/client/src/components/layout/site-footer.tsx, apps/client/src/components/layout/footer-link-group.tsx, apps/client/src/app/layout.tsx</files>
  <action>
**MobileMenuDrawer (`mobile-menu-drawer.tsx`):**
- "use client". Uses Sheet component (side="left") from `@/components/ui/sheet`.
- Props: `open: boolean`, `onOpenChange: (open: boolean) => void`.
- Content: Full-height left panel with:
  - Close button top-right
  - Navigation sections using AccordionItem: "Shop" (with sub-links: New Arrivals, Women, Men, Collections), "Customer Care" (with sub-links: Contact, Shipping, Returns)
  - Bottom section: sign in link, country selector placeholder text
- Links: text-sm, py-3, border-b border-border, uppercase tracking-wider.
- AccordionItem for expandable sections with category sub-links.

**FooterLinkGroup (`footer-link-group.tsx`):**
- Props: `title: string`, `links: { label: string; href: string }[]`.
- Desktop: heading (text-xs uppercase tracking-widest mb-4) + link list (text-sm text-foreground-muted, hover:text-foreground).
- Mobile: wraps in AccordionItem for collapsible behavior. Use responsive: AccordionItem on `lg:hidden`, static list on `hidden lg:block`.

**SiteFooter (`site-footer.tsx`):**
- 4 columns on desktop (lg:grid-cols-4), stacked accordions on mobile.
- Column groups: "Shop" (New Arrivals, Women, Men, Sale), "Help" (Customer Service, Shipping Info, Returns, Size Guide), "About" (Our Story, Sustainability, Careers, Press), "Legal" (Privacy Policy, Terms of Service, Cookie Policy).
- Newsletter section: heading "Join Our World", text-sm description, email input + Button (from ui/button).
- Bottom bar: copyright text, payment icons placeholder (Visa, MC, Amex, PayPal as text for now), social media links (Instagram, Twitter, Pinterest as text links).
- Colors: bg-surface for footer bg, text-foreground-muted for links.
- Generous padding: py-16 for main section, py-6 for bottom bar.

**Layout integration (`layout.tsx`):**
- Replace the existing inline header in `apps/client/src/app/layout.tsx` with the new layout components.
- Import SiteHeader and SiteFooter. Create a client wrapper component `LayoutShell` in the same file (or inline) that manages mobile menu state.
- Structure: `<AnnouncementBar>` + `<SiteHeader>` + `<MobileMenuDrawer>` + `<main>{children}</main>` + `<SiteFooter>`.
- Keep ClerkProvider, NuqsAdapter, ChatWidget, and MegaMenu (MegaMenu stays inside SiteHeader or below it).
- Remove the old inline `<header>` and `<nav>` blocks.
- Pass `userId` to HeaderActions from server-side auth() call.
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && test -f apps/client/src/components/layout/mobile-menu-drawer.tsx && test -f apps/client/src/components/layout/site-footer.tsx && test -f apps/client/src/components/layout/footer-link-group.tsx && grep -q "SiteHeader" apps/client/src/app/layout.tsx && grep -q "SiteFooter" apps/client/src/app/layout.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>MobileMenuDrawer opens from left with accordion navigation. SiteFooter has 4-column desktop layout with mobile accordions and newsletter. Root layout uses SiteHeader + SiteFooter replacing the old inline header.</done>
</task>

</tasks>

<verification>
- SiteHeader has scroll-based class toggling (transparent to solid)
- MobileMenuDrawer uses Sheet (side="left") from UI primitives
- SiteFooter renders 4 link groups with accordion behavior on mobile
- Root layout imports and renders SiteHeader + SiteFooter
- AnnouncementBar rotates messages and can be dismissed
</verification>

<success_criteria>
The client app has a polished layout shell with luxury aesthetic: sticky header with transparent-to-solid scroll, dismissible announcement bar, mobile navigation drawer, and 4-column footer with newsletter signup. The old inline header is fully replaced.
</success_criteria>

<output>
After completion, create `.planning/phases/23-e-commerce-ui-design-system-component-library/23-02-SUMMARY.md`
</output>
