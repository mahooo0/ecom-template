---
phase: 23-e-commerce-ui-design-system-component-library
plan: 04
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - apps/client/src/components/product/product-card.tsx
  - apps/client/src/components/product/product-card-image.tsx
  - apps/client/src/components/product/product-card-badge.tsx
  - apps/client/src/components/product/product-card-quick-add.tsx
  - apps/client/src/components/product/color-swatches.tsx
  - apps/client/src/components/product/product-grid.tsx
  - apps/client/src/components/product/sort-selector.tsx
  - apps/client/src/components/product/pagination.tsx
  - apps/client/src/components/product/product-count.tsx
  - apps/client/src/components/product/view-toggle.tsx
  - apps/client/src/components/product/plp-skeleton.tsx
autonomous: true
requirements:
  - DS-05

must_haves:
  truths:
    - "Product card shows no border/shadow/radius with image swap on hover (crossfade to second image)"
    - "Badges appear on product cards for New, Sale, and discount percentage"
    - "Color swatches on card change the displayed image"
    - "Quick-add button appears on card hover"
    - "Product grid uses tight 1-8px gaps matching luxury aesthetic"
    - "PLP skeleton shows pulsing gray rectangles during loading"
  artifacts:
    - path: "apps/client/src/components/product/product-card.tsx"
      provides: "Redesigned product card with luxury aesthetic"
      exports: ["ProductCard"]
    - path: "apps/client/src/components/product/product-card-image.tsx"
      provides: "3:4 aspect ratio image with crossfade hover"
      exports: ["ProductCardImage"]
    - path: "apps/client/src/components/product/product-grid.tsx"
      provides: "Responsive grid with tight gaps"
      exports: ["ProductGrid"]
  key_links:
    - from: "apps/client/src/components/product/product-card.tsx"
      to: "apps/client/src/components/product/product-card-image.tsx"
      via: "Image composition"
      pattern: "import.*ProductCardImage"
    - from: "apps/client/src/components/product/product-card.tsx"
      to: "apps/client/src/components/product/product-card-badge.tsx"
      via: "Badge overlay"
      pattern: "import.*ProductCardBadge"
---

<objective>
Redesign the product listing components to match the luxury minimalist aesthetic: no borders/shadows/radius, image swap on hover, badges, color swatches, and quick-add.

Purpose: Product cards and the grid are the most-viewed components in any e-commerce app. The current cards have rounded corners, blue CTAs, and borders — all of which conflict with the minimalist luxury direction.
Output: 11 product listing components redesigned to luxury spec.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-e-commerce-ui-design-system-component-library/UI-RESEARCH.md
@apps/client/src/components/product/product-card.tsx
@apps/client/src/components/product/product-grid.tsx
@apps/client/src/components/product/wishlist-button.tsx
@apps/client/src/components/product/compare-checkbox.tsx

<interfaces>
From apps/client/src/lib/cn.ts (created in Plan 01):
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From apps/client/src/components/ui/button.tsx (created in Plan 01):
```typescript
export function Button({ variant, size, ... }: ButtonProps): JSX.Element;
```

From apps/client/src/components/ui/badge.tsx (created in Plan 01):
```typescript
export function Badge({ variant, className, children }: BadgeProps): JSX.Element;
```

From apps/client/src/components/ui/skeleton.tsx (created in Plan 01):
```typescript
export function SkeletonText(props): JSX.Element;
export function SkeletonImage(props): JSX.Element;
export function SkeletonCard(props): JSX.Element;
```

Existing product type from @repo/types:
```typescript
export interface Product {
  id: string;
  name: string;
  slug: string;
  price: number;
  compareAtPrice?: number | null;
  images: string[];
  status: string;
  type: string;
  brand?: { name: string };
  tags?: { name: string }[];
  createdAt: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProductCard sub-components (Image, Badge, QuickAdd, ColorSwatches)</name>
  <files>apps/client/src/components/product/product-card-image.tsx, apps/client/src/components/product/product-card-badge.tsx, apps/client/src/components/product/product-card-quick-add.tsx, apps/client/src/components/product/color-swatches.tsx</files>
  <action>
**ProductCardImage (`product-card-image.tsx`):**
- Props: `primarySrc: string`, `secondarySrc?: string`, `alt: string`, `aspectRatio?: string`.
- Container: `aspect-[3/4]` (override via prop), `overflow-hidden`, `bg-surface` (gray fallback).
- Primary image: `next/image` with `fill`, `object-cover`, `sizes` responsive.
- Secondary image: If provided, render second `next/image` layered on top with `opacity-0 group-hover/card:opacity-100 transition-opacity duration-500`. This creates the crossfade hover effect.
- Both images: `transition-transform duration-700 ease-out-expo group-hover/card:scale-105` for subtle zoom on hover.
- Handle missing images: show bg-surface placeholder with minimal camera icon.

**ProductCardBadge (`product-card-badge.tsx`):**
- Props: `product: { isNew?: boolean; compareAtPrice?: number | null; price: number; status?: string }`.
- Position: absolute top-2 left-2 z-10. Stack vertically if multiple badges.
- Logic: if `isNew` (or `createdAt` within 14 days), show Badge variant="new" with "NEW". If `compareAtPrice > price`, show Badge variant="sale" with calculated discount percentage like "-30%". If `status === 'PRE_ORDER'`, show Badge variant="default" with "PRE-ORDER".
- Uses Badge component from `@/components/ui/badge`.

**ProductCardQuickAdd (`product-card-quick-add.tsx`):**
- "use client". Props: `productId: string`, `productSlug: string`, `hasVariants: boolean`.
- Appears on hover: `opacity-0 group-hover/card:opacity-100 transition-opacity duration-normal`.
- Position: absolute bottom-0 left-0 right-0 (overlays bottom of image area).
- If `hasVariants`: render a link "Select Options" that navigates to `/products/${productSlug}`.
- If no variants: render a "+" button (Button variant="solid", size="sm") that adds to cart. For now, just log to console — cart integration comes from existing cart store.
- Background: bg-white/90 backdrop-blur-sm py-2 px-3.

**ColorSwatches (`color-swatches.tsx`):**
- "use client". Props: `colors: Array<{ name: string; hex: string; imageIndex: number }>`, `onSelect: (imageIndex: number) => void`, `selectedIndex?: number`.
- Small circles (w-4 h-4, rounded-full) with the color hex as background.
- Max 5 swatches visible + "+N" overflow indicator.
- Active swatch has ring-1 ring-offset-1 ring-foreground.
- On click, calls `onSelect(imageIndex)` to change displayed image.
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && for f in product-card-image product-card-badge product-card-quick-add color-swatches; do test -f "apps/client/src/components/product/$f.tsx" || echo "MISSING: $f"; done && grep -q "group-hover" apps/client/src/components/product/product-card-image.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>ProductCardImage has crossfade hover with secondary image. ProductCardBadge shows NEW/SALE/-30% badges. ProductCardQuickAdd appears on hover. ColorSwatches show small color circles with selection ring.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign ProductCard, ProductGrid, and listing utilities</name>
  <files>apps/client/src/components/product/product-card.tsx, apps/client/src/components/product/product-grid.tsx, apps/client/src/components/product/sort-selector.tsx, apps/client/src/components/product/pagination.tsx, apps/client/src/components/product/product-count.tsx, apps/client/src/components/product/view-toggle.tsx, apps/client/src/components/product/plp-skeleton.tsx</files>
  <action>
**ProductCard (`product-card.tsx`) — FULL REWRITE:**
- Remove: border, rounded-lg, shadow, blue "Add to Cart" button, padding wrapper.
- "use client" (needed for hover state tracking for color swatches).
- Structure: `group/card` container with NO border, NO shadow, NO border-radius.
  - ProductCardImage (primary = images[0], secondary = images[1])
  - ProductCardBadge (top-left overlay inside image area)
  - WishlistButton (existing, top-right overlay inside image area, opacity on hover)
  - CompareCheckbox (existing, moved or kept top-left, below badge)
  - ProductCardQuickAdd (bottom of image area, on hover)
  - Text area below image: NO padding container, just mt-3 spacing.
    - Brand name: text-[11px] uppercase tracking-widest text-foreground-muted.
    - Product name: text-sm font-normal text-foreground mt-1 line-clamp-1 (NOT bold — luxury pattern uses normal weight).
    - Price: text-sm mt-1. Sale: current price in text-foreground + compare-at in line-through text-foreground-muted. Regular: just text-foreground.
  - ColorSwatches below price if product has color variants (check for `variants` prop or `colors` prop). Optional — only render if data available.
- Keep existing ProductCardProps interface but extend: add optional `isNew?: boolean`, `colors?: Array<{name: string; hex: string; imageIndex: number}>`.
- Maintain existing WishlistButton and CompareCheckbox integration.

**ProductGrid (`product-grid.tsx`) — REWRITE:**
- Props: `children: React.ReactNode`, `columns?: 2 | 3 | 4`, `gap?: 'tight' | 'normal'`.
- Tight gap (default): gap-px (1px) or gap-1 (4px). Normal gap: gap-4.
- Responsive: `grid-cols-2 md:grid-cols-3 lg:grid-cols-${columns || 4}`.
- No background, no padding — pure grid.

**SortSelector (`sort-selector.tsx`) — RESTYLE:**
- Keep existing functionality but restyle to luxury: text-xs uppercase tracking-widest, no border on select, underline pattern instead. Custom dropdown arrow SVG.

**Pagination (`pagination.tsx`) — RESTYLE:**
- Keep existing logic but restyle: text-xs, active page gets bg-accent text-accent-foreground (black bg, white text). No rounded-full buttons. Square buttons with no border-radius.

**ProductCount (`product-count.tsx`):**
- New component. Props: `showing: number`, `total: number`.
- Text: `"Showing ${showing} of ${total} products"`, text-xs text-foreground-muted uppercase tracking-wider.

**ViewToggle (`view-toggle.tsx`):**
- "use client". Props: `view: 'grid' | 'list'`, `onViewChange: (view: 'grid' | 'list') => void`.
- Two icon buttons (grid icon = 4 squares, list icon = 3 lines). Active: text-foreground. Inactive: text-foreground-muted.
- Small: w-8 h-8 each.

**PLPSkeleton (`plp-skeleton.tsx`):**
- Uses Skeleton components from `@/components/ui/skeleton`.
- Renders a grid of 8 skeleton cards: SkeletonImage (aspect-[3/4]) + 3 lines of SkeletonText beneath.
- Same grid layout as ProductGrid.
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && grep -qv "rounded-lg" apps/client/src/components/product/product-card.tsx && grep -qv "bg-blue-600" apps/client/src/components/product/product-card.tsx && test -f apps/client/src/components/product/plp-skeleton.tsx && test -f apps/client/src/components/product/product-count.tsx && test -f apps/client/src/components/product/view-toggle.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>ProductCard has no border/shadow/radius, uses crossfade hover image, brand above name in small uppercase, normal weight text. ProductGrid uses tight gaps. PLP skeleton, ProductCount, and ViewToggle are created. SortSelector and Pagination are restyled.</done>
</task>

</tasks>

<verification>
- ProductCard has NO border-radius, NO box-shadow, NO blue CTA button
- ProductCardImage renders two images with crossfade on hover
- ProductGrid uses gap-px or gap-1 for tight luxury gaps
- ProductCardBadge calculates and displays discount percentage
- PLPSkeleton renders 8 pulsing skeleton cards
- All components use cn() for class merging
</verification>

<success_criteria>
Product listing components match the luxury minimalist aesthetic defined in the research: no chrome, image-first with crossfade hover, tight grid gaps, subtle typography with brand name above product name, and badges for new/sale items. The blue "Add to Cart" button is eliminated in favor of a hover-revealed quick-add.
</success_criteria>

<output>
After completion, create `.planning/phases/23-e-commerce-ui-design-system-component-library/23-04-SUMMARY.md`
</output>
