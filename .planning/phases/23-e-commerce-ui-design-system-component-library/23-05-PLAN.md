---
phase: 23-e-commerce-ui-design-system-component-library
plan: 05
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - apps/client/src/components/ui/back-to-top.tsx
  - apps/client/src/components/ui/empty-state.tsx
  - apps/client/src/components/ui/cookie-consent.tsx
  - apps/client/src/components/product/product-accordion.tsx
  - apps/client/src/components/product/stock-indicator.tsx
  - apps/client/src/components/product/delivery-estimate.tsx
  - apps/client/src/app/layout.tsx
autonomous: true
requirements:
  - DS-06
  - DS-07

must_haves:
  truths:
    - "Back to top button appears after scrolling 500px and smoothly scrolls to top on click"
    - "Empty state component shows icon, heading, description, and CTA button"
    - "Cookie consent banner appears at bottom with Accept and Customize buttons"
    - "Product accordion uses the Accordion primitive for collapsible Description, Materials, Shipping sections"
    - "Stock indicator shows colored dot with stock status text"
    - "Delivery estimate shows truck icon with estimated delivery date"
  artifacts:
    - path: "apps/client/src/components/ui/back-to-top.tsx"
      provides: "Fixed bottom-right scroll-to-top button"
      exports: ["BackToTop"]
    - path: "apps/client/src/components/ui/empty-state.tsx"
      provides: "Reusable empty state with icon, heading, CTA"
      exports: ["EmptyState"]
    - path: "apps/client/src/components/ui/cookie-consent.tsx"
      provides: "Bottom banner with accept/customize"
      exports: ["CookieConsent"]
    - path: "apps/client/src/components/product/product-accordion.tsx"
      provides: "Product detail accordion sections"
      exports: ["ProductAccordion"]
  key_links:
    - from: "apps/client/src/components/product/product-accordion.tsx"
      to: "apps/client/src/components/ui/accordion.tsx"
      via: "AccordionItem composition"
      pattern: "import.*AccordionItem"
    - from: "apps/client/src/app/layout.tsx"
      to: "apps/client/src/components/ui/back-to-top.tsx"
      via: "Global component in layout"
      pattern: "import.*BackToTop"
---

<objective>
Create shared utility components (BackToTop, EmptyState, CookieConsent) and product detail enhancements (ProductAccordion, StockIndicator, DeliveryEstimate). Wire global components into layout.

Purpose: These components complete the design system by addressing cross-cutting UI needs (empty states, cookie compliance, scroll navigation) and enhancing the product detail page with luxury-standard information display.
Output: 6 components and updated layout with global utilities.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-e-commerce-ui-design-system-component-library/UI-RESEARCH.md

<interfaces>
From apps/client/src/components/ui/accordion.tsx (created in Plan 01):
```typescript
export function AccordionItem({ title, children, defaultOpen }: AccordionItemProps): JSX.Element;
```

From apps/client/src/components/ui/button.tsx (created in Plan 01):
```typescript
export function Button({ variant, size, ... }: ButtonProps): JSX.Element;
```

From apps/client/src/components/ui/toast.tsx (created in Plan 01):
```typescript
export function Toaster(): JSX.Element;
```

From apps/client/src/lib/cn.ts (created in Plan 01):
```typescript
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared utility components (BackToTop, EmptyState, CookieConsent)</name>
  <files>apps/client/src/components/ui/back-to-top.tsx, apps/client/src/components/ui/empty-state.tsx, apps/client/src/components/ui/cookie-consent.tsx</files>
  <action>
**BackToTop (`back-to-top.tsx`):**
- "use client". Fixed bottom-6 right-6 z-40.
- Visibility: hidden by default. Shows when `scrollY > 500` (useEffect with scroll listener). Use `opacity-0 pointer-events-none` to `opacity-100 pointer-events-auto` transition (duration-normal).
- Appearance: w-10 h-10 bg-accent text-accent-foreground flex items-center justify-center. NO border-radius (luxury square pattern). Shadow-sm.
- Icon: chevron-up SVG (16px).
- onClick: `window.scrollTo({ top: 0, behavior: 'smooth' })`.
- Accessible: aria-label="Back to top".

**EmptyState (`empty-state.tsx`):**
- Props: `icon?: React.ReactNode`, `title: string`, `description?: string`, `ctaText?: string`, `ctaHref?: string`, `onCtaClick?: () => void`.
- Layout: flex flex-col items-center text-center py-20.
- Icon: text-foreground-muted mb-4, default generic empty box SVG if not provided.
- Title: text-lg font-medium. Description: text-sm text-foreground-muted mt-2 max-w-sm.
- CTA: Button variant="outline" mt-6 if ctaText provided. Renders as `<a>` if ctaHref, `<button>` if onCtaClick.

**CookieConsent (`cookie-consent.tsx`):**
- "use client". Fixed bottom-0 left-0 right-0 z-50.
- Shows if `localStorage.getItem('cookie-consent')` is null. Hidden after consent given.
- Appearance: bg-surface border-t border-border py-4 px-6. Flex row: text (left) + buttons (right).
- Text: text-sm text-foreground-muted "We use cookies to enhance your experience. By continuing to visit this site you agree to our use of cookies."
- Buttons: "Accept All" (Button variant="solid" size="sm") — sets localStorage and hides. "Customize" (Button variant="outline" size="sm") — for now just sets localStorage and hides (placeholder for full settings).
- Slide-up animation on mount: translate-y-full to translate-y-0.
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && for f in back-to-top empty-state cookie-consent; do test -f "apps/client/src/components/ui/$f.tsx" || echo "MISSING: $f"; done && grep -q "scrollTo" apps/client/src/components/ui/back-to-top.tsx && grep -q "localStorage" apps/client/src/components/ui/cookie-consent.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>BackToTop appears after 500px scroll with smooth scroll-to-top. EmptyState shows icon/title/description/CTA. CookieConsent persists consent in localStorage with slide-up animation.</done>
</task>

<task type="auto">
  <name>Task 2: Product detail enhancements and layout wiring</name>
  <files>apps/client/src/components/product/product-accordion.tsx, apps/client/src/components/product/stock-indicator.tsx, apps/client/src/components/product/delivery-estimate.tsx, apps/client/src/app/layout.tsx</files>
  <action>
**ProductAccordion (`product-accordion.tsx`):**
- Props: `description?: string`, `materials?: string`, `shippingInfo?: string`, `careInstructions?: string`.
- Uses AccordionItem from `@/components/ui/accordion` for each non-null section.
- Sections: "Description" (defaultOpen=true), "Materials & Composition", "Shipping & Returns", "Care Instructions".
- Content: text-sm text-foreground-muted leading-relaxed. Render HTML content safely using `dangerouslySetInnerHTML` for description (product descriptions may contain basic HTML) or just plain text.
- Container: border-t border-border mt-8. Each AccordionItem separated by border-b border-border.

**StockIndicator (`stock-indicator.tsx`):**
- Props: `quantity: number`, `lowStockThreshold?: number` (default 5).
- States:
  - In stock (quantity > lowStockThreshold): green dot + "In Stock" text
  - Low stock (0 < quantity <= lowStockThreshold): amber dot + "Only {quantity} left" text
  - Out of stock (quantity === 0): red dot + "Out of Stock" text
- Dot: w-2 h-2 rounded-full inline-block mr-2. Text: text-sm.
- Colors: bg-green-500, bg-amber-500, bg-red-500 for dots. Text inherits foreground color.

**DeliveryEstimate (`delivery-estimate.tsx`):**
- Props: `businessDays?: number` (default 3-5 range).
- Calculates estimated delivery date from today, skipping weekends.
- Display: flex items-center gap-2. Truck SVG icon (16px, text-foreground-muted) + text "Free delivery by {day, month date}" (e.g., "Free delivery by Thu, Mar 20").
- Text: text-sm text-foreground-muted.
- If businessDays is a range: show "Delivery in {min}-{max} business days".

**Layout wiring (`layout.tsx`):**
- Add BackToTop component before closing `</NuqsAdapter>` tag.
- Add CookieConsent component before closing `</NuqsAdapter>` tag.
- Add Toaster component from `@/components/ui/toast` before closing `</NuqsAdapter>` tag.
- Keep all existing components (SiteHeader, SiteFooter, ChatWidget, etc.).
- Ensure import order follows project conventions (external, workspace, internal).
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && test -f apps/client/src/components/product/product-accordion.tsx && test -f apps/client/src/components/product/stock-indicator.tsx && test -f apps/client/src/components/product/delivery-estimate.tsx && grep -q "BackToTop" apps/client/src/app/layout.tsx && grep -q "CookieConsent" apps/client/src/app/layout.tsx && grep -q "Toaster" apps/client/src/app/layout.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>ProductAccordion renders collapsible product detail sections. StockIndicator shows colored dot with stock text. DeliveryEstimate calculates and displays estimated delivery. Layout includes BackToTop, CookieConsent, and Toaster globally.</done>
</task>

</tasks>

<verification>
- BackToTop uses scroll listener with 500px threshold
- CookieConsent checks and sets localStorage for persistence
- ProductAccordion uses AccordionItem from ui/accordion
- StockIndicator renders 3 different states (in stock, low, out)
- DeliveryEstimate calculates future date skipping weekends
- Root layout includes BackToTop, CookieConsent, and Toaster components
</verification>

<success_criteria>
All shared utility components and product detail enhancements are built and integrated. The app has global BackToTop, CookieConsent, and Toaster. Product detail pages can use ProductAccordion, StockIndicator, and DeliveryEstimate for rich information display matching luxury e-commerce standards.
</success_criteria>

<output>
After completion, create `.planning/phases/23-e-commerce-ui-design-system-component-library/23-05-SUMMARY.md`
</output>
