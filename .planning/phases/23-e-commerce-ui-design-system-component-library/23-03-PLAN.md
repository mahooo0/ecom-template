---
phase: 23-e-commerce-ui-design-system-component-library
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - apps/client/src/components/sections/hero-full-bleed.tsx
  - apps/client/src/components/sections/hero-split.tsx
  - apps/client/src/components/sections/category-grid.tsx
  - apps/client/src/components/sections/category-card.tsx
  - apps/client/src/components/sections/product-carousel.tsx
  - apps/client/src/components/sections/section-heading.tsx
  - apps/client/src/components/sections/trust-bar.tsx
  - apps/client/src/components/sections/newsletter-signup.tsx
  - apps/client/src/app/page.tsx
autonomous: true
requirements:
  - DS-04

must_haves:
  truths:
    - "Homepage displays a full-bleed hero section with background image and text overlay"
    - "Category grid shows 2x2 or 3-col image cards with hover zoom effect"
    - "Product carousel scrolls horizontally with snap behavior"
    - "Trust bar shows icons for free shipping, easy returns, and secure checkout"
    - "Newsletter signup section has email input and submit button"
  artifacts:
    - path: "apps/client/src/components/sections/hero-full-bleed.tsx"
      provides: "100vh hero with background image, gradient overlay, and centered text"
      exports: ["HeroFullBleed"]
    - path: "apps/client/src/components/sections/product-carousel.tsx"
      provides: "Horizontal scroll carousel using Embla Carousel"
      exports: ["ProductCarousel"]
    - path: "apps/client/src/app/page.tsx"
      provides: "Assembled homepage with hero, categories, carousel, trust, newsletter"
  key_links:
    - from: "apps/client/src/components/sections/product-carousel.tsx"
      to: "embla-carousel-react"
      via: "useEmblaCarousel hook"
      pattern: "useEmblaCarousel"
    - from: "apps/client/src/app/page.tsx"
      to: "apps/client/src/components/sections/"
      via: "Component imports"
      pattern: "import.*sections/"
---

<objective>
Create landing page section components and assemble a polished homepage that showcases the luxury minimalist aesthetic.

Purpose: The homepage is the first impression. Currently it's a bare placeholder with a heading and link. This plan creates reusable section components and composes them into a compelling storefront homepage.
Output: 8 section components and a fully assembled homepage.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-e-commerce-ui-design-system-component-library/UI-RESEARCH.md
@apps/client/src/app/page.tsx
@apps/client/src/components/product/product-card.tsx

<interfaces>
From apps/client/src/lib/cn.ts (created in Plan 01):
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From apps/client/src/components/ui/button.tsx (created in Plan 01):
```typescript
export function Button({ variant, size, fullWidth, loading, className, children, ...props }: ButtonProps): JSX.Element;
```

From apps/client/src/components/ui/input.tsx (created in Plan 01):
```typescript
export function Input({ label, error, className, ...props }: InputProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing page section components</name>
  <files>apps/client/src/components/sections/hero-full-bleed.tsx, apps/client/src/components/sections/hero-split.tsx, apps/client/src/components/sections/category-grid.tsx, apps/client/src/components/sections/category-card.tsx, apps/client/src/components/sections/product-carousel.tsx, apps/client/src/components/sections/section-heading.tsx, apps/client/src/components/sections/trust-bar.tsx, apps/client/src/components/sections/newsletter-signup.tsx</files>
  <action>
Create 8 section components in `apps/client/src/components/sections/`:

**SectionHeading (`section-heading.tsx`):**
- Props: `title: string`, `subtitle?: string`, `viewAllHref?: string`, `className?: string`.
- Title: font-display text-2xl lg:text-3xl text-center. Subtitle: text-sm text-foreground-muted mt-2.
- "View All" link: text-xs uppercase tracking-widest, right-aligned, with arrow, hover underline.
- Flex row: title centered, "View All" absolute right on desktop.

**HeroFullBleed (`hero-full-bleed.tsx`):**
- Props: `imageSrc: string`, `title: string`, `subtitle?: string`, `ctaText?: string`, `ctaHref?: string`, `overlayOpacity?: number`.
- 100vh full-width section. Background: `next/image` with `fill` + `object-cover`, priority loading.
- Dark gradient overlay from bottom (linear-gradient, customizable opacity defaulting to 0.4).
- Content: centered vertically in bottom third, white text. Title: font-display text-4xl lg:text-6xl. Subtitle: text-sm lg:text-base mt-4 max-w-md.
- CTA: Button variant="outline" with white border/text (custom className override). No CTA if ctaText not provided.
- Use static placeholder image path for now: `/images/hero-placeholder.jpg`. Include a comment noting real images come from CMS/Cloudinary.

**HeroSplit (`hero-split.tsx`):**
- Props: `imageSrc: string`, `title: string`, `description: string`, `ctaText: string`, `ctaHref: string`, `imagePosition?: 'left' | 'right'`.
- 50/50 split on desktop (grid-cols-2), stacked on mobile (image on top).
- Image side: full-height `next/image` with aspect-[3/4] on mobile.
- Content side: vertically centered, px-12 lg:px-20. Title: font-display text-3xl lg:text-4xl. Description: text-foreground-muted mt-4. CTA: Button variant="solid" mt-8.

**CategoryCard (`category-card.tsx`):**
- Props: `title: string`, `imageSrc: string`, `href: string`.
- Image background (next/image fill, object-cover) + dark overlay + white text overlay at bottom-left.
- Hover: image scale(1.05) with overflow-hidden on container for crop effect. Transition 500ms ease-out-expo.
- No border-radius. Aspect ratio: aspect-[4/5].

**CategoryGrid (`category-grid.tsx`):**
- Props: `title?: string`, `categories: Array<{ title: string; imageSrc: string; href: string }>`.
- SectionHeading at top if title provided.
- Grid: grid-cols-2 lg:grid-cols-4, gap-1 (tight luxury gaps per research).
- Renders CategoryCard for each item.

**ProductCarousel (`product-carousel.tsx`):**
- "use client". Props: `title: string`, `viewAllHref?: string`, `products: Array<{ id: string; name: string; slug: string; price: number; images: string[]; brand?: { name: string } }>`.
- Uses Embla Carousel (useEmblaCarousel) with `{ align: 'start', containScroll: 'trimSnaps' }`.
- SectionHeading at top.
- Carousel: flex row, each slide is a product card (simplified inline version — not full ProductCard to avoid circular deps). Show image (aspect-[3/4]), brand name (small uppercase), product name, price.
- Navigation: left/right arrow buttons (absolute positioned, appear on hover). Use SVG chevron arrows.
- Responsive: show 2 slides on mobile, 3 on md, 4 on lg. Use `basis-1/2 md:basis-1/3 lg:basis-1/4` on slides.
- Dot indicators below: small circles, active = bg-foreground, inactive = bg-border.

**TrustBar (`trust-bar.tsx`):**
- Three items in a row (flex, centered, gap-8 lg:gap-16): "Free Shipping" (truck icon), "Easy Returns" (refresh icon), "Secure Checkout" (lock icon).
- Each: inline SVG icon (20px) + text (text-xs uppercase tracking-widest text-foreground-muted).
- Separator lines between items on desktop. Section padding: py-12 border-y border-border.

**NewsletterSignup (`newsletter-signup.tsx`):**
- "use client". Centered section, max-w-md mx-auto, py-20.
- Heading: font-display text-2xl "Join Our World". Subtitle: text-sm text-foreground-muted mt-2.
- Form: flex row — Input (email type, placeholder "Your email address") + Button ("Subscribe", variant="solid").
- On submit: prevent default, show success toast via useToast() hook, clear input. No actual API call — placeholder.
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && for f in hero-full-bleed hero-split category-grid category-card product-carousel section-heading trust-bar newsletter-signup; do test -f "apps/client/src/components/sections/$f.tsx" || echo "MISSING: $f"; done && echo "ALL SECTIONS PRESENT"</automated>
  </verify>
  <done>All 8 section components exist. HeroFullBleed is 100vh with gradient overlay. CategoryGrid uses tight 1px gaps. ProductCarousel uses Embla. TrustBar shows 3 trust icons. NewsletterSignup has email form.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble homepage with sections and mock data</name>
  <files>apps/client/src/app/page.tsx</files>
  <action>
Replace the current placeholder homepage with a fully composed landing page. The homepage is a Server Component.

Structure (top to bottom):
1. **HeroFullBleed** — imageSrc: `/images/hero.jpg` (placeholder path), title: "New Season", subtitle: "Discover the latest collection", ctaText: "Shop Now", ctaHref: "/products"
2. **CategoryGrid** — title: "Shop by Category", 4 categories with placeholder image paths:
   - { title: "Women", imageSrc: "/images/category-women.jpg", href: "/categories/women" }
   - { title: "Men", imageSrc: "/images/category-men.jpg", href: "/categories/men" }
   - { title: "Accessories", imageSrc: "/images/category-accessories.jpg", href: "/categories/accessories" }
   - { title: "New Arrivals", imageSrc: "/images/category-new.jpg", href: "/products?sort=newest" }
3. **ProductCarousel** — title: "New Arrivals", viewAllHref: "/products?sort=newest". Use hardcoded mock product data array (6-8 products with realistic names like "Wool Blend Coat", "Silk Camisole", prices in cents, placeholder image paths). Include a comment: `// TODO: Replace with server fetch from API`
4. **TrustBar**
5. **HeroSplit** — imageSrc: `/images/editorial.jpg`, title: "Crafted with Care", description: "Every piece in our collection is made with sustainable materials and ethical manufacturing.", ctaText: "Our Story", ctaHref: "/about", imagePosition: "right"
6. **ProductCarousel** — title: "Best Sellers", viewAllHref: "/products?sort=best-selling". Another mock products array.
7. **NewsletterSignup**

Add a comment at the top of the mock data: `// Mock data — replace with API calls when server endpoints are ready`

Create placeholder images directory: add a `.gitkeep` file to `apps/client/public/images/` so the directory exists. Real images will be added later. The components should handle missing images gracefully (show gray background when image fails to load).
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && grep -q "HeroFullBleed" apps/client/src/app/page.tsx && grep -q "CategoryGrid" apps/client/src/app/page.tsx && grep -q "ProductCarousel" apps/client/src/app/page.tsx && grep -q "TrustBar" apps/client/src/app/page.tsx && grep -q "NewsletterSignup" apps/client/src/app/page.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Homepage renders HeroFullBleed -> CategoryGrid -> ProductCarousel (New Arrivals) -> TrustBar -> HeroSplit -> ProductCarousel (Best Sellers) -> NewsletterSignup. All using mock data with placeholder image paths.</done>
</task>

</tasks>

<verification>
- All 8 section component files exist in apps/client/src/components/sections/
- Homepage imports and renders at least 5 different section types
- ProductCarousel uses Embla Carousel (useEmblaCarousel import present)
- HeroFullBleed has 100vh height and gradient overlay
- CategoryCard has hover zoom scale transform
- Mock data arrays present with realistic product names and cent prices
</verification>

<success_criteria>
The homepage is transformed from a bare placeholder into a polished luxury e-commerce landing page with hero, category grid, product carousels, trust bar, editorial split section, and newsletter signup. All section components are reusable with configurable props.
</success_criteria>

<output>
After completion, create `.planning/phases/23-e-commerce-ui-design-system-component-library/23-03-SUMMARY.md`
</output>
