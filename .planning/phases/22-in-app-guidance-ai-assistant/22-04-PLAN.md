---
phase: 22-in-app-guidance-ai-assistant
plan: 04
type: execute
wave: 3
depends_on: ["22-01", "22-02", "22-03"]
files_modified:
  - apps/client/src/app/layout.tsx
  - apps/admin/src/app/layout.tsx
autonomous: false
requirements:
  - GUIDE-01
  - GUIDE-03
  - GUIDE-05

must_haves:
  truths:
    - "ChatWidget is rendered on every client page via root layout"
    - "AdminChatWidget is rendered on every admin page via admin layout"
    - "Client and admin apps build successfully with all new components wired in"
  artifacts:
    - path: "apps/client/src/app/layout.tsx"
      provides: "Root layout with ChatWidget integrated"
    - path: "apps/admin/src/app/layout.tsx"
      provides: "Admin root layout with AdminChatWidget integrated"
  key_links:
    - from: "apps/client/src/app/layout.tsx"
      to: "apps/client/src/components/ai-assistant/ChatWidget.tsx"
      via: "import and render in body"
      pattern: "ChatWidget"
    - from: "apps/admin/src/app/layout.tsx"
      to: "apps/admin/src/components/ai-assistant/AdminChatWidget.tsx"
      via: "import and render in body"
      pattern: "AdminChatWidget"
---

<objective>
Wire all Phase 22 components into the application: ChatWidget into client layout, AdminChatWidget into admin layout, and verify builds pass.

Purpose: Make the AI assistants and guidance system available to all users across both apps.
Output: Fully integrated AI assistants visible on every page, verified with a build check.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-in-app-guidance-ai-assistant/22-RESEARCH.md
@.planning/phases/22-in-app-guidance-ai-assistant/22-01-SUMMARY.md
@.planning/phases/22-in-app-guidance-ai-assistant/22-02-SUMMARY.md
@.planning/phases/22-in-app-guidance-ai-assistant/22-03-SUMMARY.md

<interfaces>
<!-- Key interfaces from prior plans that this plan wires together -->

From apps/client/src/components/ai-assistant/ChatWidget.tsx:
```typescript
export function ChatWidget(): JSX.Element;
// Floating chat button + drawer, self-contained with useChat
```

From apps/admin/src/components/ai-assistant/AdminChatWidget.tsx:
```typescript
export function AdminChatWidget(): JSX.Element;
// Admin AI helper widget, self-contained with useChat
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ChatWidget into client layout and AdminChatWidget into admin layout</name>
  <files>apps/client/src/app/layout.tsx, apps/admin/src/app/layout.tsx</files>
  <action>
**Client layout** (`apps/client/src/app/layout.tsx`):
- Read existing layout first (it has ClerkProvider, NuqsAdapter, MegaMenu, SearchBar, etc.)
- Add import: `import { ChatWidget } from '@/components/ai-assistant/ChatWidget';`
- Add `<ChatWidget />` inside the body, AFTER `{children}` and BEFORE the closing NuqsAdapter/body tags. It renders as a fixed-position element so placement in DOM doesn't affect layout.
- Do NOT modify any existing code -- only add the import and the component.

**Admin layout** (`apps/admin/src/app/layout.tsx`):
- Read existing layout first (it has ClerkProvider, sidebar nav, etc.)
- Add import: `import { AdminChatWidget } from '@/components/ai-assistant/AdminChatWidget';`
- Add `<AdminChatWidget />` inside the body, AFTER the main content area. Place it as a sibling of the flex container (after the closing `</div>` of the flex wrapper), before closing body tag.
- Do NOT modify any existing code -- only add the import and the component.
  </action>
  <verify>
    <automated>grep -q "ChatWidget" apps/client/src/app/layout.tsx && grep -q "AdminChatWidget" apps/admin/src/app/layout.tsx</automated>
  </verify>
  <done>ChatWidget rendered in client layout, AdminChatWidget rendered in admin layout</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify AI assistants work end-to-end in both apps</name>
  <files>apps/client/src/app/layout.tsx, apps/admin/src/app/layout.tsx</files>
  <action>
Human verification of the complete Phase 22 integration across both apps.
  </action>
  <verify>
    <automated>grep -q "ChatWidget" apps/client/src/app/layout.tsx && grep -q "AdminChatWidget" apps/admin/src/app/layout.tsx</automated>
  </verify>
  <done>User has approved the visual and functional integration of both AI assistants</done>
  <what-built>AI shopping assistant (client) and AI admin helper (admin) integrated into both apps, plus guidance system (HelpTooltip and Driver.js tours)</what-built>
  <how-to-verify>
    1. Start client app: `cd apps/client && pnpm dev`
    2. Visit http://localhost:3000 -- verify a floating blue "AI" chat button appears bottom-right
    3. Click the button -- verify a chat drawer opens with "Shopping Assistant" header and welcome message
    4. Type "What is your return policy?" and send -- verify a streaming response appears (requires OPENAI_API_KEY in .env)
    5. Close and reopen the drawer -- verify conversation persists
    6. Start admin app: `cd apps/admin && pnpm dev`
    7. Visit the admin dashboard -- verify an indigo "Help" button appears bottom-right
    8. Click it -- verify admin assistant drawer opens with "Admin Assistant" header
    9. Type "How do I create a product?" and send -- verify admin-focused response
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Both apps render their respective AI chat widgets on every page
- Chat widgets are functional (open, send, receive streaming responses)
- No layout shifts or styling conflicts
- Build passes for both apps
</verification>

<success_criteria>
- Client app shows floating chat button on all pages
- Admin app shows help button on all pages
- Both chat interfaces send/receive messages via streaming
- No regressions to existing layout or functionality
</success_criteria>

<output>
After completion, create `.planning/phases/22-in-app-guidance-ai-assistant/22-04-SUMMARY.md`
</output>
