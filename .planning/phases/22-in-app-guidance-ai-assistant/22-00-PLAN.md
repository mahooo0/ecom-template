---
phase: 22-in-app-guidance-ai-assistant
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/src/__tests__/api/chat.test.ts
  - apps/client/src/__tests__/api/chat-rate-limit.test.ts
  - apps/client/src/__tests__/components/ChatWidget.test.tsx
  - apps/client/src/__tests__/components/HelpTooltip.test.tsx
  - apps/admin/src/__tests__/api/chat.test.ts
  - apps/admin/src/__tests__/components/AdminChatWidget.test.tsx
autonomous: true
requirements:
  - GUIDE-01
  - GUIDE-02
  - GUIDE-03
  - GUIDE-04
  - GUIDE-05
  - GUIDE-06

must_haves:
  truths:
    - "AI SDK mock is available in Vitest setup for all test files"
    - "Test stubs exist for every testable behavior in Phase 22"
    - "AI SDK and Driver.js packages are installed in client and admin apps"
  artifacts:
    - path: "apps/client/src/__tests__/api/chat.test.ts"
      provides: "Client AI chat route test stubs"
    - path: "apps/client/src/__tests__/api/chat-rate-limit.test.ts"
      provides: "Rate limiter unit test stubs"
    - path: "apps/client/src/__tests__/components/ChatWidget.test.tsx"
      provides: "Client ChatWidget test stubs"
    - path: "apps/client/src/__tests__/components/HelpTooltip.test.tsx"
      provides: "HelpTooltip test stubs"
    - path: "apps/admin/src/__tests__/api/chat.test.ts"
      provides: "Admin AI chat route test stubs"
    - path: "apps/admin/src/__tests__/components/AdminChatWidget.test.tsx"
      provides: "Admin ChatWidget test stubs"
  key_links: []
---

<objective>
Install AI SDK and Driver.js packages in client and admin apps, and create Wave 0 test infrastructure with stubs for all Phase 22 testable behaviors.

Purpose: Establish test foundation and dependencies before any implementation begins.
Output: Installed packages, mock setup for `ai` package, test stub files with `it.todo()` for all requirements.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-in-app-guidance-ai-assistant/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install AI SDK and Driver.js packages</name>
  <files>apps/client/package.json, apps/admin/package.json</files>
  <action>
Install packages in both apps:

```bash
# Client app: AI assistant + guidance
pnpm --filter client add ai @ai-sdk/react @ai-sdk/openai driver.js

# Admin app: AI assistant + guidance
pnpm --filter admin add ai @ai-sdk/react @ai-sdk/openai driver.js
```

Verify both package.json files now contain `ai`, `@ai-sdk/react`, `@ai-sdk/openai`, and `driver.js` in dependencies.
  </action>
  <verify>
    <automated>pnpm --filter client list ai @ai-sdk/react @ai-sdk/openai driver.js && pnpm --filter admin list ai @ai-sdk/react @ai-sdk/openai driver.js</automated>
  </verify>
  <done>All 4 packages listed in both apps/client/package.json and apps/admin/package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for all Phase 22 testable behaviors</name>
  <files>
    apps/client/src/__tests__/api/chat.test.ts,
    apps/client/src/__tests__/api/chat-rate-limit.test.ts,
    apps/client/src/__tests__/components/ChatWidget.test.tsx,
    apps/client/src/__tests__/components/HelpTooltip.test.tsx,
    apps/admin/src/__tests__/api/chat.test.ts,
    apps/admin/src/__tests__/components/AdminChatWidget.test.tsx
  </files>
  <action>
Create test stub files using the `it.todo()` pattern established in Phase 03-00. Each file uses `describe` blocks with `it.todo()` stubs describing expected behaviors. Use `vi.mock('ai', ...)` and `vi.mock('@ai-sdk/react', ...)` mocks at the top of relevant files.

**apps/client/src/__tests__/api/chat.test.ts:**
- `describe('POST /api/chat')`: it.todo stubs for:
  - returns streaming response for valid messages
  - uses gpt-4o-mini model
  - includes system prompt with store context
  - sets maxDuration to 30
  - returns 429 when rate limited

**apps/client/src/__tests__/api/chat-rate-limit.test.ts:**
- `describe('checkRateLimit')`: it.todo stubs for:
  - allows requests under limit
  - blocks requests over limit (10 per minute)
  - resets after window expires
  - tracks different IPs independently

**apps/client/src/__tests__/components/ChatWidget.test.tsx:**
- `describe('ChatWidget')`: it.todo stubs for:
  - renders floating chat button
  - opens chat drawer on button click
  - displays messages from useChat
  - shows streaming indicator during generation
  - sends message on form submit

**apps/client/src/__tests__/components/HelpTooltip.test.tsx:**
- `describe('HelpTooltip')`: it.todo stubs for:
  - renders ? icon button
  - shows content on hover
  - hides content on mouse leave
  - toggles on click

**apps/admin/src/__tests__/api/chat.test.ts:**
- `describe('POST /api/chat (admin)')`: it.todo stubs for:
  - returns streaming response for admin questions
  - uses admin-specific system prompt
  - sets maxDuration to 30

**apps/admin/src/__tests__/components/AdminChatWidget.test.tsx:**
- `describe('AdminChatWidget')`: it.todo stubs for:
  - renders help button
  - opens chat panel on click
  - displays admin-focused messages

Create `__tests__/api/` and `__tests__/components/` directories as needed in both apps.
  </action>
  <verify>
    <automated>find apps/client/src/__tests__ apps/admin/src/__tests__ -name "*.test.*" -newer apps/client/package.json | wc -l | xargs test 6 -eq</automated>
  </verify>
  <done>6 test stub files created with it.todo() stubs covering all Phase 22 testable behaviors; files pass vitest collection without errors</done>
</task>

</tasks>

<verification>
- All packages installed in both apps
- All test stub files exist and are parseable by Vitest
- `pnpm test` does not fail (todo tests are pending, not failing)
</verification>

<success_criteria>
- `ai`, `@ai-sdk/react`, `@ai-sdk/openai`, `driver.js` in both apps' dependencies
- 6 test files with it.todo() stubs exist
- No test failures introduced
</success_criteria>

<output>
After completion, create `.planning/phases/22-in-app-guidance-ai-assistant/22-00-SUMMARY.md`
</output>
