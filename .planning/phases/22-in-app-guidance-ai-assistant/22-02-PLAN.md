---
phase: 22-in-app-guidance-ai-assistant
plan: 02
type: execute
wave: 2
depends_on: ["22-00"]
files_modified:
  - apps/admin/src/app/api/chat/route.ts
  - apps/admin/src/components/ai-assistant/AdminChatWidget.tsx
  - apps/admin/src/components/ai-assistant/AdminChatMessages.tsx
  - apps/admin/src/components/ai-assistant/AdminChatInput.tsx
autonomous: true
requirements:
  - GUIDE-03
  - GUIDE-04

must_haves:
  truths:
    - "Admin app has a /api/chat POST endpoint that streams AI responses with admin-focused system prompt"
    - "Admin app has a help button that opens a chat panel"
    - "Admin user can ask questions about how to use the admin panel and get helpful answers"
  artifacts:
    - path: "apps/admin/src/app/api/chat/route.ts"
      provides: "Admin AI chat API route with admin-specific system prompt"
      exports: ["POST", "maxDuration"]
    - path: "apps/admin/src/components/ai-assistant/AdminChatWidget.tsx"
      provides: "Admin AI helper widget"
    - path: "apps/admin/src/components/ai-assistant/AdminChatMessages.tsx"
      provides: "Admin chat message list"
    - path: "apps/admin/src/components/ai-assistant/AdminChatInput.tsx"
      provides: "Admin chat input"
  key_links:
    - from: "apps/admin/src/components/ai-assistant/AdminChatWidget.tsx"
      to: "/api/chat"
      via: "useChat hook with api: '/api/chat'"
      pattern: "useChat.*api.*chat"
---

<objective>
Build the admin AI helper assistant: a Next.js API route with admin-focused system prompt and a chat widget for the admin panel.

Purpose: Admins get an AI helper that explains how to use admin features (product management, order handling, shipping setup, etc.).
Output: Working admin chat API route and admin chat widget.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-in-app-guidance-ai-assistant/22-RESEARCH.md
@.planning/phases/22-in-app-guidance-ai-assistant/22-00-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin AI chat API route</name>
  <files>apps/admin/src/app/api/chat/route.ts</files>
  <action>
Create `apps/admin/src/app/api/chat/route.ts` with the same structure as the client route but with an admin-specific system prompt and rate limiter.

1. **Rate limiter**: Same IP-based Map pattern as client (10 requests per 60s window).

2. **POST handler**:
   - Rate limit check, 429 on block
   - Parses `{ messages, context }` (context optional: `{ page?: string }`)
   - Admin-specific system prompt:
     ```
     You are an admin panel assistant for an e-commerce platform.
     Current admin page: {context.page ?? 'dashboard'}.
     Help administrators with:
     - How to create, edit, and manage products (simple, variable, weighted, digital, bundled types)
     - How to manage categories, brands, tags, and collections
     - How to set up shipping zones and methods
     - How to manage inventory across warehouses
     - How to configure search settings and synonyms
     - How to manage user accounts and roles
     - General admin panel navigation and features
     Keep answers concise and actionable. Reference specific admin panel sections when possible.
     If asked about technical issues or bugs, suggest checking the documentation or contacting the development team.
     ```
   - Calls `streamText` with `openai('gpt-4o-mini')`, system prompt, `convertToModelMessages(messages)`, `maxTokens: 500`
   - Returns `result.toUIMessageStreamResponse()`

3. **Export** `const maxDuration = 30;`

Same imports as client route: `streamText`, `UIMessage`, `convertToModelMessages` from 'ai'; `openai` from '@ai-sdk/openai'.
  </action>
  <verify>
    <automated>test -f apps/admin/src/app/api/chat/route.ts && grep -q "streamText" apps/admin/src/app/api/chat/route.ts && grep -q "admin" apps/admin/src/app/api/chat/route.ts</automated>
  </verify>
  <done>Admin API route exists with admin-focused system prompt, rate limiting, and streamText</done>
</task>

<task type="auto">
  <name>Task 2: Create admin chat widget components</name>
  <files>
    apps/admin/src/components/ai-assistant/AdminChatWidget.tsx,
    apps/admin/src/components/ai-assistant/AdminChatMessages.tsx,
    apps/admin/src/components/ai-assistant/AdminChatInput.tsx
  </files>
  <action>
Create three client components in `apps/admin/src/components/ai-assistant/`. These mirror the client app components but with admin-specific styling and messaging.

**AdminChatInput.tsx** (`'use client'`):
- Same interface as client ChatInput: `{ onSend: (text: string) => void, isLoading: boolean }`
- Form with input (placeholder: "Ask about admin features...") and send button
- Tailwind: border-t, p-3, flex gap-2

**AdminChatMessages.tsx** (`'use client'`):
- Same interface as client ChatMessages
- Welcome message: "Hi! I'm your admin assistant. Ask me about managing products, orders, shipping, or any admin feature."
- Auto-scroll to bottom on new messages
- User messages right-aligned, assistant messages left-aligned

**AdminChatWidget.tsx** (`'use client'`):
- Uses `useChat` from `@ai-sdk/react` with `api: '/api/chat'`
- State: `open` boolean
- Fixed-position button at bottom-4 right-4 (rounded-full, indigo bg to differentiate from client's blue) with "?" or "Help" text
- When open, renders panel (bottom-20 right-4, w-80 sm:w-96, h-[28rem], white bg, rounded-xl, shadow-2xl, z-50)
  - Header: "Admin Assistant" title + close button
  - AdminChatMessages (flex-1 overflow-y-auto)
  - AdminChatInput
- Derives `isLoading` from `status === 'submitted' || status === 'streaming'`
- Export as named export `AdminChatWidget`
  </action>
  <verify>
    <automated>test -f apps/admin/src/components/ai-assistant/AdminChatWidget.tsx && test -f apps/admin/src/components/ai-assistant/AdminChatMessages.tsx && test -f apps/admin/src/components/ai-assistant/AdminChatInput.tsx && grep -q "useChat" apps/admin/src/components/ai-assistant/AdminChatWidget.tsx</automated>
  </verify>
  <done>Three admin components exist with admin-specific styling, welcome message, and useChat wired to /api/chat</done>
</task>

</tasks>

<verification>
- Admin chat API route uses admin-specific system prompt
- AdminChatWidget uses useChat connected to /api/chat
- All components are client components
- Rate limiting active on admin route
</verification>

<success_criteria>
- POST /api/chat in admin app streams AI responses with admin context
- Admin chat widget opens, sends messages, shows streaming responses
- System prompt focuses on admin panel guidance
</success_criteria>

<output>
After completion, create `.planning/phases/22-in-app-guidance-ai-assistant/22-02-SUMMARY.md`
</output>
