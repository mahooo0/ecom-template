---
phase: 03-product-catalog
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/admin/package.json
  - apps/admin/src/app/products/page.tsx
  - apps/admin/src/components/product/products-table.tsx
  - apps/admin/src/components/product/columns.tsx
  - apps/admin/src/components/product/product-status-badge.tsx
  - apps/admin/src/lib/api.ts
autonomous: true
requirements: [PROD-07]

must_haves:
  truths:
    - "Admin can view a paginated table of all products with name, type, status, price, and created date columns"
    - "Admin can sort products by any column header click"
    - "Admin can filter products by status (DRAFT, ACTIVE, ARCHIVED) and product type"
    - "Admin can search products by name"
    - "Admin can change product status from the table row actions menu"
    - "Product status displays as colored badges (DRAFT=gray, ACTIVE=green, ARCHIVED=red)"
  artifacts:
    - path: "apps/admin/src/app/products/page.tsx"
      provides: "Server component that fetches products and renders table"
      exports: ["default"]
    - path: "apps/admin/src/components/product/products-table.tsx"
      provides: "Client component with TanStack Table for product listing"
      exports: ["ProductsTable"]
    - path: "apps/admin/src/components/product/columns.tsx"
      provides: "TanStack Table column definitions for products"
      exports: ["columns"]
    - path: "apps/admin/src/components/product/product-status-badge.tsx"
      provides: "Status badge component with color coding"
      exports: ["ProductStatusBadge"]
  key_links:
    - from: "apps/admin/src/app/products/page.tsx"
      to: "apps/admin/src/lib/api.ts"
      via: "api.products.getAll() fetch"
      pattern: "api\\.products\\.getAll"
    - from: "products-table.tsx"
      to: "columns.tsx"
      via: "useReactTable({ columns })"
      pattern: "columns"
    - from: "apps/admin/src/lib/api.ts"
      to: "apps/server/src/modules/product/product.routes.ts"
      via: "HTTP fetch to /api/products"
      pattern: "fetcher.*products"
---

<objective>
Build the admin product list page with a TanStack Table featuring sorting, filtering, search, pagination, status badges, and row actions (edit, delete, status change).

Purpose: Admins need to view and manage their product catalog. This creates the primary admin product management interface where they see all products and can perform quick actions like status changes and deletion.
Output: Admin /products page with fully featured data table, column definitions, status badge component, and expanded admin API client.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-product-catalog/03-RESEARCH.md
@.planning/phases/03-product-catalog/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 server API (expected endpoints) -->
GET /api/products?page=1&limit=20&status=ACTIVE&productType=SIMPLE&search=foo&sortBy=createdAt&sortOrder=desc
  -> { success, data: Product[], total, page, limit, totalPages }

PATCH /api/products/:id/status { status: "ACTIVE" }
  -> { success, data: Product }

DELETE /api/products/:id
  -> { success, message }

PATCH /api/products/bulk/status { ids: string[], status: "ARCHIVED" }
  -> { success, data: { count } }

POST /api/products/bulk/delete { ids: string[] }
  -> { success, data: { count } }

<!-- From existing admin API client -->
From apps/admin/src/lib/api.ts:
```typescript
const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:4000/api';
async function fetcher<T>(url: string, options?: RequestInit): Promise<T>
export const api = { products: { getAll, getById, create, update, delete } }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install TanStack Table and expand admin API client</name>
  <files>apps/admin/package.json, apps/admin/src/lib/api.ts, apps/admin/src/components/product/product-status-badge.tsx</files>
  <action>
1. Install TanStack Table in admin app:
   ```
   pnpm add --filter admin @tanstack/react-table
   ```

2. Expand `apps/admin/src/lib/api.ts` to add product management methods:
   - Update `products.getAll` to accept full query params: `(params?: { page?, limit?, status?, productType?, search?, sortBy?, sortOrder? })` -- build query string from params
   - Add `products.updateStatus(id: string, status: string)` -> PATCH /products/:id/status
   - Add `products.bulkUpdateStatus(ids: string[], status: string)` -> PATCH /products/bulk/status
   - Add `products.bulkDelete(ids: string[])` -> POST /products/bulk/delete
   - Keep existing create, update, delete methods
   - Add auth token forwarding: update the fetcher to accept and pass Authorization header. For admin app, use Clerk's `auth()` to get the token on the server side. For client-side fetches, the fetcher should accept an optional token parameter.

3. Create `apps/admin/src/components/product/product-status-badge.tsx`:
   - 'use client' (or server component -- no interactivity needed)
   - Props: `{ status: 'DRAFT' | 'ACTIVE' | 'ARCHIVED' }`
   - Render a styled span/badge:
     - DRAFT: gray background, gray text
     - ACTIVE: green background, green text
     - ARCHIVED: red/amber background, matching text
   - Use Tailwind classes for styling
   - Export named: `ProductStatusBadge`
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && pnpm list @tanstack/react-table --filter admin --depth 0 && test -f apps/admin/src/components/product/product-status-badge.tsx && echo "PASS"</automated>
  </verify>
  <done>TanStack Table installed, admin API client expanded with status management and bulk operation methods, ProductStatusBadge component created.</done>
</task>

<task type="auto">
  <name>Task 2: Create admin products page with data table</name>
  <files>apps/admin/src/components/product/columns.tsx, apps/admin/src/components/product/products-table.tsx, apps/admin/src/app/products/page.tsx</files>
  <action>
1. Create `apps/admin/src/components/product/columns.tsx`:
   - 'use client' directive (column definitions use client-side handlers)
   - Import ColumnDef from '@tanstack/react-table'
   - Import ProductStatusBadge
   - Define and export `columns: ColumnDef<Product>[]` with:
     - Checkbox column for row selection (using TanStack Table row selection API)
     - Name column: sortable, renders as link to `/products/${row.original.id}/edit`, show first image thumbnail if available (small 40x40px)
     - Product Type column: filterable, render as capitalized badge (Simple, Variable, etc.)
     - Status column: filterable, render as ProductStatusBadge
     - Price column: sortable, format as currency (divide by 100, add $ prefix, 2 decimal places)
     - Created At column: sortable, format as readable date
     - Actions column: dropdown menu with Edit (link), Change Status (submenu: DRAFT, ACTIVE, ARCHIVED), Delete (with confirmation)
   - Format price helper: `(cents: number) => '$' + (cents / 100).toFixed(2)`
   - Format date helper: `new Intl.DateTimeFormat('en-US', { dateStyle: 'medium' }).format(new Date(date))`

2. Create `apps/admin/src/components/product/products-table.tsx`:
   - 'use client' directive
   - Props: `{ data: Product[]; pageCount: number; pageIndex: number; pageSize: number; total: number }`
   - Import useReactTable, getCoreRowModel, getSortedRowModel, getFilteredRowModel, getPaginationRowModel from '@tanstack/react-table'
   - Import columns from './columns'
   - State: sorting, columnFilters, rowSelection
   - Configure table with manual pagination (server-side), client-side sorting, client-side filtering
   - Render:
     - Top bar: Search input (filters 'name' column), Status filter dropdown, Product Type filter dropdown, "Create Product" button (link to /products/new)
     - Table with headers (clickable for sort, with sort direction indicator), body rows, empty state
     - Bottom bar: Selected count, bulk actions dropdown (Change Status, Delete Selected), pagination controls (Previous, Page X of Y, Next), page size selector (10, 25, 50)
   - Bulk action handlers:
     - bulkUpdateStatus: call api.products.bulkUpdateStatus with selected row IDs
     - bulkDelete: call api.products.bulkDelete with selected row IDs, confirm first
   - Row action handlers:
     - changeStatus: call api.products.updateStatus, then router.refresh() to reload data
     - deleteProduct: confirm, call api.products.delete, then router.refresh()
   - Use `useRouter` from 'next/navigation' for refresh and navigation
   - Style with Tailwind: full-width table, hover rows, striped rows optional, responsive overflow-x-auto

3. Create `apps/admin/src/app/products/page.tsx`:
   - Server Component (no 'use client')
   - Parse searchParams for page (default 1), limit (default 25), status, productType, search, sortBy, sortOrder
   - Call api.products.getAll({ page, limit, status, productType, search, sortBy, sortOrder }) server-side
   - Render page title "Products" with count, ProductsTable with data and pagination props
   - Wrap in standard admin page layout (h1 heading, content area)
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && test -f apps/admin/src/app/products/page.tsx && test -f apps/admin/src/components/product/products-table.tsx && test -f apps/admin/src/components/product/columns.tsx && echo "PASS"</automated>
  </verify>
  <done>Admin /products page renders a data table with sortable columns, filterable by status and type, searchable by name, with row actions for edit/status/delete and bulk operations. Pagination controls navigate server-side paginated data.</done>
</task>

</tasks>

<verification>
- Admin /products page exists as server component
- ProductsTable renders with TanStack Table
- Columns include name, type, status (with badge), price (formatted), created date, and actions
- Row actions include edit link, status change, and delete
- Bulk operations (status change, delete) use bulk API endpoints
- Search input filters products by name
- Pagination controls present with page count
</verification>

<success_criteria>
- Navigating to /admin/products shows paginated product table
- Clicking column headers sorts the table
- Status filter narrows results to selected status
- Search box filters by product name
- Row action dropdown allows status change and deletion
- Bulk selection + action updates multiple products at once
</success_criteria>

<output>
After completion, create `.planning/phases/03-product-catalog/03-03-SUMMARY.md`
</output>
