---
phase: 03-product-catalog
plan: 05
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/client/src/app/products/page.tsx
  - apps/client/src/app/products/loading.tsx
  - apps/client/src/components/product/product-grid.tsx
  - apps/client/src/components/product/product-card.tsx
  - apps/client/src/components/product/sort-selector.tsx
  - apps/client/src/components/product/pagination.tsx
  - apps/client/src/components/ui/star-rating.tsx
  - apps/client/src/lib/api.ts
autonomous: true
requirements: [PROD-08, PROD-09]

must_haves:
  truths:
    - "Customer can see a grid of product cards on /products page"
    - "Product cards show image, name, price (formatted), average rating stars, and brand name"
    - "Customer can sort products by price, name, date, and popularity"
    - "Customer can navigate between pages of products"
    - "Only ACTIVE products with isActive=true are shown"
    - "Product card links to /products/[slug] detail page"
    - "Product card has a quick 'Add to Cart' button"
  artifacts:
    - path: "apps/client/src/app/products/page.tsx"
      provides: "Server component product listing page"
      exports: ["default"]
    - path: "apps/client/src/components/product/product-card.tsx"
      provides: "Product card with image, name, price, rating, add-to-cart"
      exports: ["ProductCard"]
    - path: "apps/client/src/components/product/product-grid.tsx"
      provides: "Responsive product grid layout"
      exports: ["ProductGrid"]
    - path: "apps/client/src/components/product/sort-selector.tsx"
      provides: "Sort dropdown for product listings"
      exports: ["SortSelector"]
    - path: "apps/client/src/components/product/pagination.tsx"
      provides: "Pagination controls with page numbers"
      exports: ["Pagination"]
    - path: "apps/client/src/components/ui/star-rating.tsx"
      provides: "Star rating display component"
      exports: ["StarRating"]
  key_links:
    - from: "apps/client/src/app/products/page.tsx"
      to: "apps/client/src/lib/api.ts"
      via: "api.products.getAll() server-side fetch"
      pattern: "api\\.products\\.getAll"
    - from: "product-card.tsx"
      to: "/products/[slug]"
      via: "Next.js Link component"
      pattern: "href.*products.*slug"
    - from: "apps/client/src/lib/api.ts"
      to: "apps/server/src/modules/product/product.routes.ts"
      via: "HTTP fetch to /api/products"
      pattern: "fetcher.*products"
---

<objective>
Build the client-facing product listing page with responsive product grid, product cards showing image/name/price/rating/add-to-cart, sorting controls, and pagination.

Purpose: Customers need to browse the product catalog. This creates the main product discovery interface with attractive product cards and intuitive navigation controls.
Output: Product listing page at /products, ProductCard component, ProductGrid layout, SortSelector, Pagination, StarRating components.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-product-catalog/03-RESEARCH.md
@.planning/phases/03-product-catalog/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: Server API for client-facing listing -->
GET /api/products?page=1&limit=20&sortBy=createdAt&sortOrder=desc&status=ACTIVE
  -> { success, data: Product[], total, page, limit, totalPages }

GET /api/products/slug/:slug
  -> { success, data: Product (with all relations, reviews) }

<!-- Product type from shared types -->
From packages/types/src/index.ts:
```typescript
export interface Product {
  id: string;
  name: string;
  slug: string;
  description: string;
  price: number; // cents
  compareAtPrice?: number;
  images: string[];
  sku: string;
  productType: string;
  status: string;
  isActive: boolean;
  categoryId: string;
  brandId?: string;
  createdAt: Date;
  updatedAt: Date;
}
```

<!-- Existing client API -->
From apps/client/src/lib/api.ts:
```typescript
export const api = {
  products: {
    getAll: (page, limit) => fetcher<PaginatedResponse<Product>>(...)
    getById: (id) => fetcher<ApiResponse<Product>>(...)
  }
}
```

<!-- Existing cart store -->
From apps/client/src/stores/cart-store.ts:
// Zustand cart store with addItem method (available for quick-add-to-cart)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create UI components (StarRating, ProductCard, SortSelector, Pagination)</name>
  <files>apps/client/src/components/ui/star-rating.tsx, apps/client/src/components/product/product-card.tsx, apps/client/src/components/product/sort-selector.tsx, apps/client/src/components/product/pagination.tsx, tests/components/product-card.test.tsx</files>
  <behavior>
    - Test: ProductCard renders product name and formatted price ($19.99 from 1999 cents)
    - Test: ProductCard renders product image or placeholder when no image
    - Test: ProductCard renders star rating with average rating value
    - Test: ProductCard renders sale price with strikethrough on compareAtPrice
    - Test: ProductCard links to /products/{slug}
    - Test: ProductCard renders add-to-cart button
  </behavior>
  <action>
1. Implement the placeholder tests in `tests/components/product-card.test.tsx` -- replace `.todo()` stubs with real component rendering tests. Use happy-dom environment (add `// @vitest-environment happy-dom` at top of file). Mock next/image and next/link. Write tests FIRST (RED), then implement components.

2. Create `apps/client/src/components/ui/star-rating.tsx`:
   - Server component (no 'use client' needed -- pure rendering)
   - Props: `{ rating: number; maxStars?: number; showValue?: boolean; size?: 'sm' | 'md' | 'lg' }`
   - Default maxStars=5, showValue=false, size='sm'
   - Render filled, half, and empty stars using Unicode characters or simple SVG paths:
     - Filled star: gold/yellow color
     - Half star: half gold, half gray (use clip-path or two layered spans)
     - Empty star: gray color
   - If showValue, display numeric rating (e.g., "4.5")
   - Size classes: sm=w-4 h-4, md=w-5 h-5, lg=w-6 h-6
   - Style with Tailwind, inline flex, gap-0.5

3. Create `apps/client/src/components/product/product-card.tsx`:
   - Server component for data rendering, with Client "island" for add-to-cart
   - Props: `{ product: Product & { averageRating?: number; reviewCount?: number; brand?: { name: string } } }`
   - Render:
     - Link wrapper to `/products/${product.slug}` covering image and text
     - Product image: first image from product.images array, use `next/image` with Cloudinary URL, aspect-ratio square, object-cover, rounded-lg top corners
     - If no image, show placeholder with gray background and camera icon
     - If product.compareAtPrice > product.price: show compareAtPrice with line-through styling and product.price as sale price in red
     - Product name: truncate to 2 lines (line-clamp-2), font-medium
     - Brand name (if available): text-sm text-gray-500
     - StarRating component with averageRating and reviewCount in parentheses
     - Price: format as `$XX.XX` (divide cents by 100), font-semibold
     - Add to Cart button: styled button at bottom, uses client component AddToCartButton (or simple link for now -- actual cart integration is Phase 9)
   - For now, the "Add to Cart" button can be a placeholder that shows the button but links to the product page. Full cart integration comes in Phase 9.
   - Style with Tailwind: card with border, rounded-lg, hover:shadow-lg transition, overflow-hidden, bg-white

4. Create `apps/client/src/components/product/sort-selector.tsx`:
   - 'use client' (needs interactivity for onChange)
   - Props: `{ currentSort: string; currentOrder: string }`
   - Sort options:
     - "Newest" -> sortBy=createdAt, sortOrder=desc
     - "Price: Low to High" -> sortBy=price, sortOrder=asc
     - "Price: High to Low" -> sortBy=price, sortOrder=desc
     - "Name: A-Z" -> sortBy=name, sortOrder=asc
     - "Name: Z-A" -> sortBy=name, sortOrder=desc
   - On change, use `useRouter` from 'next/navigation' to push new URL with updated searchParams
   - Preserve existing params (page resets to 1 on sort change)
   - Style: select dropdown with Tailwind, or custom dropdown with options list

5. Create `apps/client/src/components/product/pagination.tsx`:
   - 'use client' (needs interactivity for navigation)
   - Props: `{ currentPage: number; totalPages: number; basePath?: string }`
   - Default basePath to '/products'
   - Render:
     - Previous button (disabled on page 1)
     - Page number buttons: show first, last, and 2 pages around current (with ellipsis for gaps)
     - Next button (disabled on last page)
   - On click, use `useRouter` to navigate with updated page param, preserving other searchParams
   - Style with Tailwind: flex row, gap-2, rounded buttons, active page highlighted with primary color
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && pnpm vitest run tests/components/product-card.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>StarRating renders 5-star display with half-star support. ProductCard shows image, name, brand, rating, price with sale formatting, and add-to-cart placeholder. SortSelector provides dropdown with 5 sort options. Pagination renders page navigation. All product card tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create product listing page with grid layout</name>
  <files>apps/client/src/components/product/product-grid.tsx, apps/client/src/app/products/page.tsx, apps/client/src/app/products/loading.tsx, apps/client/src/lib/api.ts, tests/products/listing.test.ts</files>
  <behavior>
    - Test: getAll returns paginated results with correct page/totalPages
    - Test: getAll respects sortBy and sortOrder params
    - Test: only ACTIVE products are returned for client listing
  </behavior>
  <action>
1. Update listing tests in `tests/products/listing.test.ts` -- add any remaining test stubs that were not implemented in Plan 01. If Plan 01 already implemented all listing tests, verify they still pass with the new client API changes.

2. Update `apps/client/src/lib/api.ts`:
   - Expand `products.getAll` to accept full query params: `(params?: { page?, limit?, sortBy?, sortOrder? })` and build query string
   - Add `products.getBySlug(slug: string)` -> GET /products/slug/:slug
   - Keep existing methods

3. Create `apps/client/src/components/product/product-grid.tsx`:
   - Server component (no 'use client')
   - Props: `{ products: ProductWithRating[] }` where ProductWithRating extends Product with averageRating, reviewCount, brand fields
   - Render responsive grid:
     - 1 column on mobile (< sm)
     - 2 columns on sm
     - 3 columns on md
     - 4 columns on lg
   - Gap-6 between cards
   - Map products to ProductCard components
   - Empty state: if products.length === 0, show friendly "No products found" message with illustration or icon
   - Style with Tailwind grid classes

4. Create `apps/client/src/app/products/page.tsx`:
   - Server Component
   - Parse searchParams: page (default 1), limit (default 20), sortBy (default 'createdAt'), sortOrder (default 'desc')
   - Fetch products from server API using api.products.getAll with params
   - The API should only return ACTIVE products (server handles this filter)
   - Calculate average rating per product from reviews data:
     - If the API response includes review data (rating), compute averageRating and reviewCount
     - If not, the server API should include this (Plan 01 service already computes it)
   - Render page structure:
     - Page heading: "All Products" with total count from pagination
     - Controls bar: SortSelector on the right
     - ProductGrid with products data
     - Pagination below grid with page count
   - Use Suspense if needed for streaming
   - Metadata: export `metadata = { title: 'Products | Store' }`

5. Create `apps/client/src/app/products/loading.tsx`:
   - Loading skeleton for the products page
   - Render a grid of placeholder cards (8 items):
     - Gray animated pulse rectangles for image (aspect-square)
     - Gray pulse bars for name, brand, rating, price
   - Same grid layout as product-grid (1/2/3/4 columns)
   - Style with Tailwind animate-pulse
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && pnpm vitest run tests/products/listing.test.ts tests/components/product-card.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Product listing page at /products fetches active products from API, displays in responsive grid with product cards showing image/name/price/rating. Sort controls update URL params and re-fetch. Pagination navigates between pages. Loading skeleton shows while data fetches. All listing and component tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/products/listing.test.ts tests/components/product-card.test.tsx` passes
- /products page renders a grid of product cards
- Product cards show image, name, brand name, star rating, and formatted price
- Sort selector changes URL params and re-renders with sorted data
- Pagination controls appear and navigate between pages
- Loading skeleton appears during page transitions
- Only ACTIVE products are displayed
- Empty state shown when no products match
</verification>

<success_criteria>
- Customer visiting /products sees a responsive grid of product cards
- Products display image, name, price (formatted from cents), average rating stars, and brand
- Sorting by price (low/high), name (A-Z/Z-A), and newest works
- Pagination shows correct page count and navigates between pages
- Products link to /products/[slug] detail page
- Sale prices display with strikethrough on original price
- Loading skeleton shows during page transitions
- All listing and product card tests pass via `pnpm vitest run`
</success_criteria>

<output>
After completion, create `.planning/phases/03-product-catalog/03-05-SUMMARY.md`
</output>
