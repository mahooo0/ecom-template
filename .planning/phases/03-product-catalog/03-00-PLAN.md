---
phase: 03-product-catalog
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - tests/setup.ts
  - tests/products/simple.test.ts
  - tests/products/variable.test.ts
  - tests/products/weighted.test.ts
  - tests/products/digital.test.ts
  - tests/products/bundle.test.ts
  - tests/products/status.test.ts
  - tests/products/listing.test.ts
  - tests/products/csv-import.test.ts
  - tests/components/product-card.test.tsx
  - tests/e2e/image-upload.test.ts
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03, PROD-04, PROD-05, PROD-06, PROD-07, PROD-08, PROD-09, PROD-10]

must_haves:
  truths:
    - "Running `pnpm vitest run` completes without infrastructure errors"
    - "All 10 test files exist with placeholder tests that fail (RED state)"
    - "Vitest config resolves workspace aliases (@repo/db, @repo/types)"
    - "Test setup file configures test database and mocks"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest framework configuration with alias resolution and coverage settings"
      exports: ["default"]
    - path: "tests/setup.ts"
      provides: "Test setup with database mocks and shared fixtures"
    - path: "tests/products/simple.test.ts"
      provides: "Placeholder test for PROD-01 simple product CRUD"
    - path: "tests/products/variable.test.ts"
      provides: "Placeholder test for PROD-02 variable products with variants"
    - path: "tests/products/weighted.test.ts"
      provides: "Placeholder test for PROD-03 weighted products"
    - path: "tests/products/digital.test.ts"
      provides: "Placeholder test for PROD-04 digital products"
    - path: "tests/products/bundle.test.ts"
      provides: "Placeholder test for PROD-05 bundle products"
    - path: "tests/products/status.test.ts"
      provides: "Placeholder test for PROD-07 status changes"
    - path: "tests/products/listing.test.ts"
      provides: "Placeholder test for PROD-08 pagination/sorting"
    - path: "tests/products/csv-import.test.ts"
      provides: "Placeholder test for PROD-10 CSV import"
    - path: "tests/components/product-card.test.tsx"
      provides: "Placeholder test for PROD-09 product card rendering"
    - path: "tests/e2e/image-upload.test.ts"
      provides: "Placeholder test for PROD-06 image upload workflow"
  key_links:
    - from: "vitest.config.ts"
      to: "tests/setup.ts"
      via: "setupFiles configuration"
      pattern: "setupFiles.*setup"
    - from: "tests/**/*.test.ts"
      to: "vitest.config.ts"
      via: "Vitest test runner"
      pattern: "vitest"
---

<objective>
Install Vitest test infrastructure and create placeholder test files for all 10 phase requirements so that every subsequent plan can run automated verification via `pnpm vitest run`.

Purpose: The Nyquist rule requires every task verify command to include an automated test execution. Without test infrastructure, no plan can satisfy this. Wave 0 creates the foundation so that Wave 1+ plans can write real test implementations alongside their production code.
Output: Vitest configured, 10 placeholder test files with failing tests (RED state), shared test setup with mocks.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-product-catalog/03-RESEARCH.md
@.planning/phases/03-product-catalog/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and create configuration with test setup</name>
  <files>vitest.config.ts, tests/setup.ts</files>
  <action>
1. Install Vitest and testing dependencies at workspace root:
   ```
   pnpm add -D -w vitest @vitest/ui @testing-library/react @testing-library/jest-dom happy-dom
   ```

2. Create `vitest.config.ts` at project root:
   - Import `defineConfig` from 'vitest/config'
   - Configure:
     - `test.globals`: true (enables global describe/it/expect)
     - `test.environment`: 'node' (default for server tests; component tests override per-file)
     - `test.setupFiles`: ['./tests/setup.ts']
     - `test.include`: ['tests/**/*.test.{ts,tsx}']
     - `test.coverage.provider`: 'v8'
     - `test.coverage.thresholds.global.lines`: 80
     - `test.coverage.include`: ['apps/server/src/**', 'packages/types/src/**']
     - `resolve.alias`: Map workspace aliases:
       - '@repo/db' -> './packages/db/src'
       - '@repo/types' -> './packages/types/src'
   - Do NOT include watch mode flags

3. Create `tests/setup.ts`:
   - Import '@testing-library/jest-dom' for DOM matchers (used by component tests)
   - Create mock for '@repo/db' prisma client:
     ```typescript
     vi.mock('@repo/db', () => ({
       prisma: {
         product: {
           findMany: vi.fn(),
           findUnique: vi.fn(),
           findFirst: vi.fn(),
           create: vi.fn(),
           update: vi.fn(),
           updateMany: vi.fn(),
           delete: vi.fn(),
           deleteMany: vi.fn(),
           count: vi.fn(),
         },
         productVariant: { createMany: vi.fn(), deleteMany: vi.fn() },
         digitalMeta: { create: vi.fn(), upsert: vi.fn() },
         weightedMeta: { create: vi.fn(), upsert: vi.fn() },
         bundleItem: { createMany: vi.fn(), deleteMany: vi.fn() },
         $transaction: vi.fn((fns) => Promise.all(fns.map((fn: Function) => fn()))),
       },
     }));
     ```
   - Export shared mock product fixtures:
     ```typescript
     export const mockSimpleProduct = {
       id: 'prod-1', name: 'Test Product', slug: 'test-product',
       description: 'A test product description here', price: 1999,
       sku: 'TEST-001', productType: 'SIMPLE', status: 'DRAFT',
       images: [], isActive: true, categoryId: 'cat-1',
       createdAt: new Date(), updatedAt: new Date(),
     };
     ```
   - Export mock fixtures for each product type (variable with variants, weighted with meta, digital with meta, bundled with items)

4. Add scripts to root package.json (if not already present):
   - `"test": "vitest run"`
   - `"test:watch": "vitest"`
   - `"test:coverage": "vitest run --coverage"`
   - `"test:ui": "vitest --ui"`
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && pnpm vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Vitest installed and configured with workspace alias resolution, coverage thresholds, and test setup file with Prisma mocks and product fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder test files for all 10 requirements</name>
  <files>tests/products/simple.test.ts, tests/products/variable.test.ts, tests/products/weighted.test.ts, tests/products/digital.test.ts, tests/products/bundle.test.ts, tests/products/status.test.ts, tests/products/listing.test.ts, tests/products/csv-import.test.ts, tests/components/product-card.test.tsx, tests/e2e/image-upload.test.ts</files>
  <action>
Create 10 placeholder test files. Each file has `describe` blocks with `it.todo()` stubs that document the expected behavior. These are intentionally incomplete (RED state) -- subsequent plans will fill in the implementations.

1. `tests/products/simple.test.ts` (PROD-01):
   ```typescript
   import { describe, it, expect } from 'vitest';

   describe('Simple Product CRUD', () => {
     it.todo('creates a simple product with name, description, price, SKU');
     it.todo('validates required fields and rejects incomplete data');
     it.todo('generates a unique slug from product name');
     it.todo('returns created product with all fields populated');
   });
   ```

2. `tests/products/variable.test.ts` (PROD-02):
   ```typescript
   describe('Variable Product CRUD', () => {
     it.todo('creates a variable product with at least one variant');
     it.todo('rejects variable product with zero variants');
     it.todo('each variant has SKU, price, stock, and option selections');
     it.todo('validates variant option references');
   });
   ```

3. `tests/products/weighted.test.ts` (PROD-03):
   ```typescript
   describe('Weighted Product CRUD', () => {
     it.todo('creates a weighted product with unit pricing metadata');
     it.todo('validates weight unit enum (KG, LB, OZ, G)');
     it.todo('requires pricePerUnit as positive integer');
     it.todo('optional min/max/step weight fields');
   });
   ```

4. `tests/products/digital.test.ts` (PROD-04):
   ```typescript
   describe('Digital Product CRUD', () => {
     it.todo('creates a digital product with file metadata');
     it.todo('requires fileUrl and fileName');
     it.todo('optional maxDownloads and accessDuration');
     it.todo('validates fileUrl is a valid URL');
   });
   ```

5. `tests/products/bundle.test.ts` (PROD-05):
   ```typescript
   describe('Bundle Product CRUD', () => {
     it.todo('creates a bundle product with at least 2 items');
     it.todo('rejects bundle with fewer than 2 items');
     it.todo('each bundle item has productId, quantity, and discount');
     it.todo('validates referenced product IDs exist');
   });
   ```

6. `tests/products/status.test.ts` (PROD-07):
   ```typescript
   describe('Product Status Management', () => {
     it.todo('updates product status to ACTIVE');
     it.todo('updates product status to ARCHIVED and sets isActive to false');
     it.todo('updates product status to DRAFT');
     it.todo('rejects invalid status value');
     it.todo('bulk status change updates multiple products');
   });
   ```

7. `tests/products/listing.test.ts` (PROD-08):
   ```typescript
   describe('Product Listing with Pagination and Sorting', () => {
     it.todo('returns paginated products with page and totalPages');
     it.todo('sorts products by price ascending');
     it.todo('sorts products by name');
     it.todo('sorts products by createdAt descending (default)');
     it.todo('filters by product status');
     it.todo('filters by product type');
     it.todo('searches by product name');
     it.todo('only returns ACTIVE products for client-facing endpoint');
   });
   ```

8. `tests/products/csv-import.test.ts` (PROD-10):
   ```typescript
   describe('CSV Bulk Import', () => {
     it.todo('imports valid CSV rows as products');
     it.todo('returns error details for invalid rows');
     it.todo('handles mixed valid and invalid rows');
     it.todo('rejects non-CSV file upload');
     it.todo('parses price strings to integer cents');
     it.todo('handles all product types except VARIABLE');
   });
   ```

9. `tests/components/product-card.test.tsx` (PROD-09):
   ```typescript
   describe('ProductCard Component', () => {
     it.todo('renders product name and formatted price');
     it.todo('renders product image or placeholder');
     it.todo('renders star rating with average');
     it.todo('renders sale price with strikethrough on original');
     it.todo('links to product detail page by slug');
     it.todo('renders add-to-cart button');
   });
   ```

10. `tests/e2e/image-upload.test.ts` (PROD-06):
    ```typescript
    describe('Image Upload and Management', () => {
      it.todo('Cloudinary signing endpoint returns valid signature');
      it.todo('ImageManager component renders upload widget');
      it.todo('uploaded image URL is added to images array');
      it.todo('images can be reordered via drag-and-drop');
      it.todo('individual images can be removed');
    });
    ```

Ensure all test files use consistent imports from 'vitest' and reference the setup fixtures where appropriate via comments indicating which mock fixtures to use when implementing.
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && pnpm vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All 10 test files exist with .todo() stubs documenting expected behaviors. Running `pnpm vitest run` completes without errors, showing all tests as skipped/todo. Subsequent plans will implement these tests alongside production code.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run` executes without infrastructure errors
- All 10 test files are discovered by Vitest
- Todo tests are reported as skipped (not failures)
- vitest.config.ts resolves workspace aliases
- tests/setup.ts provides Prisma mocks and product fixtures
</verification>

<success_criteria>
- Vitest framework installed and configured at workspace root
- `pnpm vitest run` completes in under 5 seconds with all tests reported
- 10 test files cover all 10 PROD-* requirements
- Test setup provides reusable Prisma mocks so subsequent plans can write tests without infrastructure work
- Coverage configuration ready for phase gate verification
</success_criteria>

<output>
After completion, create `.planning/phases/03-product-catalog/03-00-SUMMARY.md`
</output>
