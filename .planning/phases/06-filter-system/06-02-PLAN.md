---
phase: 06-filter-system
plan: 02
type: execute
wave: 2
depends_on:
  - 06-00
files_modified:
  - apps/client/package.json
  - apps/client/src/hooks/use-filters.ts
  - apps/client/src/components/filters/price-filter.tsx
  - apps/client/src/components/filters/attribute-filter.tsx
  - apps/client/src/components/filters/availability-filter.tsx
  - apps/client/src/components/filters/active-filters.tsx
autonomous: true
requirements:
  - FILT-02
  - FILT-04

must_haves:
  truths:
    - "useFilters hook manages filter state in URL via nuqs with type-safe parsers"
    - "Price filter has dual-handle slider and min/max inputs synced together with debounce"
    - "Active filters display as removable chips with clear all button"
  artifacts:
    - path: "apps/client/src/hooks/use-filters.ts"
      provides: "nuqs-based filter state hook"
      exports: ["useFilters"]
    - path: "apps/client/src/components/filters/price-filter.tsx"
      provides: "Price range slider + inputs"
      contains: "PriceFilter"
    - path: "apps/client/src/components/filters/attribute-filter.tsx"
      provides: "Dynamic attribute filter by type"
      contains: "AttributeFilter"
    - path: "apps/client/src/components/filters/availability-filter.tsx"
      provides: "In stock / out of stock checkboxes"
      contains: "AvailabilityFilter"
    - path: "apps/client/src/components/filters/active-filters.tsx"
      provides: "Active filter chips with clear buttons"
      contains: "ActiveFilters"
  key_links:
    - from: "apps/client/src/components/filters/price-filter.tsx"
      to: "apps/client/src/hooks/use-filters.ts"
      via: "useFilters hook for URL state"
      pattern: "useFilters"
    - from: "apps/client/src/components/filters/attribute-filter.tsx"
      to: "apps/client/src/hooks/use-filters.ts"
      via: "useFilters hook for attribute toggling"
      pattern: "useFilters"
---

<objective>
Install nuqs and use-debounce, create the useFilters hook for URL state management, and build all individual filter components (price, attribute, availability, active filters).

Purpose: Provide reusable filter UI components that manage state via URL parameters for shareable/bookmarkable filtered views.
Output: useFilters hook, PriceFilter, AttributeFilter, AvailabilityFilter, ActiveFilters components.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-filter-system/06-RESEARCH.md

@apps/client/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create useFilters hook with nuqs</name>
  <files>apps/client/package.json, apps/client/src/hooks/use-filters.ts, apps/client/src/hooks/use-filters.test.tsx</files>
  <action>
1. Install dependencies in client app:
   ```
   pnpm add --filter client nuqs use-debounce
   ```

2. Also install shadcn/ui components needed (from apps/client directory):
   ```
   cd apps/client && pnpm dlx shadcn@latest add slider checkbox sheet separator badge --yes
   ```

3. Create apps/client/src/hooks/use-filters.ts:
   - 'use client' directive
   - Import parseAsArrayOf, parseAsInteger, parseAsString, useQueryStates from 'nuqs'
   - Export useFilters() that returns useQueryStates with:
     - minPrice: parseAsInteger.withDefault(0)
     - maxPrice: parseAsInteger.withDefault(999999) (cents)
     - brands: parseAsArrayOf(parseAsString).withDefault([])
     - attributes: parseAsArrayOf(parseAsString).withDefault([]) (format: "key:value")
     - availability: parseAsArrayOf(parseAsString).withDefault([])
     - page: parseAsInteger.withDefault(1)
     - sortBy: parseAsString.withDefault('createdAt')
     - sortOrder: parseAsString.withDefault('desc')
   - Options: history: 'push', shallow: true, clearOnDefault: true

4. Convert FILT-04 it.todo() stubs in use-filters.test.tsx to real tests. Mock nuqs useQueryStates to verify it's called with correct parser config. Test that the hook returns expected default values.
  </action>
  <verify>
    <automated>pnpm test apps/client/src/hooks/use-filters.test.tsx --run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>nuqs and use-debounce installed, useFilters hook created with type-safe URL state, FILT-04 tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create filter components (price, attribute, availability, active filters)</name>
  <files>
    apps/client/src/components/filters/price-filter.tsx
    apps/client/src/components/filters/attribute-filter.tsx
    apps/client/src/components/filters/availability-filter.tsx
    apps/client/src/components/filters/active-filters.tsx
    apps/client/src/components/filters/price-filter.test.tsx
    apps/client/src/components/filters/attribute-filter.test.tsx
    apps/client/src/components/filters/availability-filter.test.tsx
  </files>
  <action>
1. Create price-filter.tsx (FILT-02):
   - 'use client' directive
   - Props: minPrice (catalog min), maxPrice (catalog max)
   - Use useFilters() for URL state
   - Local state for slider position (avoid lag during drag)
   - shadcn/ui Slider with dual handles (min/max), step=100 (cents = $1 increments)
   - Two Input fields for manual min/max entry (display as dollars, store as cents)
   - useDebouncedCallback from 'use-debounce' at 300ms to update URL
   - Sync inputs with slider and vice versa
   - Reset button to clear price filter
   - Always reset page to 1 on price change

2. Create attribute-filter.tsx (FILT-01, FILT-03):
   - 'use client' directive
   - Props: attribute (CategoryAttribute with name, key, type, values, unit), facetCounts array
   - Switch on attribute.type:
     - SELECT: Render shadcn/ui Checkbox group with facet counts per value
     - RANGE: Render shadcn/ui Slider with attribute.values as discrete points
     - BOOLEAN: Render single Checkbox
   - Toggle updates attributes array in useFilters ("key:value" format)
   - Reset page to 1 on attribute change

3. Create availability-filter.tsx (FILT-07):
   - 'use client' directive
   - Props: facetCounts for in_stock and out_of_stock
   - Render two shadcn/ui Checkboxes: "In Stock" and "Out of Stock"
   - Toggle updates availability array in useFilters
   - Show product count next to each option

4. Create active-filters.tsx:
   - 'use client' directive
   - Read all active filters from useFilters
   - Render Badge components for each active filter (brands, attributes, availability, price range)
   - Each badge has X button to remove that specific filter
   - "Clear all" link resets all filters to defaults

5. Convert FILT-01, FILT-02, FILT-03, FILT-07 client it.todo() stubs into real tests for attribute-filter, price-filter, and availability-filter. Mock useFilters hook, verify components render correct UI elements and call setFilters on interaction.
  </action>
  <verify>
    <automated>pnpm test apps/client/src/components/filters/ --run --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <done>All 4 filter components created. Price filter has slider + inputs with debounce. Attribute filter renders by type. Availability filter shows stock options. Active filters shows removable chips. Client filter tests pass.</done>
</task>

</tasks>

<verification>
pnpm test apps/client/src/components/filters/ apps/client/src/hooks/ --run shows all filter component and hook tests green
</verification>

<success_criteria>
- nuqs and use-debounce installed in client app
- useFilters hook manages 8 filter params via URL with type-safe parsers
- PriceFilter has dual slider + inputs with 300ms debounce
- AttributeFilter renders SELECT/RANGE/BOOLEAN types with facet counts
- AvailabilityFilter shows in-stock/out-of-stock checkboxes
- ActiveFilters shows removable chips with clear all
- All client filter tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-filter-system/06-02-SUMMARY.md`
</output>
