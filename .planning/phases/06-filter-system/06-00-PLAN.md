---
phase: 06-filter-system
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/src/modules/product/product.service.test.ts
  - apps/client/src/components/filters/attribute-filter.test.tsx
  - apps/client/src/components/filters/price-filter.test.tsx
  - apps/client/src/components/filters/filter-drawer.test.tsx
  - apps/client/src/components/filters/availability-filter.test.tsx
  - apps/client/src/hooks/use-filters.test.tsx
autonomous: true
requirements:
  - FILT-01
  - FILT-02
  - FILT-03
  - FILT-04
  - FILT-05
  - FILT-06
  - FILT-07

must_haves:
  truths:
    - "Test stubs exist for all 7 FILT requirements"
    - "pnpm test runs without errors (all stubs use it.todo)"
  artifacts:
    - path: "apps/server/src/modules/product/product.service.test.ts"
      provides: "Server-side filter and facet count test stubs"
      contains: "it.todo"
    - path: "apps/client/src/components/filters/attribute-filter.test.tsx"
      provides: "Dynamic attribute filter rendering test stubs"
      contains: "it.todo"
    - path: "apps/client/src/components/filters/price-filter.test.tsx"
      provides: "Price range filter test stubs"
      contains: "it.todo"
    - path: "apps/client/src/components/filters/filter-drawer.test.tsx"
      provides: "Mobile filter drawer test stubs"
      contains: "it.todo"
    - path: "apps/client/src/components/filters/availability-filter.test.tsx"
      provides: "Availability filter test stubs"
      contains: "it.todo"
    - path: "apps/client/src/hooks/use-filters.test.tsx"
      provides: "URL state persistence test stubs"
      contains: "it.todo"
  key_links: []
---

<objective>
Wave 0: Create test stub files for all FILT-01 through FILT-07 requirements covering server-side filtering logic and client filter components.

Purpose: Establish test scaffolding so subsequent plans can implement against failing tests per Nyquist validation.
Output: 7 test files with it.todo() stubs, all passing pnpm test (todos are pending, not failing).
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-filter-system/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create server-side filter and facet count test stubs</name>
  <files>apps/server/src/modules/product/product.service.test.ts</files>
  <action>
Create (or extend if exists) product.service.test.ts with a describe('ProductService - Filtering') block containing it.todo() stubs for:

1. FILT-01: "returns products filtered by category attribute (JSONB path query)"
2. FILT-03: "calculates facet counts for brand filter values"
3. FILT-03: "calculates facet counts for dynamic attribute values"
4. FILT-05: "applies OR logic within same attribute group (red OR blue)"
5. FILT-05: "applies AND logic across different attribute groups (color AND size)"
6. FILT-07: "filters in-stock products by variant stock > 0"
7. FILT-07: "filters out-of-stock products where all variants stock = 0"
8. FILT-07: "handles variable products with mixed variant stock"

Use it.todo('description') format so tests are pending not failing. Import describe and it from vitest.
  </action>
  <verify>
    <automated>pnpm test apps/server/src/modules/product/product.service.test.ts --run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>Server filter test file exists with 8 todo stubs, pnpm test shows them as pending</done>
</task>

<task type="auto">
  <name>Task 2: Create client filter component and hook test stubs</name>
  <files>
    apps/client/src/components/filters/attribute-filter.test.tsx
    apps/client/src/components/filters/price-filter.test.tsx
    apps/client/src/components/filters/filter-drawer.test.tsx
    apps/client/src/components/filters/availability-filter.test.tsx
    apps/client/src/hooks/use-filters.test.tsx
  </files>
  <action>
Create 5 test files with it.todo() stubs:

1. attribute-filter.test.tsx (FILT-01, FILT-03):
   - "renders SELECT attributes as checkbox groups"
   - "renders RANGE attributes as slider"
   - "renders BOOLEAN attributes as single checkbox"
   - "shows facet count next to each checkbox value"
   - "toggling checkbox updates filter state"

2. price-filter.test.tsx (FILT-02):
   - "renders dual-handle range slider"
   - "renders min and max input fields"
   - "slider change updates local state immediately"
   - "URL updates are debounced by 300ms"
   - "manual input syncs with slider position"

3. filter-drawer.test.tsx (FILT-06):
   - "renders filter button on mobile with active count badge"
   - "opens Sheet drawer on button click"
   - "Apply button commits pending filters to URL"
   - "Clear All button resets all filter values"

4. availability-filter.test.tsx (FILT-07):
   - "renders In Stock checkbox option"
   - "renders Out of Stock checkbox option"
   - "toggling availability updates filter state"

5. use-filters.test.tsx (FILT-04):
   - "initializes with default filter values"
   - "persists filter state to URL search params"
   - "restores filter state from URL on mount"
   - "resets page to 1 when filters change"
   - "clearOnDefault removes default values from URL"

Each file imports describe, it from vitest. Use it.todo() for all stubs.
  </action>
  <verify>
    <automated>pnpm test apps/client/src/components/filters/ apps/client/src/hooks/use-filters.test.tsx --run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>5 client test files exist with 22 total todo stubs, all pass as pending</done>
</task>

</tasks>

<verification>
pnpm test --run --reporter=dot shows all new test stubs as pending (not failing)
</verification>

<success_criteria>
- 7 test files created covering all FILT-01 through FILT-07
- 30 total it.todo() stubs across server and client
- pnpm test runs cleanly with no failures
</success_criteria>

<output>
After completion, create `.planning/phases/06-filter-system/06-00-SUMMARY.md`
</output>
