---
phase: 06-filter-system
plan: 04
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-03
files_modified:
  - apps/client/src/app/categories/[slug]/page.tsx
  - apps/client/src/lib/api.ts
autonomous: true
requirements:
  - FILT-01
  - FILT-04

must_haves:
  truths:
    - "Category page reads URL search params and passes them to server filter API"
    - "Category page renders FilterSidebar on desktop and FilterDrawer on mobile alongside product grid"
    - "Facet counts are fetched and passed to filter components"
    - "Copying a filtered URL and opening it reproduces the exact filtered view"
  artifacts:
    - path: "apps/client/src/app/categories/[slug]/page.tsx"
      provides: "Server Component category page with filter integration"
      contains: "FilterSidebar"
    - path: "apps/client/src/lib/api.ts"
      provides: "API client methods for filter and facets endpoints"
      contains: "filterProducts"
  key_links:
    - from: "apps/client/src/app/categories/[slug]/page.tsx"
      to: "apps/client/src/lib/api.ts"
      via: "api.products.filter and api.products.facets calls"
      pattern: "api\\.products\\.filter|api\\.products\\.facets"
    - from: "apps/client/src/app/categories/[slug]/page.tsx"
      to: "apps/client/src/components/filters/filter-sidebar.tsx"
      via: "renders FilterSidebar with categoryAttributes and facetCounts"
      pattern: "FilterSidebar"
    - from: "apps/client/src/app/categories/[slug]/page.tsx"
      to: "apps/client/src/components/filters/filter-drawer.tsx"
      via: "renders FilterDrawer for mobile"
      pattern: "FilterDrawer"
---

<objective>
Integrate filter components into the category page Server Component, wiring URL searchParams to the server filter API and passing facet counts to filter UI.

Purpose: Complete the end-to-end filter flow: URL params -> server API -> filtered products + facet counts -> filter UI + product grid.
Output: Updated category page with filters, API client extensions for filter/facets endpoints.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-filter-system/06-RESEARCH.md
@.planning/phases/06-filter-system/06-01-SUMMARY.md
@.planning/phases/06-filter-system/06-02-SUMMARY.md
@.planning/phases/06-filter-system/06-03-SUMMARY.md

@apps/client/src/app/categories/[slug]/page.tsx
@apps/client/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend API client with filter and facets methods</name>
  <files>apps/client/src/lib/api.ts</files>
  <action>
Add two new methods to the api.products namespace in the client API module:

1. api.products.filter(params):
   - GET /api/products/filter with query params: categoryPath, minPrice, maxPrice, brands (comma-separated), attributes (comma-separated key:value), availability (comma-separated), page, limit, sortBy, sortOrder
   - Returns { success, data: products[], total, totalPages }

2. api.products.facets(params):
   - GET /api/products/facets with query params: categoryPath, minPrice, maxPrice, brands, attributes, availability
   - Returns { success, data: { brands: [{id, name, count}], attributes: {key: [{value, count}]}, availability: [{status, count}], priceRange: {min, max} } }

Build query string from params, omitting undefined/default values. Reuse existing fetcher pattern from the api module (with auth token forwarding if present).
  </action>
  <verify>
    <automated>pnpm test apps/client/ --run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>API client has filter and facets methods that call correct server endpoints</done>
</task>

<task type="auto">
  <name>Task 2: Wire filters into category page Server Component</name>
  <files>apps/client/src/app/categories/[slug]/page.tsx</files>
  <action>
Update the existing category page (apps/client/src/app/categories/[slug]/page.tsx) to integrate filters:

1. Read searchParams (Next.js 15+ async: const query = await searchParams)
2. Parse filter values from query: minPrice, maxPrice, brands (split comma), attributes (split comma), availability (split comma), page, sortBy, sortOrder
3. Fetch category by slug (existing) to get category.path and category.attributes (CategoryAttribute[])
4. Call api.products.filter({ categoryPath: category.path, ...parsedFilters }) for filtered products
5. Call api.products.facets({ categoryPath: category.path, ...parsedFilters }) for facet counts
6. Use Promise.all for parallel fetch of products and facets

7. Update the layout to a two-column flex layout:
   - Left: FilterSidebar (hidden on mobile via CSS, visible lg+) with categoryAttributes and facetCounts
   - Right: Main content area with:
     a. FilterDrawer trigger (visible only on mobile, lg:hidden)
     b. ActiveFilters chips (Suspense wrapped)
     c. Results count text: "{total} products found"
     d. Existing ProductGrid with filtered products
     e. Existing pagination (using page from filters)

8. Wrap filter components in NuqsAdapter if required by nuqs for Server Component compatibility (check nuqs docs -- may need <NuqsAdapter> at layout level).

9. Add generateMetadata for SEO: canonical URL to base /categories/[slug] (without filter params), robots noindex when any filters are applied (per research pitfall 6).

10. Import Suspense from react, wrap client filter components appropriately.
  </action>
  <verify>
    <automated>pnpm test apps/client/ --run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>Category page shows FilterSidebar on desktop, FilterDrawer on mobile, filtered products, facet counts, and active filter chips. URL params drive server-side filtering. SEO canonical/noindex applied.</done>
</task>

</tasks>

<verification>
pnpm test --run --reporter=dot shows all tests green across server and client
</verification>

<success_criteria>
- Category page fetches filtered products and facet counts from server API
- FilterSidebar visible on desktop with all filter groups populated
- FilterDrawer available on mobile with apply/clear
- ActiveFilters shows current filters as removable chips
- URL parameters drive filtering (shareable/bookmarkable)
- SEO: canonical to base URL, noindex on filtered views
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-filter-system/06-04-SUMMARY.md`
</output>
