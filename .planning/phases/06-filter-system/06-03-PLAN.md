---
phase: 06-filter-system
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - apps/client/src/components/filters/filter-sidebar.tsx
  - apps/client/src/components/filters/filter-drawer.tsx
  - apps/client/src/components/filters/filter-content.tsx
  - apps/client/src/components/filters/filter-button.tsx
autonomous: true
requirements:
  - FILT-06

must_haves:
  truths:
    - "Desktop shows fixed sidebar with all filter groups"
    - "Mobile shows filter button that opens full-screen Sheet drawer"
    - "Mobile drawer has Apply and Clear All buttons"
    - "Same FilterContent component used in both sidebar and drawer"
  artifacts:
    - path: "apps/client/src/components/filters/filter-content.tsx"
      provides: "Shared filter content (all filter groups composed)"
      contains: "FilterContent"
    - path: "apps/client/src/components/filters/filter-sidebar.tsx"
      provides: "Desktop sidebar wrapper"
      contains: "FilterSidebar"
    - path: "apps/client/src/components/filters/filter-drawer.tsx"
      provides: "Mobile Sheet drawer with apply/clear"
      contains: "FilterDrawer"
  key_links:
    - from: "apps/client/src/components/filters/filter-sidebar.tsx"
      to: "apps/client/src/components/filters/filter-content.tsx"
      via: "renders FilterContent inside desktop sidebar"
      pattern: "FilterContent"
    - from: "apps/client/src/components/filters/filter-drawer.tsx"
      to: "apps/client/src/components/filters/filter-content.tsx"
      via: "renders FilterContent inside Sheet"
      pattern: "FilterContent"
---

<objective>
Build the responsive filter layout: desktop sidebar and mobile full-screen drawer, sharing a common FilterContent component.

Purpose: Provide responsive filter UI -- always-visible sidebar on desktop (lg+), full-screen Sheet drawer on mobile with apply/clear buttons.
Output: FilterSidebar, FilterDrawer, FilterContent, FilterButton components.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-filter-system/06-RESEARCH.md
@.planning/phases/06-filter-system/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FilterContent and FilterSidebar (desktop layout)</name>
  <files>apps/client/src/components/filters/filter-content.tsx, apps/client/src/components/filters/filter-sidebar.tsx</files>
  <action>
1. Create filter-content.tsx:
   - 'use client' directive
   - Props: categoryAttributes (array of CategoryAttribute), facetCounts ({ brands, attributes, availability, priceRange })
   - Renders all filter groups in order, separated by Separator components:
     a. PriceFilter (pass priceRange.min, priceRange.max)
     b. Brand filter section: map facetCounts.brands to Checkbox items using useFilters brands array
     c. Map categoryAttributes to AttributeFilter components (pass matching facetCounts.attributes[key])
     d. AvailabilityFilter (pass facetCounts.availability)
   - Each section has a collapsible header (use details/summary or simple state toggle)

2. Create filter-sidebar.tsx:
   - 'use client' directive
   - Props: same as FilterContent (categoryAttributes, facetCounts)
   - Renders aside element with className "hidden lg:block w-64 shrink-0"
   - Sticky positioning: "sticky top-20" wrapper
   - Header "Filters" with ActiveFilters summary below
   - Renders FilterContent inside scrollable container
  </action>
  <verify>
    <automated>pnpm test apps/client/src/components/filters/ --run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>FilterContent composes all filter groups, FilterSidebar wraps it in sticky desktop layout</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create FilterDrawer (mobile Sheet) and FilterButton</name>
  <files>apps/client/src/components/filters/filter-drawer.tsx, apps/client/src/components/filters/filter-button.tsx, apps/client/src/components/filters/filter-drawer.test.tsx</files>
  <behavior>
    - FilterButton renders only on mobile (lg:hidden) with filter icon and active count badge
    - FilterDrawer opens Sheet from left side, full width on mobile (sm:max-w-md)
    - Drawer tracks pendingFilters locally, only commits to URL on Apply
    - Clear All resets pendingFilters to defaults
    - Apply button calls setFilters(pendingFilters) and closes drawer
  </behavior>
  <action>
1. Create filter-button.tsx:
   - 'use client' directive
   - Props: activeFilterCount (number)
   - Renders Button (variant="outline") with Filter icon from lucide-react
   - Shows count badge when activeFilterCount > 0
   - ClassName includes "lg:hidden" to hide on desktop

2. Create filter-drawer.tsx:
   - 'use client' directive
   - Props: categoryAttributes, facetCounts
   - Use shadcn/ui Sheet component with SheetContent side="left"
   - State: open (boolean), pendingFilters (copy of current filters on open)
   - On open: snapshot current filters into pendingFilters
   - FilterContent inside renders with pendingFilters (NOT directly modifying URL)
   - Bottom bar (absolute positioned): Apply button (commits pendingFilters to URL via setFilters, closes) and Clear All button (resets pendingFilters to defaults)
   - Count active filters for badge on trigger button

3. Convert FILT-06 it.todo() stubs in filter-drawer.test.tsx into real tests:
   - Test that FilterButton renders with correct badge count
   - Test that Sheet opens when button clicked
   - Test that Apply button calls setFilters
   - Test that Clear All resets filter state
  </action>
  <verify>
    <automated>pnpm test apps/client/src/components/filters/filter-drawer.test.tsx --run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>Mobile filter drawer uses Sheet with pending state, Apply/Clear buttons work correctly, FILT-06 tests pass</done>
</task>

</tasks>

<verification>
pnpm test apps/client/src/components/filters/ --run shows all filter component tests green
</verification>

<success_criteria>
- FilterContent composes PriceFilter, brand checkboxes, AttributeFilters, AvailabilityFilter
- FilterSidebar is hidden on mobile, sticky on desktop
- FilterDrawer opens Sheet on mobile with pending filter state
- Apply commits to URL, Clear All resets
- Active filter count badge shown on mobile button
- FILT-06 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-filter-system/06-03-SUMMARY.md`
</output>
