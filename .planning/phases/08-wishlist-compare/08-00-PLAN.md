---
phase: 08-wishlist-compare
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/types/src/index.ts
  - tests/setup.ts
  - tests/fixtures/wishlist.fixtures.ts
  - tests/wishlist/wishlist-service.test.ts
  - tests/wishlist/wishlist-events.test.ts
  - tests/compare/compare-store.test.ts
autonomous: true
requirements: [WISH-01, WISH-02, WISH-03, WISH-04, WISH-05, WISH-06]

must_haves:
  truths:
    - "WishlistItem model has priceAtAdd Int field in Prisma schema"
    - "WishlistItem TypeScript interface includes priceAtAdd number field"
    - "Prisma mock includes wishlist and wishlistItem with all CRUD methods"
    - "Test stub files exist for all wishlist and compare test cases"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "WishlistItem.priceAtAdd field"
      contains: "priceAtAdd"
    - path: "packages/types/src/index.ts"
      provides: "Updated WishlistItem interface with priceAtAdd"
      contains: "priceAtAdd"
    - path: "tests/setup.ts"
      provides: "wishlist and wishlistItem Prisma mocks"
      contains: "wishlist:"
    - path: "tests/fixtures/wishlist.fixtures.ts"
      provides: "Shared wishlist and compare test fixtures"
  key_links:
    - from: "tests/setup.ts"
      to: "packages/db/prisma/schema.prisma"
      via: "Mock mirrors schema model methods"
      pattern: "wishlistItem.*findMany"
---

<objective>
Wave 0: Prisma schema migration for priceAtAdd, types update, test infrastructure for wishlist and compare features.

Purpose: The existing WishlistItem model lacks the priceAtAdd field required for price-drop badges. All downstream plans depend on this schema change, updated types, and test stubs.
Output: Updated schema, types, test setup, fixtures, and test stub files.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wishlist-compare/08-CONTEXT.md
@.planning/phases/08-wishlist-compare/08-RESEARCH.md

@packages/db/prisma/schema.prisma
@packages/types/src/index.ts
@tests/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add priceAtAdd to WishlistItem schema and update types</name>
  <files>packages/db/prisma/schema.prisma, packages/types/src/index.ts</files>
  <action>
1. In packages/db/prisma/schema.prisma, add `priceAtAdd Int @default(0)` field to the WishlistItem model, between `addedAt` and `notifyOnPriceDrop`. This stores the product price in cents at the time the item was wishlisted, used for price-drop badge display.

2. Run `cd packages/db && npx prisma db push` to apply the schema change (or `npx prisma generate` if no database is running — the key output is a regenerated Prisma client).

3. In packages/types/src/index.ts, add `priceAtAdd: number;` to the WishlistItem interface between `addedAt` and `notifyOnPriceDrop`.

4. Also add new event types to the types file for the EventBus:
```typescript
// WISHLIST EVENT TYPES
export interface WishlistPriceDropEvent {
  productId: string;
  oldPrice: number;
  newPrice: number;
  affectedUserIds: string[];
}

export interface WishlistRestockEvent {
  productId: string;
  affectedUserIds: string[];
}
```
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx tsc --noEmit -p packages/types/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>WishlistItem has priceAtAdd field in both Prisma schema and TypeScript types. Event types exported.</done>
</task>

<task type="auto">
  <name>Task 2: Test infrastructure — mocks, fixtures, and test stubs</name>
  <files>tests/setup.ts, tests/fixtures/wishlist.fixtures.ts, tests/wishlist/wishlist-service.test.ts, tests/wishlist/wishlist-events.test.ts, tests/compare/compare-store.test.ts</files>
  <action>
1. In tests/setup.ts, add `wishlist` and `wishlistItem` mocks to the prismaMock object (same pattern as existing models):
```typescript
wishlist: {
  findMany: vi.fn(),
  findUnique: vi.fn(),
  findFirst: vi.fn(),
  create: vi.fn(),
  update: vi.fn(),
  delete: vi.fn(),
  count: vi.fn(),
},
wishlistItem: {
  findMany: vi.fn(),
  findUnique: vi.fn(),
  findFirst: vi.fn(),
  create: vi.fn(),
  update: vi.fn(),
  upsert: vi.fn(),
  delete: vi.fn(),
  deleteMany: vi.fn(),
  count: vi.fn(),
},
```

2. Create tests/fixtures/wishlist.fixtures.ts with shared mock data:
- mockWishlist: { id: 'wl-1', name: 'My Wishlist', userId: 'user-1', isPublic: false, createdAt, updatedAt }
- mockWishlistItem: { id: 'wli-1', wishlistId: 'wl-1', productId: 'prod-1', addedAt, priceAtAdd: 1999, notifyOnPriceDrop: true, notifyOnRestock: true }
- mockWishlistItemWithProduct: same but with product relation included (spread mockSimpleProduct)
- mockCompareProducts: array of 3 products with different attributes for diff-highlighting tests

3. Create test stub files with it.todo() for all behaviors:
- tests/wishlist/wishlist-service.test.ts: stubs for getWishlist, addItem (with priceAtAdd), removeItem, syncItems (merge + dedup), updateNotifyPrefs
- tests/wishlist/wishlist-events.test.ts: stubs for product.updated triggers priceDrop event, inventory.stockUpdated triggers restock event, no event when price increases, no event when already in stock
- tests/compare/compare-store.test.ts: stubs for addToCompare, removeFromCompare, clearCompare, max 4 enforcement, hasItem check

Use the exact it.todo() pattern from Phase 13/14 test stubs.
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && pnpm vitest run tests/wishlist/ tests/compare/ tests/fixtures/wishlist.fixtures.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Prisma mocks include wishlist/wishlistItem. Fixture file exports test data. All test stubs exist with it.todo() markers. vitest runs without errors (all tests are todo/skipped).</done>
</task>

</tasks>

<verification>
- `npx prisma validate` passes (schema is valid)
- TypeScript compiles without errors for packages/types
- `pnpm vitest run tests/wishlist/ tests/compare/` runs with 0 failures (all todos)
- tests/setup.ts exports prismaMock with wishlist and wishlistItem keys
</verification>

<success_criteria>
- WishlistItem.priceAtAdd exists in Prisma schema and TypeScript types
- Test infrastructure ready for all 6 requirement IDs
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-wishlist-compare/08-00-SUMMARY.md`
</output>
