---
phase: 08-wishlist-compare
plan: 04
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - apps/client/src/app/compare/page.tsx
  - apps/client/src/app/compare/compare-page-client.tsx
  - apps/client/src/lib/api.ts
autonomous: true
requirements: [WISH-04, WISH-05]

must_haves:
  truths:
    - "Compare page shows side-by-side specification table for 2-4 products"
    - "Differing attribute values are highlighted with subtle background color"
    - "Compare page handles empty state with guidance to browse products"
    - "Products can be removed from comparison directly on the compare page"
  artifacts:
    - path: "apps/client/src/app/compare/page.tsx"
      provides: "Compare page entry point"
    - path: "apps/client/src/app/compare/compare-page-client.tsx"
      provides: "Client compare table with diff highlighting"
      exports: ["ComparePageClient"]
  key_links:
    - from: "apps/client/src/app/compare/compare-page-client.tsx"
      to: "apps/client/src/stores/compare-store.ts"
      via: "useCompareStore for selected product IDs"
      pattern: "useCompareStore"
    - from: "apps/client/src/app/compare/compare-page-client.tsx"
      to: "apps/server/src/modules/product/product.routes.ts"
      via: "Fetch full product details by ID for comparison"
      pattern: "api/products"
---

<objective>
Compare page with side-by-side specification table and difference highlighting.

Purpose: Users can compare 2-4 selected products in a detailed specification table. Differing values are highlighted to help purchasing decisions. Pure client-side feature (sessionStorage, no server storage).
Output: /compare page with responsive comparison table.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-wishlist-compare/08-CONTEXT.md
@.planning/phases/08-wishlist-compare/08-RESEARCH.md
@.planning/phases/08-wishlist-compare/08-02-SUMMARY.md

@apps/client/src/stores/compare-store.ts
@apps/client/src/app/products/[slug]/product-page-client.tsx

<interfaces>
From packages/types/src/index.ts:
```typescript
export interface Product {
  id: string;
  name: string;
  slug: string;
  price: number;
  images: string[];
  attributes: Record<string, any>;
  productType: ProductType;
  categoryId: string;
  brandId?: string;
}

export interface CategoryAttribute {
  id: string;
  name: string;
  key: string;
  type: AttributeType;
  values: string[];
}
```

From apps/client/src/stores/compare-store.ts:
```typescript
export const useCompareStore = create<CompareStore>()(persist(...));
// items: CompareItem[] with productId, name, imageUrl, slug
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compare page with side-by-side table and diff highlighting</name>
  <files>apps/client/src/app/compare/page.tsx, apps/client/src/app/compare/compare-page-client.tsx</files>
  <action>
1. Create apps/client/src/app/compare/page.tsx as Server Component:
- Export metadata: { title: 'Compare Products' }
- Renders ComparePageClient

2. Create apps/client/src/app/compare/compare-page-client.tsx as 'use client':
- Uses useCompareStore to read selected items (productIds)
- On mount, fetch full product details for each productId from GET /api/products/:slug (or /api/products/:id) with category + attributes included. Use Promise.all for parallel fetches.
- Handle hydration: show loading skeleton until mounted and data fetched

**Empty state** (items.length === 0):
- Show: scale/compare icon illustration + "No products selected for comparison" + "Browse products and use the compare checkbox to select items" + Link to /products

**Minimum check** (items.length < 2):
- Show: "Select at least 2 products to compare" + current selection thumbnails + Link to browse more

**Comparison table** (items.length >= 2):
- Horizontal layout: each product is a column, attributes are rows
- Header row: product image (120x120), product name (linked to /products/slug), price, "Remove" button (X icon calling removeItem)
- Build unified attribute key list from all compared products' attributes (union of all keys)
- For each attribute key, create a row:
  - First column: attribute display name (from category attributes if available, fallback to key)
  - Subsequent columns: each product's value for that key (or "-" if missing)
  - isDifferent helper: `const values = products.map(p => String(p.attributes?.[key] ?? '')); return new Set(values).size > 1;`
  - If isDifferent(key, products), apply `bg-yellow-50` to the row cells
- Additional comparison rows (not from attributes): Price, Product Type, Brand, Category, Stock Status
- Price row: format as dollars, highlight if different
- Responsive: on mobile, table scrolls horizontally with sticky first column (attribute names)

3. Optionally add a helper in apps/client/src/lib/api.ts (or inline):
- fetchProductById(id: string) or fetchProductBySlug(slug: string) that calls the server API
- Include category with attributes in the response for display name mapping
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx tsc --noEmit -p apps/client/tsconfig.json 2>&1 | head -20 && pnpm vitest run tests/compare/compare-page.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Compare page renders side-by-side table with products as columns and specs as rows. Differing values highlighted with bg-yellow-50. Empty/minimum states handled. Mobile horizontal scroll works.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors for apps/client
- /compare page renders correctly with 0, 1, 2, 3, and 4 products
- Diff highlighting applies bg-yellow-50 only to rows where values differ
- Products can be removed from the compare page
- Mobile layout scrolls horizontally
</verification>

<success_criteria>
- Side-by-side comparison table with all product attributes displayed
- Differing values visually highlighted per user decision (subtle yellow background)
- Empty state and minimum-products state handled gracefully
- Responsive horizontal scroll on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/08-wishlist-compare/08-04-SUMMARY.md`
</output>
