---
phase: 14-inventory-management
plan: 04
type: execute
wave: 4
depends_on: [14-03]
files_modified:
  - apps/admin/src/app/dashboard/inventory/page.tsx
  - apps/admin/src/app/dashboard/inventory/warehouses/page.tsx
  - apps/admin/src/app/dashboard/inventory/warehouses/warehouse-form.tsx
  - apps/admin/src/app/dashboard/inventory/movements/page.tsx
  - apps/admin/src/app/dashboard/inventory/adjustments/page.tsx
autonomous: false
requirements: [INV-03, INV-06, INV-07]

must_haves:
  truths:
    - "Admin can view inventory dashboard with stock levels and active alerts"
    - "Admin can create, edit, and deactivate warehouses"
    - "Admin can view stock movement history with reason filtering"
    - "Admin can manually adjust stock with reason tracking"
  artifacts:
    - path: "apps/admin/src/app/dashboard/inventory/page.tsx"
      provides: "Inventory dashboard with stock overview and alerts"
      min_lines: 50
    - path: "apps/admin/src/app/dashboard/inventory/warehouses/page.tsx"
      provides: "Warehouse list with CRUD actions"
      min_lines: 50
    - path: "apps/admin/src/app/dashboard/inventory/warehouses/warehouse-form.tsx"
      provides: "Reusable warehouse create/edit form"
      min_lines: 40
    - path: "apps/admin/src/app/dashboard/inventory/movements/page.tsx"
      provides: "Movement history table with reason filter"
      min_lines: 40
    - path: "apps/admin/src/app/dashboard/inventory/adjustments/page.tsx"
      provides: "Manual stock adjustment form"
      min_lines: 40
  key_links:
    - from: "apps/admin/src/app/dashboard/inventory/page.tsx"
      to: "apps/admin/src/lib/api.ts"
      via: "api.inventory.dashboard()"
      pattern: "api\\.inventory\\.dashboard"
    - from: "apps/admin/src/app/dashboard/inventory/warehouses/page.tsx"
      to: "apps/admin/src/lib/api.ts"
      via: "api.inventory.warehouses.getAll()"
      pattern: "api\\.inventory\\.warehouses"
---

<objective>
Build the admin inventory management UI pages.

Purpose: Give admins visibility into stock levels, low stock alerts, warehouse management, movement history, and manual stock adjustments.
Output: Five admin pages under /dashboard/inventory covering INV-03, INV-06, and INV-07.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-inventory-management/14-RESEARCH.md
@.planning/phases/14-inventory-management/14-03-SUMMARY.md

@apps/admin/src/app/dashboard/shipping/zones/page.tsx
@apps/admin/src/lib/api.ts
</context>

<interfaces>
<!-- Admin API client from Plan 03: -->
```typescript
api.inventory.dashboard()        // => { totalItems, totalWarehouses, lowStockCount, recentMovements }
api.inventory.stock.getByVariant(variantId)
api.inventory.stock.adjust(data) // => { variantId, warehouseId, quantity, reason, note?, reference? }
api.inventory.alerts()           // => array of low stock items
api.inventory.warehouses.getAll()
api.inventory.warehouses.getById(id)
api.inventory.warehouses.create(data)
api.inventory.warehouses.update(id, data)
api.inventory.warehouses.delete(id)
api.inventory.movements.getAll(params?) // => { inventoryItemId?, reason?, page?, limit? }
```

<!-- UI pattern from shipping zones page: 'use client', useState for data, useEffect for fetch, Tailwind table -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Inventory dashboard and warehouse management pages</name>
  <files>apps/admin/src/app/dashboard/inventory/page.tsx, apps/admin/src/app/dashboard/inventory/warehouses/page.tsx, apps/admin/src/app/dashboard/inventory/warehouses/warehouse-form.tsx</files>
  <action>
1. Create apps/admin/src/app/dashboard/inventory/page.tsx ('use client'):
- Fetch dashboard data on mount via api.inventory.dashboard()
- Fetch low stock alerts via api.inventory.alerts()
- Display 4 stat cards at top: Total SKUs Tracked, Total Warehouses, Low Stock Alerts (highlighted red if > 0), Recent Movements
- Below cards: "Low Stock Alerts" section with a table showing: Product Name, SKU, Warehouse, Available, Threshold, with rows highlighted in amber/red
- Each stat card uses Tailwind: rounded-lg border p-6 shadow-sm
- Loading state with "Loading..." text while fetching

2. Create apps/admin/src/app/dashboard/inventory/warehouses/page.tsx ('use client'):
- Fetch warehouses via api.inventory.warehouses.getAll()
- Table columns: Name, Code, City/State, Country, Priority, Status (Active/Inactive), Actions
- Actions: Edit (opens form), Deactivate/Activate toggle
- "Add Warehouse" button at top right that shows the warehouse-form
- Use useState for showForm and editingWarehouse
- On deactivate: call api.inventory.warehouses.delete(id), refresh list
- Tailwind table pattern: same as shipping zones page

3. Create apps/admin/src/app/dashboard/inventory/warehouses/warehouse-form.tsx ('use client'):
- Props: { warehouse?: any; onSave: () => void; onCancel: () => void }
- Form fields: name (text), code (text, uppercase), address, city, state, country (2-char), zipCode, latitude (number, optional), longitude (number, optional), priority (number), isActive (checkbox)
- On submit: call api.inventory.warehouses.create or .update depending on whether warehouse prop is provided
- Basic validation: name and code required
- Tailwind form styling consistent with existing admin forms
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx tsc --noEmit apps/admin/src/app/dashboard/inventory/page.tsx apps/admin/src/app/dashboard/inventory/warehouses/page.tsx 2>&1 | head -20 || echo "TypeScript check done"</automated>
  </verify>
  <done>Inventory dashboard shows stats + low stock alerts. Warehouse page has table with CRUD. Warehouse form handles create and edit.</done>
</task>

<task type="auto">
  <name>Task 2: Movement history and stock adjustment pages</name>
  <files>apps/admin/src/app/dashboard/inventory/movements/page.tsx, apps/admin/src/app/dashboard/inventory/adjustments/page.tsx</files>
  <action>
1. Create apps/admin/src/app/dashboard/inventory/movements/page.tsx ('use client'):
- Fetch movements via api.inventory.movements.getAll(params)
- Filter bar at top: Reason dropdown (All, SALE, RETURN, MANUAL_ADJUSTMENT, DAMAGE, RESTOCK, RESERVATION, RESERVATION_RELEASE), page/limit controls
- Table columns: Date (formatted), Product/SKU, Warehouse, Quantity (positive green, negative red), Reason (badge with color), Reference, Note
- Reason badges with colors: SALE (blue), RETURN (green), MANUAL_ADJUSTMENT (yellow), DAMAGE (red), RESTOCK (purple), RESERVATION (gray), RESERVATION_RELEASE (gray)
- Pagination at bottom with Previous/Next buttons
- Tailwind table pattern matching existing admin pages

2. Create apps/admin/src/app/dashboard/inventory/adjustments/page.tsx ('use client'):
- Form for manual stock adjustment
- Fields:
  - Variant ID or SKU lookup (text input — for now just variantId text field)
  - Warehouse (dropdown populated from api.inventory.warehouses.getAll())
  - Quantity (number — positive to add, negative to remove)
  - Reason (dropdown: MANUAL_ADJUSTMENT, DAMAGE, RESTOCK, RETURN)
  - Note (textarea, optional)
- On submit: call api.inventory.stock.adjust(data)
- Success: show green success message, reset form
- Error: show red error message
- Include a note explaining: "Positive quantity adds stock, negative removes stock"
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx tsc --noEmit apps/admin/src/app/dashboard/inventory/movements/page.tsx apps/admin/src/app/dashboard/inventory/adjustments/page.tsx 2>&1 | head -20 || echo "TypeScript check done"</automated>
  </verify>
  <done>Movements page shows filterable, paginated history with color-coded reasons. Adjustments page provides a form for manual stock changes with warehouse selection and reason tracking.</done>
</task>

</tasks>

<verification>
All 5 admin pages compile without TypeScript errors.
pnpm vitest run tests/inventory/ still passes (no regressions).
</verification>

<success_criteria>
- Dashboard shows stock overview stats and low stock alert table
- Warehouse list with create/edit/deactivate functionality
- Movement history with reason filter and color-coded badges
- Stock adjustment form with warehouse dropdown and reason selection
- All pages use 'use client' with api.inventory.* fetcher pattern
- Tailwind styling consistent with existing admin pages
</success_criteria>

<output>
After completion, create `.planning/phases/14-inventory-management/14-04-SUMMARY.md`
</output>
