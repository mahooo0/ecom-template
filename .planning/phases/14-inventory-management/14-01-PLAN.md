---
phase: 14-inventory-management
plan: 01
type: execute
wave: 1
depends_on: [14-00]
files_modified:
  - apps/server/src/modules/inventory/inventory.service.ts
  - apps/server/src/modules/inventory/inventory.validation.ts
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-04, INV-07, INV-08]

must_haves:
  truths:
    - "Stock can be queried per variant with available = quantity - reserved"
    - "Low stock alerts return items where available is at or below threshold"
    - "Warehouses can be created, listed, updated, and deactivated"
    - "Nearest warehouse is selected by Haversine distance with priority fallback"
    - "Stock movements are recorded with reason tracking"
    - "SKU is auto-generated from product SKU prefix + variant option values"
  artifacts:
    - path: "apps/server/src/modules/inventory/inventory.service.ts"
      provides: "InventoryService class with all business logic"
      exports: ["inventoryService"]
      min_lines: 200
    - path: "apps/server/src/modules/inventory/inventory.validation.ts"
      provides: "Zod schemas for all inventory endpoints"
      exports: ["createWarehouseSchema", "updateWarehouseSchema", "adjustStockSchema", "getMovementsSchema"]
  key_links:
    - from: "apps/server/src/modules/inventory/inventory.service.ts"
      to: "@repo/db"
      via: "prisma import"
      pattern: "import.*prisma.*@repo/db"
    - from: "apps/server/src/modules/inventory/inventory.service.ts"
      to: "apps/server/src/common/events/event-bus.ts"
      via: "eventBus emit"
      pattern: "eventBus\\.emit"
---

<objective>
Build the InventoryService with all core business logic and Zod validation schemas.

Purpose: Implement the service layer covering stock tracking, warehouse management, nearest-warehouse routing, movement history, low stock alerts, and SKU auto-generation. This is the foundation that API routes (Plan 03) will expose.
Output: inventory.service.ts with InventoryService class exported as singleton, inventory.validation.ts with Zod schemas.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-inventory-management/14-RESEARCH.md
@.planning/phases/14-inventory-management/14-00-SUMMARY.md

@apps/server/src/modules/shipping/shipping.service.ts
@apps/server/src/modules/shipping/shipping.validation.ts
@apps/server/src/common/events/event-bus.ts
@apps/server/src/common/middleware/error-handler.ts
@packages/db/prisma/schema.prisma
</context>

<interfaces>
<!-- Pattern from shipping.service.ts: class with methods, import prisma from @repo/db, throw AppError, export singleton -->
<!-- Pattern from shipping.validation.ts: z.object({ body: z.object({...}) }) for POST/PUT, z.object({ query: ... }) for GET -->

From packages/db/prisma/schema.prisma:
```prisma
enum StockMovementReason {
  SALE, RETURN, MANUAL_ADJUSTMENT, DAMAGE, RESTOCK, RESERVATION, RESERVATION_RELEASE
}
```

From event-bus.ts — must add new event types:
```typescript
'inventory.lowStock': { variantId: string; warehouseId: string; available: number; threshold: number };
'inventory.stockUpdated': { variantId: string; warehouseId: string; quantity: number; available: number };
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InventoryService with stock tracking, warehouse CRUD, routing, movements, alerts, and SKU generation</name>
  <files>apps/server/src/modules/inventory/inventory.service.ts, tests/inventory/inventory.service.test.ts</files>
  <behavior>
    - getStockByVariant(variantId): returns array of { warehouseId, warehouseName, quantity, reserved, available, lowStockThreshold }
    - getStockLevel(variantId, warehouseId): returns single inventory item with available = quantity - reserved
    - getLowStockAlerts(): returns items where (quantity - reserved) <= lowStockThreshold using $queryRaw
    - createWarehouse(data): creates warehouse with all fields, returns created record
    - getWarehouses(): returns all warehouses (optionally filter by isActive)
    - updateWarehouse(id, data): updates warehouse fields
    - deactivateWarehouse(id): sets isActive = false
    - findBestWarehouse(variantId, qty, destLat, destLon): uses Haversine formula for nearest warehouse with sufficient stock; falls back to priority when coords null; returns null if none available
    - adjustStock(variantId, warehouseId, quantity, reason, reference?, note?): creates StockMovement, updates InventoryItem quantity, emits inventory.stockUpdated event; if new available <= threshold, emits inventory.lowStock
    - getMovements(inventoryItemId, filters?): returns paginated movements with optional reason filter
    - generateSku(productSku, options): generates SKU as {PRODUCT_SKU}-{OPTION_VALUES} uppercased/slugified; checks uniqueness via prisma.productVariant.findUnique; appends -2, -3 etc if collision
    - getDashboardData(): returns { totalItems, totalWarehouses, lowStockCount, recentMovements }
  </behavior>
  <action>
First, convert the it.todo() stubs in tests/inventory/inventory.service.test.ts to real tests for at least these key behaviors:
- getStockByVariant returns available = quantity - reserved
- getLowStockAlerts returns only items below threshold
- findBestWarehouse picks closest warehouse (mock two warehouses at known coordinates)
- findBestWarehouse falls back to priority when coords are null
- adjustStock creates movement and updates quantity
- generateSku produces correct format and handles collisions

Then implement apps/server/src/modules/inventory/inventory.service.ts:
- Import { prisma } from '@repo/db' (with .js extension for ESM)
- Import { AppError } from error-handler (use relative path with .js extension)
- Import { eventBus } from event-bus (use relative path with .js extension)
- Add 'inventory.lowStock' and 'inventory.stockUpdated' event types to event-bus.ts EventMap

Class InventoryService with all methods listed in behavior block above.

Helper function haversineDistance(lat1, lon1, lat2, lon2) as a private module-level function.

Helper function available(item) = item.quantity - item.reserved used consistently throughout.

Export: `export const inventoryService = new InventoryService();`

IMPORTANT: Use .js extensions on all relative imports (ESM Node.js convention per project). Use AppError for all error cases (404, 409). All monetary values as integers (cents). All stock quantities as integers.
  </action>
  <verify>
    <automated>pnpm vitest run tests/inventory/inventory.service.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>InventoryService class covers INV-01 (stock tracking), INV-02 (low stock alerts), INV-03 (warehouse CRUD), INV-04 (nearest warehouse routing), INV-07 (movement history with reasons), INV-08 (SKU auto-generation). Tests pass for key behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: Zod validation schemas for inventory endpoints</name>
  <files>apps/server/src/modules/inventory/inventory.validation.ts</files>
  <action>
Create apps/server/src/modules/inventory/inventory.validation.ts following the exact pattern from shipping.validation.ts:

Schemas to create:
1. `createWarehouseSchema` — body: name (string 1-100), code (string 1-20 uppercase), address, city, state, country (2-char), zipCode, latitude (number nullable optional), longitude (number nullable optional), priority (int default 0), isActive (boolean default true)

2. `updateWarehouseSchema` — body: same fields as create but all .partial()

3. `adjustStockSchema` — body: variantId (string.cuid()), warehouseId (string.cuid()), quantity (int, can be positive or negative), reason (z.enum matching StockMovementReason values: 'SALE', 'RETURN', 'MANUAL_ADJUSTMENT', 'DAMAGE', 'RESTOCK'), note (string max 500 optional), reference (string optional)
   NOTE: Do NOT include RESERVATION or RESERVATION_RELEASE in the manual adjust schema — those are system-only reasons used by the reservation system.

4. `getMovementsSchema` — query: inventoryItemId (string optional), reason (enum optional), page (coerce number int default 1), limit (coerce number int default 50)

5. `reserveStockSchema` — body: variantId (string.cuid()), warehouseId (string.cuid() optional — if omitted, auto-select best warehouse), quantity (int positive), checkoutSessionId (string)

6. `commitReservationSchema` — body: checkoutSessionId (string)

7. `releaseReservationSchema` — body: checkoutSessionId (string)

8. `getStockSchema` — query: variantId (string optional), warehouseId (string optional)

Export all schemas as named exports.
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx tsc --noEmit apps/server/src/modules/inventory/inventory.validation.ts 2>&1 | head -20 || echo "TypeScript check done"</automated>
  </verify>
  <done>All 8 Zod schemas exported. Schema field types match Prisma model exactly. RESERVATION/RESERVATION_RELEASE excluded from manual adjust enum.</done>
</task>

</tasks>

<verification>
pnpm vitest run tests/inventory/inventory.service.test.ts passes with real tests green.
inventory.validation.ts compiles without TypeScript errors.
</verification>

<success_criteria>
- InventoryService has methods for stock tracking, warehouse CRUD, routing, movements, alerts, SKU gen
- available() helper consistently used (never raw quantity exposed)
- Haversine distance calculation with priority fallback
- eventBus events added for inventory.lowStock and inventory.stockUpdated
- Zod schemas cover all planned endpoints
- Tests pass for core service behaviors
</success_criteria>

<output>
After completion, create `.planning/phases/14-inventory-management/14-01-SUMMARY.md`
</output>
