---
phase: 01-database-schema-design
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/db/prisma/schema.prisma
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-08
  - SCHEMA-09
  - SCHEMA-12

must_haves:
  truths:
    - "User model has three roles (CUSTOMER, ADMIN, SUPER_ADMIN) with profile fields and address relation"
    - "Address model supports multiple addresses per user with a default flag"
    - "Wishlist and WishlistItem models allow users to save products"
    - "Review model supports star ratings 1-5, text, photos, moderation status, and verified purchase flag"
    - "Coupon and Promotion are separate models with date ranges, usage limits, and discount types"
    - "ShippingZone, ShippingMethod models support geographic zones with multiple rate types"
    - "Warehouse and InventoryItem models support multi-warehouse stock tracking per variant"
    - "All foreign keys have explicit indexes and referential actions (onDelete Cascade/Restrict/SetNull)"
    - "All unique constraints and validation constraints are properly defined"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "Complete Prisma schema with all 25+ models, enums, relations, and indexes"
      contains: "model User"
  key_links:
    - from: "User.role"
      to: "Role enum with SUPER_ADMIN"
      via: "Prisma enum field"
      pattern: "SUPER_ADMIN"
    - from: "Address.userId"
      to: "User.id"
      via: "foreign key with cascade delete"
      pattern: "onDelete: Cascade"
    - from: "InventoryItem.warehouseId"
      to: "Warehouse.id"
      via: "foreign key relation"
      pattern: "warehouse\\s+Warehouse"
    - from: "InventoryItem.variantId"
      to: "ProductVariant.id"
      via: "foreign key relation"
      pattern: "variant\\s+ProductVariant"
---

<objective>
Complete the Prisma schema by adding User (with SUPER_ADMIN role), Address, Wishlist, Review, Coupon, Promotion, Shipping, Warehouse, and Inventory models. Ensure all models across the entire schema have proper indexes, cascading deletes, and validation constraints.

Purpose: Finish the PostgreSQL data layer with all commerce entities and ensure referential integrity across the entire schema.
Output: Fully complete `packages/db/prisma/schema.prisma` with all models for the e-commerce template.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-schema-design/01-CONTEXT.md
@.planning/phases/01-database-schema-design/01-RESEARCH.md
@.planning/phases/01-database-schema-design/01-01-SUMMARY.md
@packages/db/prisma/schema.prisma

<interfaces>
<!-- After Plan 01, schema.prisma contains:
     - generator/datasource blocks
     - Enums: Role (CUSTOMER, ADMIN), ProductType, ProductStatus, WeightUnit, AttributeType
     - Models: User (basic), Product (full), ProductVariant, OptionGroup, OptionValue, VariantOption
     - Models: Category (full), CategoryAttribute, Brand, Tag, ProductTag, Collection, ProductCollection
     - Models: DigitalMeta, WeightedMeta, BundleItem

     This plan:
     1. Expands User model (add SUPER_ADMIN to Role, add profile fields)
     2. Adds Address, Wishlist/WishlistItem, Review models
     3. Adds Coupon, Promotion models
     4. Adds ShippingZone, ShippingMethod, Warehouse, InventoryItem models
     5. Adds relation fields on Product/ProductVariant for new models (reviews, wishlistItems, inventoryItems)
     6. Audits all models for missing indexes and referential actions
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add User profile, Address, Wishlist, and Review models</name>
  <files>packages/db/prisma/schema.prisma</files>
  <action>
Modify `packages/db/prisma/schema.prisma`:

**1. Update Role enum** - add SUPER_ADMIN:
```
enum Role {
  CUSTOMER
  ADMIN
  SUPER_ADMIN
}
```

**2. Expand User model** - add profile fields and relations:
- avatar: String? (URL)
- phone: String?
- isActive: Boolean @default(true)
- lastLoginAt: DateTime?
- addresses: Address[]
- reviews: Review[]
- wishlists: Wishlist[]
- Add @@index([clerkId]), @@index([role])

**3. Create Address model:**
- id: String @id @default(cuid())
- userId: String
- user: User @relation(fields: [userId], references: [id], onDelete: Cascade)
- firstName: String
- lastName: String
- street: String
- street2: String? (apartment, suite, etc.)
- city: String
- state: String
- zipCode: String
- country: String
- phone: String?
- isDefault: Boolean @default(false)
- label: String? ("Home", "Work", etc.)
- createdAt: DateTime @default(now())
- updatedAt: DateTime @updatedAt
- @@index([userId])
- @@map("addresses")

**4. Create Wishlist model:**
- id: String @id @default(cuid())
- name: String @default("My Wishlist")
- userId: String
- user: User @relation(fields: [userId], references: [id], onDelete: Cascade)
- items: WishlistItem[]
- isPublic: Boolean @default(false)
- createdAt: DateTime @default(now())
- updatedAt: DateTime @updatedAt
- @@index([userId])
- @@map("wishlists")

**5. Create WishlistItem model:**
- id: String @id @default(cuid())
- wishlistId: String
- wishlist: Wishlist @relation(fields: [wishlistId], references: [id], onDelete: Cascade)
- productId: String
- product: Product @relation(fields: [productId], references: [id], onDelete: Cascade)
- addedAt: DateTime @default(now())
- notifyOnPriceDrop: Boolean @default(true)
- notifyOnRestock: Boolean @default(true)
- @@unique([wishlistId, productId])
- @@index([wishlistId])
- @@index([productId])
- @@map("wishlist_items")

**6. Add ReviewStatus enum:** PENDING, APPROVED, REJECTED, FLAGGED

**7. Create Review model:**
- id: String @id @default(cuid())
- userId: String
- user: User @relation(fields: [userId], references: [id], onDelete: Cascade)
- productId: String
- product: Product @relation(fields: [productId], references: [id], onDelete: Cascade)
- rating: Int (1-5, validated in application layer)
- title: String?
- body: String? @db.Text
- photos: String[] (array of URLs)
- isVerifiedPurchase: Boolean @default(false)
- status: ReviewStatus @default(PENDING)
- helpfulCount: Int @default(0)
- createdAt: DateTime @default(now())
- updatedAt: DateTime @updatedAt
- @@unique([userId, productId]) (one review per user per product)
- @@index([productId])
- @@index([userId])
- @@index([status])
- @@index([productId, status]) (for fetching approved reviews per product)
- @@map("reviews")

**8. Add relation fields on Product model** for the new models:
- reviews: Review[]
- wishlistItems: WishlistItem[]

(These were noted as "Plan 03" placeholders in Plan 01.)
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx prisma validate --schema=packages/db/prisma/schema.prisma</automated>
  </verify>
  <done>User model has SUPER_ADMIN role, profile fields, and relations to Address/Review/Wishlist. Address supports multiple per user with default flag. Wishlist/WishlistItem allow product saving with notification preferences. Review has rating, moderation status, verified purchase badge. All foreign keys indexed with explicit onDelete actions.</done>
</task>

<task type="auto">
  <name>Task 2: Add Coupon, Promotion, Shipping, Warehouse, and Inventory models</name>
  <files>packages/db/prisma/schema.prisma</files>
  <action>
Continue modifying `packages/db/prisma/schema.prisma`:

**1. Add DiscountType enum:** PERCENTAGE, FIXED_AMOUNT, FREE_SHIPPING

**2. Create Coupon model** (user-entered codes):
- id: String @id @default(cuid())
- code: String @unique
- description: String?
- discountType: DiscountType
- discountValue: Int (percentage as whole number e.g. 15 for 15%, or cents for fixed)
- minOrderAmount: Int? (cents, minimum cart total to apply)
- maxDiscountAmount: Int? (cents, cap for percentage discounts)
- usageLimit: Int? (null = unlimited)
- usageCount: Int @default(0)
- perCustomerLimit: Int @default(1)
- applicableProductIds: String[] (empty = all products)
- applicableCategoryIds: String[] (empty = all categories)
- startsAt: DateTime
- expiresAt: DateTime?
- isActive: Boolean @default(true)
- createdAt: DateTime @default(now())
- updatedAt: DateTime @updatedAt
- @@index([code])
- @@index([isActive, startsAt, expiresAt])
- @@map("coupons")

**3. Add PromotionType enum:** BOGO, TIERED_PRICING, FLASH_SALE, AUTOMATIC_DISCOUNT

**4. Create Promotion model** (automatic rule-based):
- id: String @id @default(cuid())
- name: String
- description: String?
- type: PromotionType
- discountType: DiscountType
- discountValue: Int
- conditions: Json @default("{}") (JSONB for flexible conditions: minQuantity, minAmount, buyQuantity, getQuantity, etc.)
- applicableProductIds: String[]
- applicableCategoryIds: String[]
- stackable: Boolean @default(false)
- priority: Int @default(0) (higher = applied first)
- usageLimit: Int?
- usageCount: Int @default(0)
- startsAt: DateTime
- expiresAt: DateTime?
- isActive: Boolean @default(true)
- createdAt: DateTime @default(now())
- updatedAt: DateTime @updatedAt
- @@index([isActive, startsAt, expiresAt])
- @@index([type])
- @@map("promotions")

**5. Create ShippingZone model:**
- id: String @id @default(cuid())
- name: String (e.g., "Continental US", "Europe")
- countries: String[] (ISO country codes)
- states: String[] (optional, for zone subdivision)
- isActive: Boolean @default(true)
- methods: ShippingMethod[]
- freeShippingThreshold: Int? (cents, null = no free shipping)
- createdAt: DateTime @default(now())
- updatedAt: DateTime @updatedAt
- @@map("shipping_zones")

**6. Add ShippingRateType enum:** FLAT_RATE, WEIGHT_BASED, PRICE_BASED

**7. Create ShippingMethod model:**
- id: String @id @default(cuid())
- name: String (e.g., "Standard Shipping", "Express")
- description: String?
- zoneId: String
- zone: ShippingZone @relation(fields: [zoneId], references: [id], onDelete: Cascade)
- rateType: ShippingRateType
- flatRate: Int? (cents, for FLAT_RATE)
- weightRate: Int? (cents per kg, for WEIGHT_BASED)
- minWeight: Float? (kg)
- maxWeight: Float? (kg)
- priceThresholds: Json? (JSONB for PRICE_BASED: [{minPrice: 0, maxPrice: 5000, rate: 999}])
- estimatedDaysMin: Int? (e.g., 3)
- estimatedDaysMax: Int? (e.g., 7)
- isActive: Boolean @default(true)
- position: Int @default(0)
- createdAt: DateTime @default(now())
- updatedAt: DateTime @updatedAt
- @@index([zoneId])
- @@map("shipping_methods")

**8. Create Warehouse model:**
- id: String @id @default(cuid())
- name: String
- code: String @unique (short identifier)
- address: String
- city: String
- state: String
- country: String
- zipCode: String
- latitude: Float?
- longitude: Float?
- priority: Int @default(0) (higher = preferred for allocation)
- isActive: Boolean @default(true)
- inventoryItems: InventoryItem[]
- createdAt: DateTime @default(now())
- updatedAt: DateTime @updatedAt
- @@map("warehouses")

**9. Add StockMovementReason enum:** SALE, RETURN, MANUAL_ADJUSTMENT, DAMAGE, RESTOCK, RESERVATION, RESERVATION_RELEASE

**10. Create InventoryItem model:**
- id: String @id @default(cuid())
- variantId: String
- variant: ProductVariant @relation(fields: [variantId], references: [id], onDelete: Cascade)
- warehouseId: String
- warehouse: Warehouse @relation(fields: [warehouseId], references: [id], onDelete: Cascade)
- quantity: Int @default(0)
- reserved: Int @default(0) (held during checkout)
- lowStockThreshold: Int @default(5)
- @@unique([variantId, warehouseId]) (one inventory record per variant per warehouse)
- @@index([variantId])
- @@index([warehouseId])
- @@map("inventory_items")

**11. Create StockMovement model** (audit trail):
- id: String @id @default(cuid())
- inventoryItemId: String
- inventoryItem: InventoryItem @relation(fields: [inventoryItemId], references: [id], onDelete: Cascade)
- quantity: Int (positive for additions, negative for removals)
- reason: StockMovementReason
- reference: String? (order ID, adjustment ID, etc.)
- note: String?
- createdAt: DateTime @default(now())
- @@index([inventoryItemId])
- @@index([createdAt])
- @@map("stock_movements")

**12. Add relation fields on ProductVariant** for inventory:
- inventoryItems: InventoryItem[]

**13. Add relation field on InventoryItem** for stock movements:
- movements: StockMovement[]

**14. Final audit of ALL models in the schema:**
- Verify every foreign key field has @@index
- Verify every relation has explicit onDelete (Cascade, Restrict, or SetNull as appropriate)
- Verify all models have @@map with snake_case
- Verify all id fields use @default(cuid())
- Verify all price/amount fields use Int (not Float)
- Verify timestamps (createdAt @default(now()), updatedAt @updatedAt) on all main entities
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx prisma validate --schema=packages/db/prisma/schema.prisma</automated>
  </verify>
  <done>Coupon and Promotion models exist with separate discount logic, date ranges, and usage limits. ShippingZone/ShippingMethod support geographic zones with multiple rate types. Warehouse/InventoryItem support multi-warehouse stock tracking with reservation fields. StockMovement provides audit trail. All models across entire schema have explicit indexes on foreign keys, referential actions on all relations, snake_case table names, cuid() IDs, and Int prices.</done>
</task>

</tasks>

<verification>
1. `npx prisma validate` passes with no errors
2. Role enum has CUSTOMER, ADMIN, SUPER_ADMIN
3. User model has avatar, phone, isActive, lastLoginAt, relations to Address/Review/Wishlist
4. Address has isDefault flag and belongs to User with Cascade delete
5. Review has rating Int, ReviewStatus enum, isVerifiedPurchase, @@unique([userId, productId])
6. Coupon has code @unique, discountType, usage limits, date range
7. Promotion has type, conditions as Json, stackable flag
8. ShippingZone has countries array, freeShippingThreshold
9. ShippingMethod has rateType enum with flat/weight/price options
10. Warehouse has code @unique, lat/long, priority
11. InventoryItem has @@unique([variantId, warehouseId]), reserved field
12. StockMovement tracks quantity changes with reason enum
13. Every foreign key has @@index
14. Every relation has explicit onDelete
</verification>

<success_criteria>
- Complete Prisma schema validates without errors
- User model has 3 roles including SUPER_ADMIN
- All commerce entities defined (Address, Wishlist, Review, Coupon, Promotion, Shipping, Warehouse, Inventory)
- All foreign keys indexed
- All relations have explicit referential actions
- All prices as Int (cents)
- All IDs use cuid()
- All tables use @@map with snake_case
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-schema-design/01-03-SUMMARY.md`
</output>
