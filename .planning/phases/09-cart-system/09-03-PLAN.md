---
phase: 09-cart-system
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - apps/client/src/components/cart/cart-header-button.tsx
  - apps/client/src/components/cart/mini-cart-drawer.tsx
  - apps/client/src/components/cart/cart-item-row.tsx
  - apps/client/src/app/layout.tsx
autonomous: true
requirements:
  - CART-09

must_haves:
  truths:
    - "Cart icon in header shows item count badge (hydration-safe, renders 0 until mounted)"
    - "Clicking cart icon opens slide-out drawer from right edge with overlay dimming page"
    - "Mini cart drawer shows cart items with thumbnail, variant info, quantity stepper, line total, remove button"
    - "Quantity stepper allows editing directly in mini cart with +/- buttons"
    - "Mini cart footer shows subtotal and Checkout + View Cart buttons"
  artifacts:
    - path: "apps/client/src/components/cart/cart-header-button.tsx"
      provides: "Cart icon with count badge and useCartSync mount point"
      exports: ["CartHeaderButton"]
    - path: "apps/client/src/components/cart/mini-cart-drawer.tsx"
      provides: "Slide-out drawer with item list and footer"
      exports: ["MiniCartDrawer"]
    - path: "apps/client/src/components/cart/cart-item-row.tsx"
      provides: "Individual cart item with stepper and remove"
      exports: ["CartItemRow"]
    - path: "apps/client/src/app/layout.tsx"
      provides: "CartHeaderButton wired into header"
      contains: "CartHeaderButton"
  key_links:
    - from: "apps/client/src/components/cart/cart-header-button.tsx"
      to: "apps/client/src/stores/cart-store.ts"
      via: "useCartStore for badge count"
      pattern: "useCartStore"
    - from: "apps/client/src/components/cart/cart-header-button.tsx"
      to: "apps/client/src/hooks/use-cart-sync.ts"
      via: "useCartSync() call"
      pattern: "useCartSync"
    - from: "apps/client/src/components/cart/mini-cart-drawer.tsx"
      to: "apps/client/src/stores/cart-store.ts"
      via: "useCartStore for items and mutations"
      pattern: "useCartStore"
---

<objective>
Mini cart drawer accessible from header on all pages: cart icon with badge count, slide-out drawer with item list, quantity editing, and navigation buttons.

Purpose: Users can view and manage their cart from any page without navigating to the full cart page (CART-09).
Output: CartHeaderButton, MiniCartDrawer, CartItemRow components, wired into client layout.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-cart-system/09-CONTEXT.md
@.planning/phases/09-cart-system/09-RESEARCH.md
@.planning/phases/09-cart-system/09-02-SUMMARY.md

@apps/client/src/app/layout.tsx
@apps/client/src/components/filters/filter-drawer.tsx
@apps/client/src/stores/cart-store.ts
@apps/client/src/hooks/use-cart-sync.ts

<interfaces>
From apps/client/src/stores/cart-store.ts (extended in Plan 02):
```typescript
interface CartStore {
  items: CartItem[];
  couponCode: string | null;
  discountAmount: number;
  addItem: (item: CartItem) => void;
  removeItem: (productId: string, variantId: string) => void;
  updateQuantity: (productId: string, variantId: string, quantity: number) => void;
  clearCart: () => void;
  totalItems: () => number;
  applyCoupon: (code: string, discount: number) => void;
  removeCoupon: () => void;
  subtotal: () => number;
  setItems: (items: CartItem[]) => void;
}
```

From apps/client/src/hooks/use-cart-sync.ts (created in Plan 02):
```typescript
export function useCartSync(): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CartItemRow and MiniCartDrawer components</name>
  <files>apps/client/src/components/cart/cart-item-row.tsx, apps/client/src/components/cart/mini-cart-drawer.tsx</files>
  <action>
1. Create `cart-item-row.tsx`:
   - 'use client' directive
   - Props: { item: CartItem, onUpdateQuantity: (qty: number) => void, onRemove: () => void }
   - Layout: flex row with:
     - Product thumbnail (48x48 next/image with item.imageUrl, fallback placeholder if empty)
     - Column with: item.name (font-medium, truncate), variant info from item.attributes rendered as "Size: M, Color: Red" (text-sm text-gray-500), item.sku (text-xs text-gray-400)
     - Quantity stepper: minus button (disabled at qty=1), count display, plus button. On click, call onUpdateQuantity with new quantity. Use min 1.
     - Line total: formatPrice(item.price * item.quantity) aligned right
     - Remove button: trash icon (SVG inline or text "x"), calls onRemove
   - Import formatPrice from existing helper

2. Create `mini-cart-drawer.tsx`:
   - 'use client' directive
   - Internal state: open (boolean)
   - Props: none (reads from useCartStore directly)
   - Get items, subtotal, totalItems from useCartStore
   - Get updateQuantity, removeItem from useCartStore
   - Lock body scroll when open (useEffect sets document.body.style.overflow)
   - Render structure (mirrors FilterDrawer from Phase 6):
     - Overlay: fixed inset-0 bg-black/50 z-40, onClick closes drawer
     - Drawer panel: fixed inset-y-0 right-0 z-50, w-full sm:max-w-md, bg-white, flex flex-col
     - Add CSS transition: translate-x-full when closed, translate-x-0 when open (use Tailwind transition-transform duration-300)
     - Header: "Shopping Cart (N)" title + close X button
     - Scrollable body (flex-1 overflow-y-auto): map items to CartItemRow components. Key by `${item.productId}-${item.variantId || 'no-variant'}`
     - Empty state: "Your cart is empty" message with a "Continue Shopping" link to "/"
     - Footer (border-t, p-4): subtotal line using formatPrice, "View Cart" link (href="/cart", text button style), "Checkout" link (href="/checkout", primary button style, bg-black text-white)
   - Export open/close controls: export a function or use an event pattern. Simplest: export the component and let CartHeaderButton control `open` via a shared state or by passing a ref. Use a simple approach: MiniCartDrawer accepts `open` and `onClose` props.

   Revised approach: MiniCartDrawer as controlled component:
   - Props: { open: boolean, onClose: () => void }
   - Renders overlay + panel only when open is true
   - Handles body scroll lock internally
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>CartItemRow renders item with stepper and remove, MiniCartDrawer renders slide-out with items, footer, empty state</done>
</task>

<task type="auto">
  <name>Task 2: CartHeaderButton and layout integration</name>
  <files>apps/client/src/components/cart/cart-header-button.tsx, apps/client/src/app/layout.tsx</files>
  <action>
1. Create `cart-header-button.tsx`:
   - 'use client' directive
   - Internal state: drawerOpen (boolean), mounted (boolean)
   - Call useCartSync() to mount sync hook (same placement strategy as useWishlistSync in WishlistHeaderBadge)
   - Get totalItems from useCartStore
   - Hydration-safe badge: useEffect sets mounted=true, show "0" until mounted, then real totalItems() count. Follow WishlistHeaderBadge pattern exactly.
   - Render: button with cart SVG icon (shopping bag outline), badge count (absolute positioned top-right corner, bg-black text-white text-xs rounded-full min-w-[18px] h-[18px] flex items-center justify-center). onClick toggles drawerOpen.
   - Render MiniCartDrawer with open={drawerOpen} and onClose={() => setDrawerOpen(false)}

2. Wire into `apps/client/src/app/layout.tsx`:
   - Import CartHeaderButton from '@/components/cart/cart-header-button'
   - Replace the existing cart link/icon in the header with <CartHeaderButton />
   - If there is no existing cart link, add CartHeaderButton in the header area alongside the existing wishlist badge and other header items
   - CartHeaderButton is a client component so it can be placed directly in the server layout
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>CartHeaderButton shows hydration-safe badge, opens MiniCartDrawer on click, wired into root layout header</done>
</task>

</tasks>

<verification>
npx tsc --noEmit --project apps/client/tsconfig.json
All cart components compile. CartHeaderButton in layout. MiniCartDrawer slides from right.
</verification>

<success_criteria>
- CartHeaderButton shows cart icon with hydration-safe count badge
- Clicking icon opens MiniCartDrawer from right edge with overlay
- MiniCartDrawer shows items with CartItemRow (thumbnail, variant info, qty stepper, remove)
- Footer has subtotal, View Cart, and Checkout buttons
- Empty state shows "Your cart is empty" message
- useCartSync is mounted via CartHeaderButton
- Root layout.tsx includes CartHeaderButton in header
</success_criteria>

<output>
After completion, create `.planning/phases/09-cart-system/09-03-SUMMARY.md`
</output>
