---
phase: 09-cart-system
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/setup.ts
  - tests/fixtures/cart.fixtures.ts
  - tests/cart/cart-service.test.ts
  - tests/cart/cart-store.test.ts
  - tests/cart/coupon-validation.test.ts
  - tests/cart/stock-validation.test.ts
autonomous: true
requirements:
  - CART-01
  - CART-02
  - CART-03
  - CART-04
  - CART-05
  - CART-06
  - CART-07
  - CART-08

must_haves:
  truths:
    - "Coupon Prisma mock is available in test setup for cart tests"
    - "Cart test fixtures provide reusable mock data for all cart test files"
    - "Test stubs exist for all 9 CART requirements with it.todo() markers"
  artifacts:
    - path: "tests/setup.ts"
      provides: "Coupon Prisma mock added"
      contains: "coupon"
    - path: "tests/fixtures/cart.fixtures.ts"
      provides: "Cart and coupon test fixtures"
      exports: ["mockCartItem", "mockCoupon"]
    - path: "tests/cart/cart-service.test.ts"
      provides: "Service test stubs for CART-01, CART-03, CART-05"
    - path: "tests/cart/coupon-validation.test.ts"
      provides: "Coupon validation test stubs for CART-07"
    - path: "tests/cart/stock-validation.test.ts"
      provides: "Stock validation test stubs for CART-08"
  key_links: []
---

<objective>
Wave 0: Cart test infrastructure — extend Prisma mocks with coupon model, create cart fixtures, and stub all cart test files.

Purpose: Every subsequent plan can run `npx vitest run tests/cart/` immediately to verify work against defined behaviors.
Output: Test setup extended, fixtures created, 4 test files with it.todo() stubs covering all CART requirements.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cart-system/09-CONTEXT.md
@.planning/phases/09-cart-system/09-RESEARCH.md

@tests/setup.ts
@tests/fixtures/wishlist.fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Prisma mocks and create cart fixtures</name>
  <files>tests/setup.ts, tests/fixtures/cart.fixtures.ts</files>
  <action>
1. In `tests/setup.ts`, add a `coupon` mock to the `prismaMock` object (inside `vi.hoisted`) with methods: findUnique, findMany, findFirst, create, update, delete, count. Follow the exact same pattern as existing model mocks (e.g., `wishlist`).

2. Create `tests/fixtures/cart.fixtures.ts` with:
   - `mockCartItem`: A CartItem matching the `@repo/types` CartItem interface — productId: 'prod-1', variantId: 'var-1', name: 'Test Product', price: 1999, quantity: 2, imageUrl: 'https://example.com/img.jpg', sku: 'TEST-001', attributes: { size: 'M', color: 'Red' }
   - `mockCartItemNoVariant`: Same but variantId: undefined, for simple products
   - `mockCoupon`: A Prisma Coupon fixture — id: 'coupon-1', code: 'SAVE20', discountType: 'PERCENTAGE', discountValue: 20, minOrderAmount: 5000 (cents), maxDiscountAmount: 2000 (cents), usageLimit: 100, usageCount: 5, perCustomerLimit: 1, applicableProductIds: [], applicableCategoryIds: [], startsAt: new Date('2024-01-01'), expiresAt: new Date('2030-12-31'), isActive: true
   - `mockExpiredCoupon`: Same but expiresAt: new Date('2024-01-01'), isActive: true
   - `mockInactiveCoupon`: Same but isActive: false
   - `mockFixedCoupon`: discountType: 'FIXED_AMOUNT', discountValue: 500 (cents = $5 off)
   - `mockFreeShippingCoupon`: discountType: 'FREE_SHIPPING', discountValue: 0
   - `mockCart` (MongoDB shape): { _id: 'cart-1', userId: 'user-1', items: [mockCartItem], couponCode: null, expiresAt: null }
   - `mockGuestCart`: { _id: 'cart-2', sessionId: 'session-abc', items: [mockCartItem], couponCode: null, expiresAt: new Date('2024-06-01') }
  </action>
  <verify>
    <automated>npx vitest run tests/fixtures/cart.fixtures.ts --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>setup.ts has coupon mock, cart.fixtures.ts exports all fixtures without TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Create cart test stub files with it.todo() for all requirements</name>
  <files>tests/cart/cart-service.test.ts, tests/cart/cart-store.test.ts, tests/cart/coupon-validation.test.ts, tests/cart/stock-validation.test.ts</files>
  <action>
Create 4 test files using `it.todo()` stubs following the established pattern (see tests/wishlist/).

1. `tests/cart/cart-service.test.ts` — CartService unit tests:
   - describe('CartService')
     - describe('getOrCreateCart') — it.todo('creates new cart if none exists for userId')
     - describe('addItem') — it.todo('adds item to cart'), it.todo('increments quantity if item already exists'), it.todo('stores price snapshot from product at add-time')
     - describe('updateQuantity') — it.todo('updates item quantity'), it.todo('clamps quantity to available stock')
     - describe('removeItem') — it.todo('removes item by productId + variantId')
     - describe('clearCart') — it.todo('removes all items from cart')
     - describe('mergeGuestCart') — it.todo('sums quantities for matching items capped at stock'), it.todo('adds new items from guest cart'), it.todo('deletes guest cart document after merge'), it.todo('caps merged quantity at available stock')

2. `tests/cart/cart-store.test.ts` — Zustand store tests:
   - describe('useCartStore')
     - it.todo('adds item to cart')
     - it.todo('increments quantity for existing item')
     - it.todo('removes item from cart')
     - it.todo('updates item quantity')
     - it.todo('clears all items')
     - it.todo('computes totalItems correctly')
     - it.todo('stores couponCode and discountAmount')
     - it.todo('applyCoupon sets coupon state')
     - it.todo('removeCoupon clears coupon state')
     - it.todo('subtotal computes sum of price * quantity')

3. `tests/cart/coupon-validation.test.ts` — Coupon validation:
   - describe('CouponValidation')
     - it.todo('returns valid=true for active coupon with valid dates')
     - it.todo('returns INVALID_CODE for unknown code')
     - it.todo('returns INVALID_CODE for inactive coupon')
     - it.todo('returns CODE_EXPIRED for expired coupon')
     - it.todo('returns NOT_STARTED for coupon with future startsAt')
     - it.todo('returns USAGE_LIMIT for fully used coupon')
     - it.todo('returns MINIMUM_ORDER when subtotal below minOrderAmount')
     - it.todo('calculates percentage discount correctly')
     - it.todo('caps percentage discount at maxDiscountAmount')
     - it.todo('calculates fixed amount discount correctly')

4. `tests/cart/stock-validation.test.ts` — Stock validation:
   - describe('StockValidation')
     - it.todo('returns in_stock for items with sufficient inventory')
     - it.todo('returns low_stock with count for items near threshold')
     - it.todo('returns out_of_stock for items with zero available')
     - it.todo('validates each cart item independently')

Each file should import from `tests/fixtures/cart.fixtures.ts` and `tests/setup.ts` (prismaMock). Use `vi.mock('@repo/db', ...)` pattern with `vi.hoisted()` as established in existing test files.
  </action>
  <verify>
    <automated>npx vitest run tests/cart/ --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>All 4 test files exist with it.todo() stubs, vitest finds them and reports todo count, zero failures</done>
</task>

</tasks>

<verification>
npx vitest run tests/cart/ --passWithNoTests
All test files discovered, all stubs show as todo, zero failures.
</verification>

<success_criteria>
- tests/setup.ts contains coupon Prisma mock
- tests/fixtures/cart.fixtures.ts exports all cart and coupon fixtures
- 4 test files exist under tests/cart/ with it.todo() stubs covering CART-01 through CART-08
- `npx vitest run tests/cart/` passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/09-cart-system/09-00-SUMMARY.md`
</output>
