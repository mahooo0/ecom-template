---
phase: 09-cart-system
plan: 04
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - apps/client/src/components/cart/coupon-section.tsx
  - apps/client/src/components/cart/price-summary.tsx
  - apps/client/src/components/cart/stock-warning.tsx
  - apps/client/src/app/cart/page.tsx
  - apps/client/src/app/cart/cart-page-client.tsx
autonomous: true
requirements:
  - CART-05
  - CART-06
  - CART-07
  - CART-08
  - CART-09

must_haves:
  truths:
    - "Cart page shows all items with quantity editing, remove, and clear cart functionality"
    - "Cart page displays subtotal, discount line (if coupon), and total"
    - "Coupon section expands to reveal input, shows validation errors inline, shows applied coupon as removable chip"
    - "Out-of-stock items are visually flagged and block checkout"
    - "Low stock items show warning with available count"
    - "Tax and shipping show 'Calculated at checkout'"
  artifacts:
    - path: "apps/client/src/components/cart/coupon-section.tsx"
      provides: "Expandable promo code input with validation feedback"
      exports: ["CouponSection"]
    - path: "apps/client/src/components/cart/price-summary.tsx"
      provides: "Subtotal, discount, tax, shipping, total breakdown"
      exports: ["PriceSummary"]
    - path: "apps/client/src/components/cart/stock-warning.tsx"
      provides: "Per-item stock status badge"
      exports: ["StockWarning"]
    - path: "apps/client/src/app/cart/page.tsx"
      provides: "Server Component wrapper with metadata"
    - path: "apps/client/src/app/cart/cart-page-client.tsx"
      provides: "Client island with full cart UI"
      exports: ["CartPageClient"]
  key_links:
    - from: "apps/client/src/components/cart/coupon-section.tsx"
      to: "/api/cart/coupon"
      via: "api.cart.applyCoupon and api.cart.removeCoupon"
      pattern: "applyCoupon|removeCoupon"
    - from: "apps/client/src/app/cart/cart-page-client.tsx"
      to: "apps/client/src/stores/cart-store.ts"
      via: "useCartStore for items, mutations, coupon state"
      pattern: "useCartStore"
    - from: "apps/client/src/app/cart/cart-page-client.tsx"
      to: "/api/cart/stock-validation"
      via: "api.cart.validateStock on mount"
      pattern: "validateStock"
---

<objective>
Full cart page with item management, coupon application with validation feedback, price summary breakdown, and stock availability warnings.

Purpose: Users can review their cart, apply coupons, see price breakdown, and identify stock issues before proceeding to checkout.
Output: Cart page route with all cart management components.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-cart-system/09-CONTEXT.md
@.planning/phases/09-cart-system/09-RESEARCH.md
@.planning/phases/09-cart-system/09-02-SUMMARY.md

@apps/client/src/stores/cart-store.ts
@apps/client/src/lib/api.ts

<interfaces>
From apps/client/src/stores/cart-store.ts (Plan 02):
```typescript
interface CartStore {
  items: CartItem[];
  couponCode: string | null;
  discountAmount: number;
  addItem: (item: CartItem) => void;
  removeItem: (productId: string, variantId: string) => void;
  updateQuantity: (productId: string, variantId: string, quantity: number) => void;
  clearCart: () => void;
  totalItems: () => number;
  applyCoupon: (code: string, discount: number) => void;
  removeCoupon: () => void;
  subtotal: () => number;
  setItems: (items: CartItem[]) => void;
}
```

From apps/server/src/modules/cart/cart.service.ts (Plan 01):
```typescript
interface CouponValidationResult {
  valid: boolean;
  discountAmount?: number;
  errorReason?: 'INVALID_CODE' | 'CODE_EXPIRED' | 'MINIMUM_ORDER' | 'USAGE_LIMIT' | 'NOT_STARTED';
  errorMessage?: string;
}

interface StockValidationResult {
  productId: string;
  variantId?: string;
  requestedQty: number;
  availableQty: number;
  status: 'in_stock' | 'low_stock' | 'out_of_stock';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CouponSection, PriceSummary, and StockWarning components</name>
  <files>apps/client/src/components/cart/coupon-section.tsx, apps/client/src/components/cart/price-summary.tsx, apps/client/src/components/cart/stock-warning.tsx</files>
  <action>
1. Create `coupon-section.tsx`:
   - 'use client' directive
   - Props: { onCouponApplied: (code: string, discount: number) => void, onCouponRemoved: () => void, currentCode: string | null, discountAmount: number }
   - Internal state: expanded (boolean, default false), inputValue (string), error (string | null), loading (boolean)
   - Per locked decision: "Have a promo code?" clickable text that toggles expanded
   - When expanded: text input + "Apply" button. Input has red border and error text below when error is set.
   - On Apply click: set loading=true, call api.cart.applyCoupon(inputValue, token) via useAuth(). On success: call onCouponApplied(code, discountAmount), clear input, collapse. On 400 error: parse errorMessage from response, set error state.
   - For guest users (no token): call api.cart.validateCoupon to check validity, then update local Zustand store directly without server persist.
   - When currentCode is set: show removable pill/chip with code text and X button. Green background. "SAVE20 x". On X click: call onCouponRemoved.
   - Per locked decision: one coupon at a time, applying new replaces previous.

2. Create `price-summary.tsx`:
   - 'use client' directive
   - Props: { subtotal: number, discountAmount: number, couponCode: string | null }
   - Renders a card/section with:
     - "Subtotal" line: formatPrice(subtotal) aligned right
     - "Discount" line (only if couponCode): "-" + formatPrice(discountAmount) in green, aligned right. Green "You save" text per locked decision.
     - "Shipping" line: "Calculated at checkout" in gray (per locked decision)
     - "Tax" line: "Calculated at checkout" in gray (per locked decision)
     - Horizontal divider
     - "Total" line: formatPrice(subtotal - discountAmount) in bold, aligned right
   - Use formatPrice helper from existing client utils

3. Create `stock-warning.tsx`:
   - 'use client' directive
   - Props: { status: 'in_stock' | 'low_stock' | 'out_of_stock', availableQty: number }
   - Renders:
     - in_stock: nothing (or optional green "In Stock" text)
     - low_stock: yellow/amber badge "Only {availableQty} left" (text-amber-600 bg-amber-50)
     - out_of_stock: red badge "Out of Stock" (text-red-600 bg-red-50)
   - Small text size (text-xs), inline badge style
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>CouponSection shows expandable input with validation, PriceSummary shows price breakdown, StockWarning shows status badges</done>
</task>

<task type="auto">
  <name>Task 2: Cart page with Server Component wrapper and Client island</name>
  <files>apps/client/src/app/cart/page.tsx, apps/client/src/app/cart/cart-page-client.tsx</files>
  <action>
1. Create `apps/client/src/app/cart/page.tsx`:
   - Server Component (no 'use client')
   - Export metadata: { title: 'Shopping Cart', description: 'Review and manage your shopping cart' }
   - Render CartPageClient as the sole child
   - Follow the same pattern as wishlist page (thin server wrapper)

2. Create `apps/client/src/app/cart/cart-page-client.tsx`:
   - 'use client' directive
   - Read from useCartStore: items, couponCode, discountAmount, updateQuantity, removeItem, clearCart, applyCoupon, removeCoupon, subtotal
   - Use useAuth() from @clerk/nextjs for token
   - State: stockResults (StockValidationResult[]), stockLoading (boolean), mounted (boolean)
   - Hydration safety: useEffect sets mounted=true, render skeleton/empty until mounted
   - On mount (and when items change): if user is authenticated and items.length > 0, call api.cart.validateStock(token) and set stockResults. Also validate on quantity change. Per research: validate on both page load and quantity change.
   - Layout (responsive):
     - Page heading "Shopping Cart" (h1) with item count, plus "Clear Cart" button (text-red-600, only if items.length > 0)
     - Two-column on lg: left column (2/3) = item list, right column (1/3) = order summary
     - Single column on mobile: items first, then summary
   - Item list: map items to CartItemRow (reuse from Plan 03) with StockWarning for each item. Look up stock status from stockResults by productId+variantId. Key by `${item.productId}-${item.variantId || 'no-variant'}`.
   - Order summary (right column, sticky top-4):
     - CouponSection with onCouponApplied={(code, discount) => applyCoupon(code, discount)}, onCouponRemoved={() => removeCoupon()}, currentCode={couponCode}, discountAmount={discountAmount}
     - PriceSummary with subtotal={subtotal()}, discountAmount={discountAmount}, couponCode={couponCode}
     - "Proceed to Checkout" button: full width, bg-black text-white py-3, links to /checkout. DISABLED if any item has status 'out_of_stock' in stockResults. Show tooltip/text "Remove out-of-stock items to continue" when disabled.
     - "Continue Shopping" link below, text style, links to "/"
   - Empty cart state: centered message "Your cart is empty", shopping bag icon, "Continue Shopping" link/button
   - For authenticated users: cart mutations (updateQuantity, removeItem, clearCart) should also fire background API calls to sync with server. Use fire-and-forget pattern (call api.cart.updateQuantity etc. in addition to Zustand update). This provides optimistic UI with server sync.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Cart page at /cart shows items with stock warnings, coupon section, price summary, checkout button disabled when out-of-stock items present</done>
</task>

</tasks>

<verification>
npx tsc --noEmit --project apps/client/tsconfig.json
Cart page compiles. CouponSection validates and shows errors. PriceSummary shows breakdown. StockWarning badges render.
</verification>

<success_criteria>
- /cart route renders cart page with all items from Zustand store
- Each item shows CartItemRow with StockWarning badge
- CouponSection: expandable "Have a promo code?" with input, Apply, error display, and removable chip
- PriceSummary: subtotal, discount (green, with "You save"), shipping/tax "Calculated at checkout", total
- Out-of-stock items flagged red, checkout button disabled when present
- Clear Cart button removes all items
- Empty state shows when cart has no items
- Responsive: 2-column on lg, single column on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/09-cart-system/09-04-SUMMARY.md`
</output>
