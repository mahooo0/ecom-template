---
phase: 04-categories-navigation
plan: 04
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - apps/admin/src/app/dashboard/collections/page.tsx
  - apps/admin/src/app/dashboard/collections/collection-form.tsx
  - apps/admin/src/app/dashboard/collections/product-selector.tsx
  - apps/admin/src/app/dashboard/brands/page.tsx
  - apps/admin/src/app/dashboard/brands/brand-form.tsx
  - apps/admin/src/app/dashboard/tags/page.tsx
  - apps/admin/src/app/dashboard/layout.tsx
autonomous: true
requirements:
  - CAT-03

must_haves:
  truths:
    - "Admin can view, create, edit, and delete collections"
    - "Admin can add and remove products from collections"
    - "Admin can view, create, edit, and delete brands"
    - "Admin can view, create, and delete tags"
  artifacts:
    - path: "apps/admin/src/app/dashboard/collections/page.tsx"
      provides: "Collections list and management page"
      min_lines: 30
    - path: "apps/admin/src/app/dashboard/brands/page.tsx"
      provides: "Brands list and management page"
      min_lines: 30
    - path: "apps/admin/src/app/dashboard/tags/page.tsx"
      provides: "Tags list and management page"
      min_lines: 20
  key_links:
    - from: "apps/admin/src/app/dashboard/collections/page.tsx"
      to: "/api/collections"
      via: "api.collections methods"
      pattern: "api\\.collections"
    - from: "apps/admin/src/app/dashboard/brands/page.tsx"
      to: "/api/brands"
      via: "api.brands methods"
      pattern: "api\\.brands"
---

<objective>
Build admin CRUD pages for collections (with product assignment), brands, and tags.

Purpose: Admin needs UI to manage these product organization entities. Collections allow curated product groups, brands organize by manufacturer, tags provide flexible labeling.
Output: Three admin pages with data tables and forms for collections, brands, and tags.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-categories-navigation/04-02-SUMMARY.md

@apps/admin/src/lib/api.ts
@apps/admin/src/app/dashboard/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build collections management page with product assignment</name>
  <files>
    apps/admin/src/app/dashboard/collections/page.tsx
    apps/admin/src/app/dashboard/collections/collection-form.tsx
    apps/admin/src/app/dashboard/collections/product-selector.tsx
  </files>
  <action>
**apps/admin/src/app/dashboard/collections/page.tsx (Server Component):**

Fetch collections from API with auth token. Display page with:
- Title "Collections" with "Add Collection" button
- Table listing collections: Name, Slug, Products count, Active status (green/red badge), Actions (edit, delete)
- Modal/panel for create/edit form triggered by button/action clicks

Use searchParams for managing state: `?action=create`, `?action=edit&id=xxx`, `?action=products&id=xxx`

When `?action=products&id=xxx`, show ProductSelector component to manage collection products.

**apps/admin/src/app/dashboard/collections/collection-form.tsx ('use client'):**

Props: `{ collection?: Collection; onSuccess: () => void }`

Form fields (react-hook-form with Zod):
- Name (text, required)
- Description (textarea, optional)
- Image URL (text, optional)
- Active (checkbox, default true)
- Slug (text, auto-generated, editable)

On submit: call api.collections.create or api.collections.update with token.

**apps/admin/src/app/dashboard/collections/product-selector.tsx ('use client'):**

Props: `{ collectionId: string; products: Product[] }`

Shows:
1. Current products in collection as a list with remove buttons
2. Search input to find products to add (calls api.products.getAll with search param)
3. Search results dropdown with "Add" button next to each product

On add: call api.collections.addProduct(collectionId, productId, token), refresh
On remove: call api.collections.removeProduct(collectionId, productId, token), refresh

Style with Tailwind: clean list, search input with dropdown results.
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx tsc --noEmit --project apps/admin/tsconfig.json 2>&1 | tail -20</automated>
  </verify>
  <done>Collections page shows table of collections, form creates/edits collections, product selector manages collection membership. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Build brands and tags management pages, update admin navigation</name>
  <files>
    apps/admin/src/app/dashboard/brands/page.tsx
    apps/admin/src/app/dashboard/brands/brand-form.tsx
    apps/admin/src/app/dashboard/tags/page.tsx
    apps/admin/src/app/dashboard/layout.tsx
  </files>
  <action>
**apps/admin/src/app/dashboard/brands/page.tsx (Server Component):**

Fetch brands from API with auth token. Display:
- Title "Brands" with "Add Brand" button
- Table: Name, Slug, Logo (thumbnail or placeholder), Website (link), Products count, Actions (edit, delete)
- Show brand-form when `?action=create` or `?action=edit&id=xxx`

**apps/admin/src/app/dashboard/brands/brand-form.tsx ('use client'):**

Props: `{ brand?: Brand; onSuccess: () => void }`

Form fields:
- Name (text, required)
- Description (textarea, optional)
- Logo URL (text, optional)
- Website (text, optional, URL validation)
- Slug (auto-generated, editable)

On submit: api.brands.create or api.brands.update with token.

**apps/admin/src/app/dashboard/tags/page.tsx:**

Simple page with:
- Title "Tags" with inline "Add Tag" form (just a text input + button)
- List/grid of tags as pills/badges with delete (X) button
- No edit needed for tags (just name + auto-slug)

Fetch tags from API. On add: api.tags.create({ name }, token). On delete: confirm, then api.tags.delete(id, token).

Style tags as rounded pill badges with the tag name and an X button.

**apps/admin/src/app/dashboard/layout.tsx:**

Update the admin sidebar/navigation to include links for:
- Collections -> `/dashboard/collections`
- Brands -> `/dashboard/brands`
- Tags -> `/dashboard/tags`

Group these under a "Catalog" section along with Products and Categories links. If the sidebar already has categories from Plan 03, add these alongside it. If Plan 03 hasn't run yet, add all catalog links: Products, Categories, Collections, Brands, Tags.
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx tsc --noEmit --project apps/admin/tsconfig.json 2>&1 | tail -20</automated>
  </verify>
  <done>Brands page shows CRUD table with form. Tags page shows pill list with inline add/delete. Admin navigation updated with catalog section links. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project apps/admin/tsconfig.json` compiles
- /dashboard/collections page with table, form, and product selector
- /dashboard/brands page with table and form
- /dashboard/tags page with inline add and delete
- Admin navigation has all catalog links
</verification>

<success_criteria>
- Admin can perform full CRUD on collections including adding/removing products
- Admin can perform full CRUD on brands
- Admin can create and delete tags
- All pages accessible from admin sidebar navigation
</success_criteria>

<output>
After completion, create `.planning/phases/04-categories-navigation/04-04-SUMMARY.md`
</output>
