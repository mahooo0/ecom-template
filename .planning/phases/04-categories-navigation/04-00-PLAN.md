---
phase: 04-categories-navigation
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/setup.ts
  - tests/categories/create.test.ts
  - tests/categories/move.test.ts
  - tests/categories/attributes.test.ts
  - tests/collections/crud.test.ts
  - tests/collections/products.test.ts
  - tests/brands-tags/crud.test.ts
  - tests/navigation/mega-menu.test.ts
  - tests/navigation/breadcrumbs.test.ts
  - tests/categories/page.test.ts
  - tests/categories/metadata.test.ts
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - CAT-04
  - CAT-05
  - CAT-06
  - CAT-07

must_haves:
  truths:
    - "Test setup mocks cover all category, brand, tag, collection Prisma models"
    - "Every phase requirement has at least one test stub file"
    - "Existing product tests still pass after setup changes"
  artifacts:
    - path: "tests/setup.ts"
      provides: "Extended Prisma mocks for category, categoryAttribute, brand, tag, collection, productTag, productCollection"
    - path: "tests/categories/create.test.ts"
      provides: "Test stubs for CAT-01 category creation"
    - path: "tests/categories/move.test.ts"
      provides: "Test stubs for CAT-01 tree move operations"
    - path: "tests/categories/attributes.test.ts"
      provides: "Test stubs for CAT-02 attribute management"
    - path: "tests/collections/crud.test.ts"
      provides: "Test stubs for CAT-03 collections"
    - path: "tests/brands-tags/crud.test.ts"
      provides: "Test stubs for CAT-03 brands and tags"
    - path: "tests/navigation/mega-menu.test.ts"
      provides: "Test stubs for CAT-04 mega menu"
    - path: "tests/navigation/breadcrumbs.test.ts"
      provides: "Test stubs for CAT-05 breadcrumbs"
    - path: "tests/categories/page.test.ts"
      provides: "Test stubs for CAT-06 category page"
    - path: "tests/categories/metadata.test.ts"
      provides: "Test stubs for CAT-07 SEO metadata"
  key_links: []
---

<objective>
Extend Vitest test infrastructure with category domain mocks and create test stub files for all Phase 4 requirements.

Purpose: Establish test coverage contracts before implementation begins (Wave 0 pattern from Phase 3).
Output: Extended setup.ts with category/brand/tag/collection mocks, 10 test stub files with it.todo() entries.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-categories-navigation/04-RESEARCH.md
@tests/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend test setup with category domain Prisma mocks</name>
  <files>tests/setup.ts</files>
  <action>
Add the following Prisma model mocks to the existing vi.mock('@repo/db') block in tests/setup.ts. Keep all existing mocks (product, productVariant, digitalMeta, weightedMeta, bundleItem, productTag, productCollection, shippingZone, shippingMethod) intact.

Add mocks for:
- `category`: findMany, findUnique, findFirst, create, update, delete, count (expand beyond the existing findUnique-only mock)
- `categoryAttribute`: findMany, findUnique, create, update, delete, count
- `brand`: findMany, findUnique, findFirst, create, update, delete, count (expand beyond existing findUnique-only mock)
- `tag`: findMany, findUnique, findFirst, create, update, delete, count
- `collection`: findMany, findUnique, findFirst, create, update, delete, count
- `productTag`: findMany, create, createMany, deleteMany (expand beyond existing deleteMany-only mock)
- `productCollection`: findMany, create, createMany, deleteMany, count (expand beyond existing deleteMany-only mock)

Update `$transaction` mock to support both array-style and callback-style transactions:
```typescript
$transaction: vi.fn((input) => {
  if (Array.isArray(input)) return Promise.all(input.map((fn: Function) => fn()));
  if (typeof input === 'function') return input(prisma);
  return Promise.resolve();
}),
```

Add mock fixtures after existing product fixtures:
- `mockCategory` with id: 'cat-1', name: 'Electronics', slug: 'electronics', path: '/electronics', depth: 0, position: 0, parentId: null
- `mockChildCategory` with id: 'cat-1-1', name: 'Phones', slug: 'phones', path: '/electronics/phones', depth: 1, position: 0, parentId: 'cat-1'
- `mockGrandchildCategory` with id: 'cat-1-1-1', name: 'Smartphones', slug: 'smartphones', path: '/electronics/phones/smartphones', depth: 2, position: 0, parentId: 'cat-1-1'
- `mockBrand` with id: 'brand-1', name: 'Samsung', slug: 'samsung'
- `mockTag` with id: 'tag-1', name: 'New Arrival', slug: 'new-arrival'
- `mockCollection` with id: 'col-1', name: 'Summer Sale', slug: 'summer-sale', isActive: true
- `mockCategoryAttribute` with id: 'attr-1', categoryId: 'cat-1', name: 'Screen Size', key: 'screen_size', type: 'SELECT', values: ['32 inch', '55 inch', '65 inch'], unit: 'inch', isFilterable: true, isRequired: false, position: 0

Export all new fixtures alongside existing exports.

IMPORTANT: The `$transaction` mock must reference the `prisma` object from within the mock factory. Use a pattern where the `prisma` object is built first then assigned to `$transaction`, or use a self-referential structure.
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && pnpm test --run --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <done>tests/setup.ts has mocks for all category domain models, mock fixtures exported, existing tests still pass</done>
</task>

<task type="auto">
  <name>Task 2: Create test stub files for all Phase 4 requirements</name>
  <files>
    tests/categories/create.test.ts
    tests/categories/move.test.ts
    tests/categories/attributes.test.ts
    tests/collections/crud.test.ts
    tests/collections/products.test.ts
    tests/brands-tags/crud.test.ts
    tests/navigation/mega-menu.test.ts
    tests/navigation/breadcrumbs.test.ts
    tests/categories/page.test.ts
    tests/categories/metadata.test.ts
  </files>
  <action>
Create 10 test stub files using it.todo() pattern (from Phase 3 convention: Phase 13-00 decision). Each file should import from setup.ts and describe the behavior being tested.

1. `tests/categories/create.test.ts` (CAT-01):
   - it.todo('creates root category with correct path')
   - it.todo('creates child category with materialized path from parent')
   - it.todo('auto-generates slug from name')
   - it.todo('validates unique slug constraint')
   - it.todo('sets depth based on parent depth')
   - it.todo('enforces maximum category depth of 5')

2. `tests/categories/move.test.ts` (CAT-01):
   - it.todo('moves category to new parent and updates path')
   - it.todo('updates all descendant paths when parent moves')
   - it.todo('prevents moving category into its own descendant')
   - it.todo('updates position when reordering siblings')
   - it.todo('moves category to root level')
   - it.todo('uses transaction for atomic path updates')

3. `tests/categories/attributes.test.ts` (CAT-02):
   - it.todo('creates category attribute with SELECT type and values')
   - it.todo('creates category attribute with RANGE type')
   - it.todo('validates SELECT type requires non-empty values array')
   - it.todo('enforces unique key per category')
   - it.todo('updates attribute position for reordering')
   - it.todo('deletes attribute and cascades')

4. `tests/collections/crud.test.ts` (CAT-03):
   - it.todo('creates collection with name, slug, description')
   - it.todo('lists all collections with pagination')
   - it.todo('updates collection details')
   - it.todo('deletes collection')
   - it.todo('auto-generates unique slug')

5. `tests/collections/products.test.ts` (CAT-03):
   - it.todo('adds product to collection')
   - it.todo('removes product from collection')
   - it.todo('lists products in collection with pagination')
   - it.todo('prevents duplicate product-collection entries')

6. `tests/brands-tags/crud.test.ts` (CAT-03):
   - it.todo('creates brand with name, slug, logo')
   - it.todo('lists brands with pagination')
   - it.todo('updates brand')
   - it.todo('deletes brand')
   - it.todo('creates tag with name and slug')
   - it.todo('lists tags')
   - it.todo('deletes tag')

7. `tests/navigation/mega-menu.test.ts` (CAT-04):
   - it.todo('fetches categories with depth <= 2')
   - it.todo('builds tree structure from flat category list')
   - it.todo('orders categories by position within each level')
   - it.todo('excludes inactive categories')

8. `tests/navigation/breadcrumbs.test.ts` (CAT-05):
   - it.todo('generates breadcrumb trail from materialized path')
   - it.todo('returns ancestors in correct depth order')
   - it.todo('handles root-level category breadcrumbs')
   - it.todo('includes Home as first breadcrumb item')

9. `tests/categories/page.test.ts` (CAT-06):
   - it.todo('fetches products from category and all subcategories')
   - it.todo('paginates product results')
   - it.todo('fetches direct subcategories for navigation')
   - it.todo('returns category attributes for filter display')

10. `tests/categories/metadata.test.ts` (CAT-07):
    - it.todo('returns metaTitle when set, falls back to name')
    - it.todo('returns metaDescription when set, falls back to description')
    - it.todo('generates canonical URL from slug')
    - it.todo('includes Open Graph metadata')

Each file pattern:
```typescript
import { describe, it } from 'vitest';

describe('[Feature Name]', () => {
  it.todo('behavior description');
  // ... more stubs
});
```
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && pnpm test --run --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <done>10 test stub files exist with it.todo() entries covering all CAT-01 through CAT-07 requirements, all tests pass (stubs show as pending/skipped)</done>
</task>

</tasks>

<verification>
- `pnpm test --run` passes with all existing tests green
- Test stubs show as pending/skipped (not failing)
- `ls tests/categories/ tests/collections/ tests/brands-tags/ tests/navigation/` shows all 10 test files
</verification>

<success_criteria>
- tests/setup.ts extended with category domain mocks without breaking existing tests
- 10 test stub files created covering all CAT-01 through CAT-07 requirements
- All existing product and shipping tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-categories-navigation/04-00-SUMMARY.md`
</output>
