---
phase: 05-search-system
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/setup.ts
  - tests/fixtures/search.fixtures.ts
  - tests/search/sync.service.test.ts
  - tests/search/search.service.test.ts
  - tests/search/search.controller.test.ts
autonomous: true
requirements:
  - SRCH-01
  - SRCH-03
  - SRCH-04
  - SRCH-05
  - SRCH-06

must_haves:
  truths:
    - "Meilisearch client is mocked in test setup so no real Meilisearch is needed for tests"
    - "Test stubs exist for all search-related requirements with it.todo() markers"
    - "Search document fixtures provide realistic product data for test assertions"
  artifacts:
    - path: "tests/setup.ts"
      provides: "Meilisearch client mock added to existing setup"
      contains: "vi.mock('meilisearch')"
    - path: "tests/fixtures/search.fixtures.ts"
      provides: "Search document fixtures for testing"
      exports: ["mockSearchDocument", "mockSearchDocuments", "mockFacetDistribution"]
    - path: "tests/search/sync.service.test.ts"
      provides: "Test stubs for SRCH-01 sync behavior"
      contains: "it.todo"
    - path: "tests/search/search.service.test.ts"
      provides: "Test stubs for SRCH-03, SRCH-04, SRCH-05"
      contains: "it.todo"
    - path: "tests/search/search.controller.test.ts"
      provides: "Test stubs for SRCH-06 admin settings"
      contains: "it.todo"
  key_links: []
---

<objective>
Wave 0: Search system test infrastructure and stubs

Purpose: Establish mock Meilisearch client in test setup and create test stub files for all search requirements before any implementation begins. This ensures the Nyquist validation pattern (tests before code) is followed.

Output: Mock Meilisearch client in setup.ts, search fixtures file, three test stub files covering SRCH-01 through SRCH-06.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-search-system/05-RESEARCH.md
@.planning/phases/05-search-system/05-VALIDATION.md
@tests/setup.ts
@vitest.config.ts

<interfaces>
<!-- Existing test setup patterns the executor must follow -->

From tests/setup.ts:
```typescript
// Pattern: vi.mock('@repo/db') with comprehensive mock object
// Pattern: Export mock fixtures for reuse across test files
// Pattern: Mock products with all fields populated
```

From vitest.config.ts:
```typescript
// Tests located in tests/**/*.test.{ts,tsx}
// Setup file: ./tests/setup.ts
// Environment: node
// Globals: true
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Meilisearch mock to test setup and create search fixtures</name>
  <files>tests/setup.ts, tests/fixtures/search.fixtures.ts</files>
  <action>
1. Read tests/setup.ts and APPEND (do not replace existing content) a Meilisearch client mock at the bottom:

```typescript
// Mock meilisearch SDK
vi.mock('meilisearch', () => {
  const mockIndex = {
    search: vi.fn().mockResolvedValue({ hits: [], estimatedTotalHits: 0, facetDistribution: {} }),
    addDocuments: vi.fn().mockResolvedValue({ taskUid: 1 }),
    updateDocuments: vi.fn().mockResolvedValue({ taskUid: 2 }),
    deleteDocument: vi.fn().mockResolvedValue({ taskUid: 3 }),
    deleteAllDocuments: vi.fn().mockResolvedValue({ taskUid: 4 }),
    updateSettings: vi.fn().mockResolvedValue({ taskUid: 5 }),
    getSettings: vi.fn().mockResolvedValue({}),
    updateSynonyms: vi.fn().mockResolvedValue({ taskUid: 6 }),
    getSynonyms: vi.fn().mockResolvedValue({}),
    updateStopWords: vi.fn().mockResolvedValue({ taskUid: 7 }),
    getStopWords: vi.fn().mockResolvedValue([]),
    updateRankingRules: vi.fn().mockResolvedValue({ taskUid: 8 }),
    getRankingRules: vi.fn().mockResolvedValue([]),
    waitForTask: vi.fn().mockResolvedValue({ status: 'succeeded' }),
  };

  const mockClient = {
    index: vi.fn().mockReturnValue(mockIndex),
    createKey: vi.fn().mockResolvedValue({ key: 'test-search-key' }),
    getKeys: vi.fn().mockResolvedValue({ results: [] }),
    waitForTask: vi.fn().mockResolvedValue({ status: 'succeeded' }),
  };

  return {
    MeiliSearch: vi.fn().mockImplementation(() => mockClient),
  };
});
```

2. Create tests/fixtures/search.fixtures.ts with:
- mockSearchDocument: A single SearchDocument matching the interface from 05-RESEARCH.md (id, name, description, sku, price, images, status, productType, brandId, brandName, categoryId, categoryName, categoryPath, createdAt as unix timestamp, updatedAt as unix timestamp)
- mockSearchDocuments: Array of 5 search documents (one per product type: SIMPLE, VARIABLE, WEIGHTED, DIGITAL, BUNDLED) with realistic e-commerce data
- mockFacetDistribution: Example facet distribution object with brandName, categoryName, and price buckets
- mockSearchResults: Pre-built search response object with hits, estimatedTotalHits, facetDistribution, processingTimeMs
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx vitest run tests/fixtures/search.fixtures.ts --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Meilisearch mock added to tests/setup.ts without breaking existing mocks. Search fixtures file exports mockSearchDocument, mockSearchDocuments, mockFacetDistribution, mockSearchResults.</done>
</task>

<task type="auto">
  <name>Task 2: Create test stub files for all search requirements</name>
  <files>tests/search/sync.service.test.ts, tests/search/search.service.test.ts, tests/search/search.controller.test.ts</files>
  <action>
1. Create tests/search/sync.service.test.ts with it.todo() stubs covering SRCH-01:
   - describe('SyncService')
     - describe('indexProduct')
       - it.todo('should build search document from product with relations')
       - it.todo('should add document to Meilisearch index')
       - it.todo('should handle products without brand gracefully')
     - describe('deleteProduct')
       - it.todo('should delete document from Meilisearch index by id')
     - describe('fullSync')
       - it.todo('should fetch all active products in batches of 10000')
       - it.todo('should build search documents and add to index in batch')
       - it.todo('should only sync ACTIVE products')
     - describe('event listeners')
       - it.todo('should sync product on product.created event')
       - it.todo('should sync product on product.updated event')
       - it.todo('should delete from index on product.deleted event')

2. Create tests/search/search.service.test.ts with it.todo() stubs covering SRCH-03, SRCH-04, SRCH-05:
   - describe('SearchService')
     - describe('initializeIndex')
       - it.todo('should configure searchableAttributes before adding documents')
       - it.todo('should configure filterableAttributes for categoryId, brandId, status, price')
       - it.todo('should set sortableAttributes for price, createdAt, name')
       - it.todo('should configure typo tolerance settings')
     - describe('search - SRCH-03')
       - it.todo('should search across name, description, SKU fields')
       - it.todo('should include brand and category in search results')
       - it.todo('should return highlighted matches')
     - describe('typo tolerance - SRCH-04')
       - it.todo('should find products with single character typos')
       - it.todo('should support synonym mapping for related terms')
     - describe('facets - SRCH-05')
       - it.todo('should return facet distribution for brandName')
       - it.todo('should return facet distribution for categoryName')
       - it.todo('should return facet counts with search results')

3. Create tests/search/search.controller.test.ts with it.todo() stubs covering SRCH-06:
   - describe('SearchController')
     - describe('GET /api/search')
       - it.todo('should return search results for query parameter')
       - it.todo('should return empty results for no matches')
       - it.todo('should include facetDistribution in response')
     - describe('PUT /api/search/synonyms - admin only')
       - it.todo('should update synonyms in Meilisearch')
       - it.todo('should require admin authentication')
     - describe('PUT /api/search/stop-words - admin only')
       - it.todo('should update stop words in Meilisearch')
     - describe('PUT /api/search/ranking-rules - admin only')
       - it.todo('should update ranking rules in Meilisearch')
     - describe('GET /api/search/settings - admin only')
       - it.todo('should return current search settings')
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx vitest run tests/search/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Three test stub files exist in tests/search/ with it.todo() markers covering all SRCH requirements. Running vitest shows all stubs as pending (not failing).</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/search/ --reporter=verbose` shows all tests as todo/pending, zero failures
- `npx vitest run tests/ --reporter=verbose` shows existing tests still pass (Meilisearch mock doesn't break anything)
- tests/fixtures/search.fixtures.ts exports are importable
</verification>

<success_criteria>
- Meilisearch SDK mock in tests/setup.ts provides mockIndex with search, addDocuments, deleteDocument, updateSettings, updateSynonyms, updateStopWords, updateRankingRules methods
- Search fixtures file exports 4 mock objects for use in implementation tests
- 3 test stub files with it.todo() stubs cover every SRCH requirement
- Existing test suite still passes with the new mocks added
</success_criteria>

<output>
After completion, create `.planning/phases/05-search-system/05-00-SUMMARY.md`
</output>
