---
phase: 05-search-system
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - apps/admin/src/app/dashboard/search/page.tsx
  - apps/admin/src/app/dashboard/search/search-settings-form.tsx
  - apps/admin/src/app/dashboard/search/actions.ts
  - apps/admin/src/app/dashboard/layout.tsx
autonomous: true
requirements:
  - SRCH-06

must_haves:
  truths:
    - "Admin can view and edit synonym mappings from the search settings page"
    - "Admin can view and edit stop words from the search settings page"
    - "Admin can view and edit ranking rules from the search settings page"
    - "Admin can trigger a full product re-sync from the search settings page"
  artifacts:
    - path: "apps/admin/src/app/dashboard/search/page.tsx"
      provides: "Admin search settings page (server component)"
      min_lines: 20
    - path: "apps/admin/src/app/dashboard/search/search-settings-form.tsx"
      provides: "Interactive form for synonyms, stop words, ranking rules"
      exports: ["SearchSettingsForm"]
    - path: "apps/admin/src/app/dashboard/search/actions.ts"
      provides: "Server actions for updating search settings"
      exports: ["updateSynonyms", "updateStopWords", "updateRankingRules", "triggerFullSync"]
  key_links:
    - from: "apps/admin/src/app/dashboard/search/actions.ts"
      to: "apps/server/src/modules/search/search.routes.ts"
      via: "fetch to /api/search/synonyms, /api/search/stop-words, /api/search/ranking-rules, /api/search/sync"
      pattern: "fetch.*api/search"
    - from: "apps/admin/src/app/dashboard/search/page.tsx"
      to: "apps/admin/src/app/dashboard/search/search-settings-form.tsx"
      via: "Client component rendered in server component"
      pattern: "SearchSettingsForm"
---

<objective>
Admin search settings UI for configuring Meilisearch

Purpose: Build the admin panel page where admins can configure search behavior: manage synonym pairs, stop words, ranking rule order, and trigger full product re-indexing. This completes the SRCH-06 requirement end-to-end.

Output: Admin search settings page at /dashboard/search with forms for synonyms, stop words, ranking rules, and a sync button.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-search-system/05-RESEARCH.md
@apps/admin/src/app/dashboard/layout.tsx
@apps/admin/src/app/dashboard/users/actions.ts
@apps/admin/src/lib/api.ts

<interfaces>
<!-- Existing admin patterns to follow -->

From apps/admin/src/lib/api.ts:
```typescript
// API helper for making authenticated requests to the server
// Uses auth() to get token and forwards it in Authorization header
```

From apps/admin/src/app/dashboard/users/actions.ts:
```typescript
// Pattern: 'use server' server actions that call server API
// Typically: get token from auth(), fetch API endpoint with Authorization header
// Return data or throw error
```

From apps/admin/src/app/dashboard/layout.tsx:
```typescript
// Dashboard layout with sidebar navigation
// Navigation links use <a href="/dashboard/..."> pattern
```

From 05-02 (will create):
```typescript
// Search API endpoints:
// GET /api/search/settings -> full settings object
// GET /api/search/synonyms -> Record<string, string[]>
// PUT /api/search/synonyms { synonyms: Record<string, string[]> }
// GET /api/search/stop-words -> string[]
// PUT /api/search/stop-words { stopWords: string[] }
// GET /api/search/ranking-rules -> string[]
// PUT /api/search/ranking-rules { rankingRules: string[] }
// POST /api/search/sync -> { success: true, message: 'Full sync started' }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create server actions and search settings page</name>
  <files>apps/admin/src/app/dashboard/search/actions.ts, apps/admin/src/app/dashboard/search/page.tsx</files>
  <action>
1. Read apps/admin/src/app/dashboard/users/actions.ts to understand the server action pattern used in this project (how auth token is obtained, how API calls are made).

2. Read apps/admin/src/lib/api.ts to understand the API helper.

3. Create apps/admin/src/app/dashboard/search/actions.ts with 'use server' directive:
   - Import auth from '@clerk/nextjs/server' (or follow the pattern from users/actions.ts for getting auth token)
   - Import the API helper or use fetch directly following existing patterns

   Server actions:
   a) async getSearchSettings(): Fetch GET /api/search/settings with auth token, return the settings object
   b) async getSynonyms(): Fetch GET /api/search/synonyms with auth token, return synonyms
   c) async updateSynonyms(synonyms: Record<string, string[]>): Fetch PUT /api/search/synonyms with auth token and { synonyms } body
   d) async getStopWords(): Fetch GET /api/search/stop-words with auth token, return stop words
   e) async updateStopWords(stopWords: string[]): Fetch PUT /api/search/stop-words with auth token and { stopWords } body
   f) async getRankingRules(): Fetch GET /api/search/ranking-rules with auth token, return rules
   g) async updateRankingRules(rankingRules: string[]): Fetch PUT /api/search/ranking-rules with auth token and { rankingRules } body
   h) async triggerFullSync(): Fetch POST /api/search/sync with auth token, return result

   Follow the exact fetch pattern from users/actions.ts (token forwarding, error handling, server URL).

4. Create apps/admin/src/app/dashboard/search/page.tsx as a Server Component:
   - Import getSearchSettings, getSynonyms, getStopWords, getRankingRules from './actions'
   - Import SearchSettingsForm from './search-settings-form'
   - Fetch current settings, synonyms, stop words, ranking rules in parallel
   - Render page with title "Search Settings" and description "Configure Meilisearch behavior for product search"
   - Pass fetched data as props to SearchSettingsForm client component
   - Style with Tailwind consistent with other admin pages (max-w-4xl, heading, sections)
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && test -f apps/admin/src/app/dashboard/search/actions.ts && test -f apps/admin/src/app/dashboard/search/page.tsx && grep -q "'use server'" apps/admin/src/app/dashboard/search/actions.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Server actions for all search settings operations. Search settings page fetches current config and passes to client form component.</done>
</task>

<task type="auto">
  <name>Task 2: Create interactive search settings form and add nav link</name>
  <files>apps/admin/src/app/dashboard/search/search-settings-form.tsx, apps/admin/src/app/dashboard/layout.tsx</files>
  <action>
1. Create apps/admin/src/app/dashboard/search/search-settings-form.tsx as a CLIENT component ('use client'):
   - Import React useState
   - Import server actions: updateSynonyms, updateStopWords, updateRankingRules, triggerFullSync from './actions'

   Props interface:
   ```typescript
   interface SearchSettingsFormProps {
     initialSynonyms: Record<string, string[]>;
     initialStopWords: string[];
     initialRankingRules: string[];
   }
   ```

   Create SearchSettingsForm component with three sections:

   a) SYNONYMS SECTION:
      - State: synonyms (Record<string, string[]>), newSynonymKey (string), newSynonymValues (string)
      - Display current synonyms as a list of key -> [values] with delete button per entry
      - Input fields: "Word" text input and "Synonyms (comma-separated)" text input with "Add Synonym" button
      - On add: parse comma-separated values, add to synonyms state
      - On delete: remove key from synonyms state
      - "Save Synonyms" button: calls updateSynonyms(synonyms), show success/error toast or status message
      - Style: card with border, p-6, space-y-4

   b) STOP WORDS SECTION:
      - State: stopWords (string[]), newStopWord (string)
      - Display current stop words as tag pills with X button to remove
      - Input field with "Add" button
      - "Save Stop Words" button: calls updateStopWords(stopWords)
      - Style: card with border, p-6

   c) RANKING RULES SECTION:
      - State: rankingRules (string[])
      - Display as ordered list with drag handles (simple up/down buttons for reordering)
      - Default rules: ['words', 'typo', 'proximity', 'attribute', 'sort', 'exactness']
      - Up/down arrow buttons to reorder
      - "Save Ranking Rules" button: calls updateRankingRules(rankingRules)
      - "Reset to Default" button to restore default order
      - Style: card with border, p-6

   d) SYNC SECTION:
      - "Full Re-sync" button that calls triggerFullSync()
      - Loading state during sync trigger
      - Description: "Re-index all active products to Meilisearch. Use after manual database changes."
      - Style: card with border, p-6, yellow/warning background for the button

   General:
   - Each section has a heading (h3), description, and save button
   - Use useState for form state, loading indicators, and success/error messages
   - Show status messages below save buttons: "Saved successfully" or error message
   - All save operations should set loading state and display result

2. Read apps/admin/src/app/dashboard/layout.tsx and add a "Search" navigation link to the sidebar:
   - Add a link to /dashboard/search in the sidebar navigation
   - Place it logically (after existing nav items like categories, brands, tags, etc.)
   - Use the same styling pattern as other nav links
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && test -f apps/admin/src/app/dashboard/search/search-settings-form.tsx && grep -q "SearchSettingsForm" apps/admin/src/app/dashboard/search/search-settings-form.tsx && grep -q "search" apps/admin/src/app/dashboard/layout.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SearchSettingsForm provides interactive management of synonyms (add/remove pairs), stop words (add/remove tags), ranking rules (reorder), and full sync trigger. Search link added to admin sidebar navigation.</done>
</task>

</tasks>

<verification>
- /dashboard/search page renders in admin app
- Synonyms section displays current synonyms, allows add/remove, saves via API
- Stop words section displays as tag pills, allows add/remove, saves via API
- Ranking rules section shows ordered list with reorder capability, saves via API
- Full Re-sync button triggers POST /api/search/sync
- Search link visible in admin sidebar navigation
- All server actions use proper auth token forwarding
</verification>

<success_criteria>
- Admin can navigate to /dashboard/search from sidebar
- Admin can add synonym pairs (e.g., "phone" -> "smartphone, mobile") and save
- Admin can add/remove stop words and save
- Admin can reorder ranking rules and save
- Admin can trigger full product re-sync
- All settings changes call the correct API endpoints with authentication
- Form shows success/error feedback after save operations
</success_criteria>

<output>
After completion, create `.planning/phases/05-search-system/05-04-SUMMARY.md`
</output>
