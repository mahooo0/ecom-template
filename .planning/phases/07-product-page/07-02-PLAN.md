---
phase: 07-product-page
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/client/src/components/product/product-image-gallery.tsx
  - apps/client/src/components/product/product-lightbox.tsx
autonomous: true
requirements:
  - PDPG-01

must_haves:
  truths:
    - "User sees a large hero image with horizontal thumbnail strip below"
    - "Hovering over the hero image shows a magnified zoom view on desktop"
    - "Clicking the hero image opens a fullscreen lightbox with arrow navigation"
    - "Mobile gallery is a swipeable horizontal carousel with dot indicators"
    - "Clicking a thumbnail updates the hero image"
  artifacts:
    - path: "apps/client/src/components/product/product-image-gallery.tsx"
      provides: "Image gallery with hero, thumbnails, hover zoom, mobile carousel"
      min_lines: 80
    - path: "apps/client/src/components/product/product-lightbox.tsx"
      provides: "Fullscreen dark overlay lightbox with arrow/swipe navigation"
      min_lines: 30
  key_links:
    - from: "apps/client/src/components/product/product-image-gallery.tsx"
      to: "apps/client/src/components/product/product-lightbox.tsx"
      via: "onClick opens lightbox"
      pattern: "setLightboxOpen|isOpen"
---

<objective>
Image gallery component with hero image, thumbnails, hover zoom on desktop, swipeable mobile carousel, and fullscreen lightbox.

Purpose: Core visual experience of the product page -- the first thing customers see and interact with.
Output: ProductImageGallery and ProductLightbox client components.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-product-page/07-CONTEXT.md
@.planning/phases/07-product-page/07-RESEARCH.md
@.planning/phases/07-product-page/07-01-SUMMARY.md

@apps/client/src/types/product-detail.ts

<interfaces>
From apps/client/src/types/product-detail.ts (created in 07-01):
```typescript
export interface ProductDetail extends Product {
  variants: ProductVariantDetail[];
  // images: string[] inherited from Product
}
```

Library to install:
```bash
pnpm add yet-another-react-lightbox --filter client
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProductImageGallery with hero, thumbnails, and hover zoom</name>
  <files>apps/client/src/components/product/product-image-gallery.tsx</files>
  <action>
    Install yet-another-react-lightbox: `pnpm add yet-another-react-lightbox --filter client`

    Create `'use client'` component `ProductImageGallery` accepting props: `{ images: string[]; productName: string }`.

    **Desktop layout (md+ breakpoints):**
    - Large hero image using `next/image` (aspect-square or 4:3, object-cover), with a relative container
    - Hover zoom: On mouse enter, show a magnifier overlay on the hero image itself. Use CSS `backgroundImage` + `backgroundPosition` + `backgroundSize: '250%'` approach. Track mouse position relative to container, update `backgroundPosition` as percentages. Show the zoom overlay div absolutely positioned on top of the image. On mouse leave, hide the zoom overlay.
    - Horizontal thumbnail strip below the hero: a flex row of small thumbnails (64x64 or 80x80) with border highlight on the active one. Clicking a thumbnail sets it as the active/hero image.
    - Fullscreen icon button (top-right of hero) opens the lightbox.
    - Clicking the hero image itself also opens the lightbox.

    **Mobile layout (below md):**
    - Swipeable horizontal carousel using CSS scroll-snap (`overflow-x: auto`, `scroll-snap-type: x mandatory`, each image `scroll-snap-align: start`, `min-w-full`).
    - Dot indicators below showing current position. Use IntersectionObserver or scroll position to track active index.
    - Tapping an image opens the lightbox.
    - No hover zoom on mobile (touch devices use pinch-to-zoom natively via lightbox).

    **State:**
    - `activeIndex: number` (which image is showing)
    - `isLightboxOpen: boolean`
    - `isZooming: boolean` + `zoomPosition: { x: number; y: number }`

    Use Tailwind for all styling. Use `next/image` for all images (Cloudinary remotePatterns configured in 07-01). Handle edge case of empty images array (show placeholder).
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx tsc --noEmit --project apps/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>ProductImageGallery renders hero with thumbnails on desktop, swipeable carousel on mobile, hover zoom works via CSS background approach</done>
</task>

<task type="auto">
  <name>Task 2: ProductLightbox fullscreen overlay integration</name>
  <files>
    apps/client/src/components/product/product-lightbox.tsx
    apps/client/src/components/product/product-image-gallery.tsx
  </files>
  <action>
    Create `'use client'` component `ProductLightbox` using `yet-another-react-lightbox`.

    Props: `{ images: string[]; isOpen: boolean; onClose: () => void; initialIndex: number }`

    Implementation:
    - Import Lightbox from 'yet-another-react-lightbox'
    - Import 'yet-another-react-lightbox/styles.css'
    - Map `images` string array to lightbox slides format: `images.map(src => ({ src }))`
    - Pass `open={isOpen}`, `close={onClose}`, `index={initialIndex}`, `slides={slides}`
    - Lightbox handles keyboard navigation (arrow keys, Escape), swipe on mobile, and dark overlay automatically

    Wire into ProductImageGallery:
    - Import ProductLightbox
    - Render `<ProductLightbox images={images} isOpen={isLightboxOpen} onClose={() => setIsLightboxOpen(false)} initialIndex={activeIndex} />` at the bottom of the gallery component
    - Hero image click and fullscreen icon both set `setIsLightboxOpen(true)`
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx tsc --noEmit --project apps/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Clicking hero image or fullscreen icon opens dark overlay lightbox with arrow/swipe navigation and keyboard support</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- `pnpm vitest run tests/products/product-page.test.ts` shows PDPG-01 stubs
- ProductImageGallery exports from its file
</verification>

<success_criteria>
- Gallery shows hero + thumbnails on desktop with hover zoom
- Mobile shows swipeable carousel with dots
- Lightbox opens on click with keyboard/swipe navigation
- All images use next/image with Cloudinary URLs
</success_criteria>

<output>
After completion, create `.planning/phases/07-product-page/07-02-SUMMARY.md`
</output>
