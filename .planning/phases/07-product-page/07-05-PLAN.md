---
phase: 07-product-page
plan: 05
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/client/src/components/product/product-specs-table.tsx
  - apps/client/src/components/product/reviews-placeholder.tsx
  - apps/client/src/components/product/related-products-carousel.tsx
  - apps/client/src/components/product/frequently-bought-together.tsx
autonomous: true
requirements:
  - PDPG-03
  - PDPG-04
  - PDPG-05
  - PDPG-06

must_haves:
  truths:
    - "Specifications table shows product attributes as two-column key-value pairs"
    - "Reviews section shows average rating with star display and rating distribution bar chart"
    - "Related products carousel shows 4-5 ProductCards on desktop, 2 on mobile, with scroll arrows"
    - "Frequently bought together section shows checkboxes (all checked by default) with total price and add-all button"
    - "FBT section hides when no products are available (empty array)"
  artifacts:
    - path: "apps/client/src/components/product/product-specs-table.tsx"
      provides: "Two-column attribute table from JSONB attributes"
      min_lines: 20
    - path: "apps/client/src/components/product/reviews-placeholder.tsx"
      provides: "Display-only reviews section with rating distribution"
      min_lines: 40
    - path: "apps/client/src/components/product/related-products-carousel.tsx"
      provides: "Horizontal scrollable product carousel with arrows"
      min_lines: 40
    - path: "apps/client/src/components/product/frequently-bought-together.tsx"
      provides: "Checkbox list with add-all-to-cart functionality"
      min_lines: 60
  key_links:
    - from: "apps/client/src/components/product/related-products-carousel.tsx"
      to: "apps/client/src/components/product/product-card.tsx"
      via: "renders ProductCard for each related product"
      pattern: "ProductCard"
    - from: "apps/client/src/components/product/frequently-bought-together.tsx"
      to: "apps/client/src/stores/cart-store.ts"
      via: "useCartStore().addItem() for each checked product"
      pattern: "useCartStore|addItem"
---

<objective>
Specs table, reviews placeholder, related products carousel, and frequently bought together section.

Purpose: Information and recommendation sections that complete the product page below the fold.
Output: ProductSpecsTable, ReviewsPlaceholder, RelatedProductsCarousel, FrequentlyBoughtTogether components.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-product-page/07-CONTEXT.md
@.planning/phases/07-product-page/07-RESEARCH.md
@.planning/phases/07-product-page/07-01-SUMMARY.md

@apps/client/src/types/product-detail.ts
@apps/client/src/components/product/product-card.tsx
@apps/client/src/components/ui/star-rating.tsx
@apps/client/src/stores/cart-store.ts

<interfaces>
From apps/client/src/components/product/product-card.tsx:
```typescript
interface ProductCardProps {
  product: Product & {
    averageRating?: number;
    reviewCount?: number;
    brand?: { name: string };
  };
}
export function ProductCard({ product }: ProductCardProps)
```

From apps/client/src/components/ui/star-rating.tsx:
```typescript
export function StarRating({ rating, size }: { rating: number; size?: 'sm' | 'md' | 'lg' })
```

Product.attributes is JSONB (Record<string, any> at runtime).
CategoryAttribute from schema: { name: string, displayName: string, type: string }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProductSpecsTable and ReviewsPlaceholder</name>
  <files>
    apps/client/src/components/product/product-specs-table.tsx
    apps/client/src/components/product/reviews-placeholder.tsx
  </files>
  <action>
    **ProductSpecsTable** (server-safe, no `'use client'`):
    - Props: `{ attributes: Record<string, any>; categoryAttributes?: Array<{ name: string; displayName: string }> }`
    - Build display rows:
      - For each key in `attributes`, find matching `categoryAttributes` entry for display name
      - If no match, humanize the key: replace underscores with spaces, capitalize first letter of each word
      - Skip null/undefined values
    - Render as a `<table>` or `<dl>` with alternating row backgrounds (gray-50 / white)
    - Two columns: attribute name (left, font-medium, text-gray-600) and value (right, text-gray-900)
    - "Specifications" heading above the table
    - If no attributes, don't render the section at all

    **ReviewsPlaceholder** (server-safe, no `'use client'`):
    - Props: `{ averageRating?: number; reviewCount?: number; reviews?: Array<{ id: string; rating: number; title?: string; comment: string; userName: string; createdAt: string }> }`
    - This is a display-only placeholder for Phase 16's full implementation.
    - Render:
      - "Customer Reviews" heading
      - Average rating: large number + StarRating component + "(X reviews)"
      - Rating distribution bar chart: 5 rows (5 stars to 1 star), each with a label, horizontal bar (proportional width), and count. Calculate distribution from reviews array. If no reviews array, show empty bars.
      - Individual reviews list (if reviews provided): star rating, reviewer name, date, review text. Show first 3 reviews.
      - If no reviews: "No reviews yet. Be the first to review this product." message
    - Style: Clean card-like section with border-t separator
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx tsc --noEmit --project apps/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>ProductSpecsTable renders two-column attribute table with humanized labels, ReviewsPlaceholder shows rating distribution bar chart and review list</done>
</task>

<task type="auto">
  <name>Task 2: RelatedProductsCarousel and FrequentlyBoughtTogether</name>
  <files>
    apps/client/src/components/product/related-products-carousel.tsx
    apps/client/src/components/product/frequently-bought-together.tsx
  </files>
  <action>
    **RelatedProductsCarousel** (`'use client'` -- needs scroll handling):
    - Props: `{ products: Product[] }` (Product with optional brand, averageRating, reviewCount)
    - Don't render if `products.length === 0`
    - "Related Products" heading
    - Horizontal scrollable container using CSS scroll-snap:
      - `overflow-x-auto scroll-snap-type-x-mandatory` (use Tailwind: `overflow-x-auto snap-x snap-mandatory`)
      - Each item: `snap-start min-w-[250px] sm:min-w-[220px]` (roughly 4-5 on desktop, 2 on mobile)
      - Render each product with existing `ProductCard` component
    - Left/right arrow buttons (absolute positioned, left/right edges):
      - `<` and `>` buttons with `bg-white shadow-lg rounded-full` styling
      - On click, scroll container by card width using `scrollBy({ left: +/- 280, behavior: 'smooth' })`
      - Hide left arrow at start, right arrow at end (track scroll position with onScroll)
      - Arrows hidden on mobile (`hidden md:flex`)
    - Scrollbar hidden: use `scrollbar-hide` class or CSS `scrollbar-width: none; -ms-overflow-style: none; ::-webkit-scrollbar { display: none }`

    **FrequentlyBoughtTogether** (`'use client'` -- needs useState + cart store):
    - Props: `{ products: Product[]; currentProduct: { id: string; name: string; price: number; image: string } }`
    - Don't render if `products.length === 0` (graceful hide per CONTEXT.md)
    - State: `checkedIds: Set<string>` -- initialized with all product IDs checked
    - "Frequently Bought Together" heading
    - Render current product (always shown, not checkable) + "+" separators + each FBT product with:
      - Checkbox (checked by default)
      - Small thumbnail (64x64)
      - Product name (truncated)
      - Price
    - Total price calculation: sum of currentProduct.price + prices of all checked FBT products
    - "Add selected to cart" button:
      - Shows total price: "Add X items - $XX.XX"
      - On click: call `useCartStore().addItem()` for current product AND each checked FBT product
      - Disabled if no FBT items checked (but current product is always added)
    - Tailwind styling: card-like container with border, products in a flex row (wrap on mobile)
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx tsc --noEmit --project apps/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Related carousel shows scrollable ProductCards with arrows, FBT shows checkboxes with add-all-to-cart and total price</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- All four components export correctly
- Components handle empty data gracefully (don't render)
</verification>

<success_criteria>
- Specs table renders attributes with humanized or category-specific labels
- Reviews placeholder shows rating distribution bars and review list
- Related carousel scrolls horizontally with arrow buttons, reuses ProductCard
- FBT shows checkboxes, calculates total, adds selected to cart
- All sections hide gracefully when data is empty
</success_criteria>

<output>
After completion, create `.planning/phases/07-product-page/07-05-SUMMARY.md`
</output>
