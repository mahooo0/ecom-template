---
phase: 07-product-page
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/client/src/components/product/weighted-quantity-selector.tsx
  - apps/client/src/components/product/digital-product-info.tsx
  - apps/client/src/components/product/bundle-items-list.tsx
  - apps/client/src/lib/utils.ts
autonomous: true
requirements:
  - PDPG-08
  - PDPG-09
  - PDPG-10

must_haves:
  truths:
    - "Weighted products show slider + input for quantity with real-time price calculation"
    - "Digital products show file type icon, file size, format, and 'Instant download after purchase' delivery method"
    - "Bundle products show included items with thumbnails, individual prices, and savings callout"
    - "Unit price (price per kg/lb) is shown prominently for weighted products"
    - "Bundle savings shows 'Save $X.XX vs buying separately'"
  artifacts:
    - path: "apps/client/src/components/product/weighted-quantity-selector.tsx"
      provides: "Slider + input weight selector with real-time price"
      min_lines: 40
    - path: "apps/client/src/components/product/digital-product-info.tsx"
      provides: "File info display with icon, size, format, delivery method"
      min_lines: 30
    - path: "apps/client/src/components/product/bundle-items-list.tsx"
      provides: "Bundle contents list with pricing comparison"
      min_lines: 40
    - path: "apps/client/src/lib/utils.ts"
      provides: "formatPrice and formatFileSize utility functions"
      exports: ["formatPrice", "formatFileSize"]
  key_links:
    - from: "apps/client/src/components/product/weighted-quantity-selector.tsx"
      to: "price calculation"
      via: "Math.round(pricePerUnit * weight)"
      pattern: "pricePerUnit.*weight|calcWeightedPrice"
    - from: "apps/client/src/components/product/bundle-items-list.tsx"
      to: "savings calculation"
      via: "sum individual prices vs bundle price"
      pattern: "savings|individual.*bundle"
---

<objective>
Type-specific display components for weighted, digital, and bundle products with utility functions.

Purpose: Each product type needs unique information displays -- weight calculator, file info, and bundle pricing comparison.
Output: WeightedQuantitySelector, DigitalProductInfo, BundleItemsList components + shared utils.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-product-page/07-CONTEXT.md
@.planning/phases/07-product-page/07-RESEARCH.md
@.planning/phases/07-product-page/07-01-SUMMARY.md

@apps/client/src/types/product-detail.ts

<interfaces>
From @repo/types (WeightedMeta, DigitalMeta, BundleItem):
```typescript
// WeightedMeta fields: pricePerUnit (cents), unit ('kg' | 'lb' | 'piece'), minWeight, maxWeight
// DigitalMeta fields: fileUrl, fileSize (bytes), fileFormat (string), maxDownloads, expiryDays
// BundleItem fields: productId, quantity, product: Product (with name, price, images)
```

Price pattern: all monetary values stored as integer cents. Display: `(cents / 100).toFixed(2)` with dollar sign.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared utilities (formatPrice, formatFileSize)</name>
  <files>apps/client/src/lib/utils.ts</files>
  <action>
    Create (or add to existing) `apps/client/src/lib/utils.ts`:

    ```typescript
    export function formatPrice(cents: number): string {
      return `$${(cents / 100).toFixed(2)}`;
    }

    export function formatFileSize(bytes: number): string {
      if (bytes < 1024) return `${bytes} B`;
      if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`;
      return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
    }
    ```

    If the file already exists, add these functions without overwriting existing content. Check first.
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx tsc --noEmit --project apps/client/tsconfig.json 2>&1 | head -5</automated>
  </verify>
  <done>formatPrice and formatFileSize exported from utils.ts</done>
</task>

<task type="auto">
  <name>Task 2: WeightedQuantitySelector, DigitalProductInfo, and BundleItemsList</name>
  <files>
    apps/client/src/components/product/weighted-quantity-selector.tsx
    apps/client/src/components/product/digital-product-info.tsx
    apps/client/src/components/product/bundle-items-list.tsx
  </files>
  <action>
    **WeightedQuantitySelector** (`'use client'`):
    - Props: `{ pricePerUnit: number; unit: string; minWeight?: number; maxWeight?: number; onWeightChange: (weight: number) => void; onPriceChange: (totalCents: number) => void }`
    - State: `weight: number` (default to minWeight or 1.0)
    - Render:
      - Unit price prominently: e.g., "$4.99 per kg" using formatPrice
      - Range slider (`<input type="range">`) from minWeight (default 0.1) to maxWeight (default 10), step 0.1
      - Number input synced with slider, same min/max/step
      - Calculated total price below: `formatPrice(Math.round(pricePerUnit * weight))`
      - Call `onWeightChange(weight)` and `onPriceChange(Math.round(pricePerUnit * weight))` on change
    - Tailwind: slider uses accent-blue-600, input has border-gray-300 styling

    **DigitalProductInfo** (no `'use client'` needed -- purely presentational):
    - Props: `{ fileFormat: string; fileSize: number; maxDownloads?: number; expiryDays?: number }`
    - File type icon map: Record<string, string> mapping lowercase format to SVG icon class or emoji placeholder. Use simple SVG icons (document, archive, music, video, image). Keep it simple -- use inline SVG or a div with text.
    - Render:
      - File type icon + format name (e.g., "PDF Document")
      - File size using formatFileSize
      - Delivery method: "Instant download after purchase"
      - "No shipping required" indicator with a truck-slash icon or text
      - If maxDownloads: "Up to X downloads"
      - If expiryDays: "Access expires after X days"
    - Styled with a light blue/gray info card background

    **BundleItemsList** (no `'use client'` needed -- purely presentational):
    - Props: `{ bundleItems: Array<{ product: Product; quantity: number }>; bundlePrice: number }`
    - Calculate individual total: `items.reduce((sum, item) => sum + item.product.price * item.quantity, 0)`
    - Calculate savings: `Math.max(0, individualTotal - bundlePrice)`
    - Render:
      - "What's Included" heading
      - List of items: thumbnail (64x64 next/image), product name, quantity badge if > 1, individual price
      - Pricing comparison box at bottom:
        - "If bought separately: ~~$XX.XX~~" (strikethrough)
        - "Bundle price: $XX.XX" (bold, larger)
        - "You save: $XX.XX (XX%)" in green if savings > 0
    - Handle empty bundleItems gracefully (don't render section)
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && npx tsc --noEmit --project apps/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>WeightedQuantitySelector shows slider+input with real-time price, DigitalProductInfo shows file details, BundleItemsList shows included items with savings callout</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- All three components export correctly
- `pnpm vitest run tests/products/product-page.test.ts` shows PDPG-08, PDPG-09, PDPG-10 stubs
</verification>

<success_criteria>
- Weighted: slider and input sync, price updates in real-time, unit price shown prominently
- Digital: file icon, size, format, delivery method, and no-shipping indicator displayed
- Bundle: item list with thumbnails, individual vs bundle pricing, savings percentage
</success_criteria>

<output>
After completion, create `.planning/phases/07-product-page/07-04-SUMMARY.md`
</output>
