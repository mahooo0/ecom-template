---
phase: 02-authentication-system
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/client/src/app/(auth)/sign-in/[[...sign-in]]/page.tsx
  - apps/client/src/app/(auth)/sign-up/[[...sign-up]]/page.tsx
  - apps/admin/src/app/unauthorized/page.tsx
  - apps/admin/src/app/(auth)/sign-in/[[...sign-in]]/page.tsx
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-05

must_haves:
  truths:
    - "User can navigate to /sign-up and see Clerk's SignUp component with email/password option"
    - "User can navigate to /sign-in and see Clerk's SignIn component with Google and GitHub OAuth options"
    - "After signing in, user is redirected to the homepage with session active"
    - "Admin app shows Clerk SignIn component for unauthenticated users"
    - "Admin app redirects non-admin users to /unauthorized page with a clear message"
    - "Admin app allows ADMIN and SUPER_ADMIN users to access dashboard"
  artifacts:
    - path: "apps/client/src/app/(auth)/sign-in/[[...sign-in]]/page.tsx"
      provides: "Client sign-in page with Clerk SignIn component"
      contains: "SignIn"
    - path: "apps/client/src/app/(auth)/sign-up/[[...sign-up]]/page.tsx"
      provides: "Client sign-up page with Clerk SignUp component"
      contains: "SignUp"
    - path: "apps/admin/src/app/unauthorized/page.tsx"
      provides: "Unauthorized access page for non-admin users"
      contains: "unauthorized"
    - path: "apps/admin/src/app/(auth)/sign-in/[[...sign-in]]/page.tsx"
      provides: "Admin sign-in page"
      contains: "SignIn"
  key_links:
    - from: "apps/client/src/app/(auth)/sign-in/[[...sign-in]]/page.tsx"
      to: "@clerk/nextjs"
      via: "SignIn component import"
      pattern: "import.*SignIn.*@clerk/nextjs"
    - from: "apps/admin/middleware.ts"
      to: "apps/admin/src/app/unauthorized/page.tsx"
      via: "redirect for non-admin roles"
      pattern: "/unauthorized"
---

<objective>
Create authentication pages for the client app (sign-in with OAuth, sign-up with email/password) and admin app (sign-in page, unauthorized landing page), enabling users to register, login, and be properly gated from admin access.

Purpose: These pages are the user-facing entry points for authentication. Without them, users cannot create accounts, login, or be properly redirected when they lack admin permissions.
Output: Client app has /sign-in and /sign-up pages with Clerk components, admin app has /sign-in and /unauthorized pages, and the admin middleware role check has a destination to redirect non-admin users.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication-system/02-RESEARCH.md
@.planning/phases/02-authentication-system/02-01-PLAN.md

<interfaces>
<!-- From Plan 01 - ClerkProvider and middleware already configured -->
<!-- Client middleware.ts marks /sign-in(.*) and /sign-up(.*) as public routes -->
<!-- Admin middleware.ts redirects non-admin users to /unauthorized -->
<!-- Both apps have ClerkProvider wrapping the layout -->

From @clerk/nextjs (components to use):
```typescript
import { SignIn } from '@clerk/nextjs';
import { SignUp } from '@clerk/nextjs';
import { SignedIn, SignedOut, UserButton } from '@clerk/nextjs';
```

From apps/client/middleware.ts (from Plan 01):
```typescript
const isPublicRoute = createRouteMatcher([
  '/',
  '/products(.*)',
  '/categories(.*)',
  '/sign-in(.*)',
  '/sign-up(.*)',
  '/api/webhooks(.*)',
]);
```

From apps/admin/middleware.ts (from Plan 01):
```typescript
// Redirects to /unauthorized if role is not ADMIN or SUPER_ADMIN
const isPublicRoute = createRouteMatcher(['/unauthorized']);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create client app sign-in and sign-up pages</name>
  <files>apps/client/src/app/(auth)/sign-in/[[...sign-in]]/page.tsx, apps/client/src/app/(auth)/sign-up/[[...sign-up]]/page.tsx</files>
  <action>
    1. Create the (auth) route group directory structure in apps/client/src/app/:
       - apps/client/src/app/(auth)/sign-in/[[...sign-in]]/page.tsx
       - apps/client/src/app/(auth)/sign-up/[[...sign-up]]/page.tsx

    2. Create apps/client/src/app/(auth)/sign-in/[[...sign-in]]/page.tsx:
       - Import { SignIn } from '@clerk/nextjs'
       - Export default function that renders SignIn component centered on the page
       - Use Tailwind classes for centering: flex items-center justify-center min-h-[calc(100vh-80px)]
       - The SignIn component automatically provides email/password + configured OAuth providers (Google, GitHub)
       - No additional configuration needed -- OAuth providers are configured in Clerk Dashboard

    3. Create apps/client/src/app/(auth)/sign-up/[[...sign-up]]/page.tsx:
       - Import { SignUp } from '@clerk/nextjs'
       - Export default function that renders SignUp component centered on the page
       - Same centering layout as sign-in
       - SignUp automatically handles email/password registration and OAuth sign-up

    Note: The [[...sign-in]] and [[...sign-up]] catch-all segments are REQUIRED by Clerk for
    multi-step auth flows (verification codes, OAuth callbacks). Without them, Clerk's internal
    navigation breaks.

    Note: OAuth providers (Google, GitHub) must be configured in the Clerk Dashboard by the user.
    The components will automatically show whatever providers are enabled there.
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && test -f "apps/client/src/app/(auth)/sign-in/[[...sign-in]]/page.tsx" && test -f "apps/client/src/app/(auth)/sign-up/[[...sign-up]]/page.tsx" && grep -c "SignIn" "apps/client/src/app/(auth)/sign-in/[[...sign-in]]/page.tsx" && grep -c "SignUp" "apps/client/src/app/(auth)/sign-up/[[...sign-up]]/page.tsx"</automated>
  </verify>
  <done>Client app has /sign-in page with Clerk SignIn component (email/password + OAuth) and /sign-up page with Clerk SignUp component, both centered and styled with Tailwind, using catch-all route segments for Clerk's multi-step flows</done>
</task>

<task type="auto">
  <name>Task 2: Create admin sign-in page and unauthorized landing page</name>
  <files>apps/admin/src/app/unauthorized/page.tsx, apps/admin/src/app/(auth)/sign-in/[[...sign-in]]/page.tsx</files>
  <action>
    1. Create apps/admin/src/app/unauthorized/page.tsx:
       - Simple page showing "Access Denied" heading
       - Message: "You do not have permission to access the admin panel. Only administrators can access this area."
       - Include a link back to the client app (use process.env.NEXT_PUBLIC_CLIENT_URL or hardcode http://localhost:3002 with env fallback)
       - Include a "Sign out" option using SignOutButton from @clerk/nextjs so users can sign in with a different account
       - Style with Tailwind: centered content, clear typography, not alarming but informative
       - Use a shield/lock icon (inline SVG or emoji-free text indicator)

    2. Create apps/admin/src/app/(auth)/sign-in/[[...sign-in]]/page.tsx:
       - Import { SignIn } from '@clerk/nextjs'
       - Render SignIn component centered on the page
       - Full-page layout (not within the sidebar layout) -- the (auth) route group should use a different layout
       - Create apps/admin/src/app/(auth)/layout.tsx that provides a simple centered layout WITHOUT the sidebar

    3. Create apps/admin/src/app/(auth)/layout.tsx:
       - Simple layout without the sidebar
       - Just a centered container for auth pages
       - Does NOT need ClerkProvider (already in root layout)
       - Style: min-h-screen flex items-center justify-center bg-gray-50

    Note: The admin middleware (from Plan 01) already handles the redirect logic:
    - Unauthenticated users get redirected to sign-in by Clerk's default behavior
    - Authenticated non-admin users get redirected to /unauthorized
    - /unauthorized is marked as a public route in the middleware
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && test -f "apps/admin/src/app/unauthorized/page.tsx" && test -f "apps/admin/src/app/(auth)/sign-in/[[...sign-in]]/page.tsx" && grep -c "Access Denied\|unauthorized\|permission" apps/admin/src/app/unauthorized/page.tsx && grep -c "SignIn" "apps/admin/src/app/(auth)/sign-in/[[...sign-in]]/page.tsx"</automated>
  </verify>
  <done>Admin app has /unauthorized page with clear "Access Denied" message and sign-out option, /sign-in page with Clerk SignIn component in a sidebar-free layout, and (auth) route group layout for centered auth pages</done>
</task>

</tasks>

<verification>
- Client /sign-in page renders Clerk SignIn component
- Client /sign-up page renders Clerk SignUp component
- Both client auth pages use [[...catch-all]] route segments
- Admin /unauthorized page shows access denied message with sign-out option
- Admin /sign-in page renders Clerk SignIn component without sidebar
- Admin (auth) route group has its own layout without sidebar
- File paths match Next.js App Router conventions
</verification>

<success_criteria>
- User can navigate to /sign-up on client app to register with email/password (AUTH-02)
- User can navigate to /sign-in on client app to login with OAuth providers (AUTH-03)
- Admin app redirects non-admin authenticated users to /unauthorized page (AUTH-05)
- Admin app provides sign-in page for unauthenticated users (AUTH-05)
- Auth pages are styled consistently with the rest of each app
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-system/02-03-SUMMARY.md`
</output>
