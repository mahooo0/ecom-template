---
phase: 02-authentication-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/package.json
  - apps/admin/package.json
  - apps/server/package.json
  - apps/client/src/app/layout.tsx
  - apps/admin/src/app/layout.tsx
  - apps/client/middleware.ts
  - apps/admin/middleware.ts
  - apps/client/src/lib/auth.ts
  - apps/admin/src/lib/auth.ts
  - .env.example
  - packages/types/src/index.ts
autonomous: true
requirements:
  - AUTH-04

must_haves:
  truths:
    - "Clerk SDK packages are installed in client, admin, and server apps"
    - "ClerkProvider wraps both Next.js apps so auth state is available throughout"
    - "clerkMiddleware() runs on all routes in both Next.js apps to manage sessions"
    - "Auth helper utilities (checkRole, requireAdmin) are available in both Next.js apps"
    - "Custom JWT session claims type is declared for role-based checks"
  artifacts:
    - path: "apps/client/src/app/layout.tsx"
      provides: "ClerkProvider wrapper for client app"
      contains: "ClerkProvider"
    - path: "apps/admin/src/app/layout.tsx"
      provides: "ClerkProvider wrapper for admin app"
      contains: "ClerkProvider"
    - path: "apps/client/middleware.ts"
      provides: "Clerk middleware for client session management"
      contains: "clerkMiddleware"
    - path: "apps/admin/middleware.ts"
      provides: "Clerk middleware for admin with role-based access"
      contains: "clerkMiddleware"
    - path: "apps/client/src/lib/auth.ts"
      provides: "Auth helper functions for client app"
      exports: ["checkRole"]
    - path: "apps/admin/src/lib/auth.ts"
      provides: "Auth helper functions for admin app"
      exports: ["checkRole", "requireAdmin"]
  key_links:
    - from: "apps/client/src/app/layout.tsx"
      to: "@clerk/nextjs"
      via: "ClerkProvider import"
      pattern: "import.*ClerkProvider.*@clerk/nextjs"
    - from: "apps/admin/middleware.ts"
      to: "@clerk/nextjs/server"
      via: "clerkMiddleware import"
      pattern: "import.*clerkMiddleware.*@clerk/nextjs/server"
---

<objective>
Install Clerk SDK packages across all three apps, configure ClerkProvider in both Next.js apps, set up clerkMiddleware() for session management, and create auth helper utilities for role-based access checks.

Purpose: This is the foundation for all authentication. Without ClerkProvider and middleware, no auth pages, session persistence, or role checks can work.
Output: All three apps have Clerk SDKs installed, both Next.js apps wrap content in ClerkProvider, middleware handles sessions, and auth helpers are ready for role-based access.
</objective>

<execution_context>
@/Users/muhemmedibrahimov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/muhemmedibrahimov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication-system/02-RESEARCH.md

<interfaces>
<!-- Existing types from packages/types/src/index.ts that auth will use -->
From packages/types/src/index.ts:
```typescript
export const Role = { CUSTOMER: 'CUSTOMER', ADMIN: 'ADMIN', SUPER_ADMIN: 'SUPER_ADMIN' } as const;
export type Role = typeof Role[keyof typeof Role];
```

From apps/server/src/config/index.ts:
```typescript
export const config = {
  clerkSecretKey: process.env.CLERK_SECRET_KEY!,
  // ...
};
```

From .env.example:
```
CLERK_SECRET_KEY=sk_test_xxxxx
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Clerk SDKs and create auth type declarations</name>
  <files>apps/client/package.json, apps/admin/package.json, apps/server/package.json, packages/types/src/index.ts, .env.example</files>
  <action>
    1. Install @clerk/nextjs in both Next.js apps:
       - `pnpm add @clerk/nextjs --filter client`
       - `pnpm add @clerk/nextjs --filter admin`

    2. Install @clerk/express and svix in the server app:
       - `pnpm add @clerk/express svix --filter server`

    3. Update .env.example to add Clerk webhook signing secret:
       Add `CLERK_WEBHOOK_SIGNING_SECRET=whsec_xxxxx` after the existing NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY line.
       Also add `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in` and `NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up` for client app,
       and comment group these Clerk vars together.

    4. Add custom JWT session claims type declaration. Create a new file `packages/types/src/clerk.d.ts` with:
       ```typescript
       export type Roles = 'CUSTOMER' | 'ADMIN' | 'SUPER_ADMIN';

       declare global {
         interface CustomJwtSessionClaims {
           metadata: {
             role?: Roles;
           };
         }
       }

       export {};
       ```
       Also add `/// <reference path="./clerk.d.ts" />` at the top of packages/types/src/index.ts so the global declaration is picked up.

    Note: Do NOT use authMiddleware() (deprecated). Use clerkMiddleware() (current API per Core 2+).
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && pnpm list @clerk/nextjs --filter client --depth 0 && pnpm list @clerk/nextjs --filter admin --depth 0 && pnpm list @clerk/express --filter server --depth 0 && pnpm list svix --filter server --depth 0</automated>
  </verify>
  <done>@clerk/nextjs installed in client and admin, @clerk/express and svix installed in server, .env.example updated with all Clerk env vars, custom JWT session claims type declared</done>
</task>

<task type="auto">
  <name>Task 2: Configure ClerkProvider in both Next.js apps and set up middleware with auth helpers</name>
  <files>apps/client/src/app/layout.tsx, apps/admin/src/app/layout.tsx, apps/client/middleware.ts, apps/admin/middleware.ts, apps/client/src/lib/auth.ts, apps/admin/src/lib/auth.ts</files>
  <action>
    1. Update apps/client/src/app/layout.tsx:
       - Import ClerkProvider from @clerk/nextjs
       - Wrap the entire body content with ClerkProvider
       - Keep existing metadata, styling, and nav structure
       - Add auth-aware nav items: show UserButton from @clerk/nextjs when signed in, show Sign In link when not
       - Use SignedIn/SignedOut components from @clerk/nextjs for conditional rendering in nav

    2. Update apps/admin/src/app/layout.tsx:
       - Import ClerkProvider from @clerk/nextjs
       - Wrap the entire body content with ClerkProvider
       - Keep existing sidebar and layout structure
       - Add UserButton from @clerk/nextjs in the sidebar header area

    3. Create apps/client/middleware.ts (Next.js middleware file, must be at app root not in src/):
       ```typescript
       import { clerkMiddleware, createRouteMatcher } from '@clerk/nextjs/server';

       const isPublicRoute = createRouteMatcher([
         '/',
         '/products(.*)',
         '/categories(.*)',
         '/sign-in(.*)',
         '/sign-up(.*)',
         '/api/webhooks(.*)',
       ]);

       export default clerkMiddleware(async (auth, req) => {
         if (!isPublicRoute(req)) {
           await auth.protect();
         }
       });

       export const config = {
         matcher: [
           '/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)',
           '/(api|trpc)(.*)',
         ],
       };
       ```

    4. Create apps/admin/middleware.ts (Next.js middleware file, must be at app root):
       - Use clerkMiddleware with createRouteMatcher
       - Mark /unauthorized as public route
       - For all other routes: check auth, then check sessionClaims.metadata.role
       - If role is not ADMIN or SUPER_ADMIN, redirect to /unauthorized
       - Import NextResponse for redirect

    5. Create apps/client/src/lib/auth.ts:
       - Export async function checkRole(role: Roles) that uses auth() from @clerk/nextjs/server
       - Returns boolean based on sessionClaims.metadata.role match
       - Import Roles type from packages/types/src/clerk.d.ts or define locally

    6. Create apps/admin/src/lib/auth.ts:
       - Export async function checkRole(role: Roles) same as client
       - Export async function requireAdmin() that checks for ADMIN or SUPER_ADMIN, redirects to /unauthorized if neither
       - Use redirect from next/navigation
  </action>
  <verify>
    <automated>cd /Users/muhemmedibrahimov/work/ecom-template && grep -l "ClerkProvider" apps/client/src/app/layout.tsx apps/admin/src/app/layout.tsx && grep -l "clerkMiddleware" apps/client/middleware.ts apps/admin/middleware.ts && grep -l "checkRole" apps/client/src/lib/auth.ts apps/admin/src/lib/auth.ts</automated>
  </verify>
  <done>Both Next.js apps wrap content in ClerkProvider, both have clerkMiddleware() configured (client with public routes for store pages, admin with role-based access check), and both have auth helper utilities exported</done>
</task>

</tasks>

<verification>
- ClerkProvider wraps both Next.js app layouts
- clerkMiddleware() is configured in both middleware.ts files
- Client middleware allows public access to /, /products, /sign-in, /sign-up
- Admin middleware redirects non-admin users to /unauthorized
- Auth helpers (checkRole, requireAdmin) are exported from lib/auth.ts in both apps
- All Clerk packages installed (check with pnpm list)
- .env.example has all required Clerk environment variables
- Custom JWT session claims type is declared globally
</verification>

<success_criteria>
- @clerk/nextjs installed in client and admin apps
- @clerk/express and svix installed in server app
- Both Next.js apps use ClerkProvider in root layout
- Both Next.js apps have clerkMiddleware() in middleware.ts
- Auth helper functions created in both apps
- Custom JWT session claims type declared
- Session persistence is structurally enabled (AUTH-04 foundation)
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-system/02-01-SUMMARY.md`
</output>
